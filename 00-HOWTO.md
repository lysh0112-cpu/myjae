# 넣는 법 — 2026-07-28 통합테스트 후속 수정 (2차)

```
바탕    1차(용신·격국) 수정이 이미 들어간 저장소
파일    lib/saju 다섯 개만 바뀝니다
검사    npx tsc  0건 · 교재 사례 19/19 · E2E 5단계 관통
```

## 넣는 법

```bash
cd myjae                                  # package.json 있는 곳
unzip -o ../myjae-src2-20260728.zip       # lib/ 만 들어 있습니다
git status --porcelain                    # ★ M 다섯 줄만 나와야 합니다
npx tsc --noEmit --skipLibCheck           # 0건
npx next build
```

⚠️ **지난번처럼 폴더가 새로 생기지 않습니다.** 압축 안에 `lib/saju/*.ts` 다섯 개뿐입니다.

## 바뀌는 파일 다섯

```
lib/saju/jaryoPick.ts          ★지지 합(육합·삼합·방합)을 재료에 연결
lib/saju/solarterm.ts          절입 시각 분=60 올림 처리
lib/saju/sinsalTable.ts        「양인살」→「양인격」 · 괴강살 문구 →「양인일주」
lib/saju/toTongbyeonInput.ts   격국·조후·세 용신 일치를 통변 재료에 배선
lib/saju/tongbyeonPrompt.ts    프롬프트에 그 네 줄을 싣는 자리
```

## 넣은 뒤 눈으로 볼 것

```
사주보기   재료에 「巳午未 화국(방합) · 월지-일지-시지 — …」 같은 줄이 새로 붙는지
           프롬프트에 「사회에서 쓰는 그릇(격)」·「세 기운이 모이는가」 두 줄이 있는지
대운·세운   합 줄이 늘어난 만큼 상한에 걸리지 않는지
```

## ⚠️ 대표님이 정하실 것 하나 — 상한

지지 합이 들어오면서 재료가 무거워졌습니다.

```
사주보기 상한 1400 · 손님이 3갈래를 골랐을 때 잘리는 명식
   1400자   9.2%
   1500자   5.4%
   1600자   2.9%
   1800자   0.6%
```

지금은 **1400 그대로** 두었습니다. 우선순위가 낮은 need 부터 덜리므로 「일간·오행·육친·충·합」은 남습니다.
넉넉하게 가시려면 `jaryoPick.SERVICE_BUDGET.saju` 한 곳만 고치면 됩니다.

## 이번에 안 한 것

```
준삼합·반합 (교재 81쪽)   두 글자로도 서는 것이라 걸리는 명식이 크게 늡니다.
                          넣으실 거라면 걸림 비율부터 재십시오. (교훈 BO)
통관용신 · 병약용신        교재 151쪽에 있으나 아직 미구현
```
