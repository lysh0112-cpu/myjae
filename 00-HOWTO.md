# 넣는 법 — 2026-07-28 통합테스트 후속 수정 (2차 · 최종)

```
바탕    1차(용신·격국) 수정이 이미 들어간 저장소
파일    lib/saju 다섯 개만 바뀝니다  (+142 / -8)
검사    npx tsc 0건 · 교재 격국 사례 19/19 · E2E 5단계 관통
```

## 넣는 법

```bash
cd myjae                                  # package.json 있는 곳
unzip -o ../myjae-src2-20260728.zip       # lib/saju/*.ts 다섯 개만 들어 있습니다
git status --porcelain                    # ★ M 다섯 줄만 나와야 합니다
npx tsc --noEmit --skipLibCheck           # 0건
npx next build
git add -A && git commit -m "통합테스트 후속 — 지지 합·격국 주입·절입 반올림·신살 명칭·상한 1600" && git push
```

⚠️ **폴더가 새로 생기지 않습니다.** 압축 안에 `lib/saju/` 다섯 개뿐입니다.

## 바뀌는 파일 다섯

```
lib/saju/jaryoPick.ts          ★지지 합(육합·삼합·방합) 연결 + 상한 saju 1400→1600
lib/saju/solarterm.ts          절입 시각 분=60 올림 처리
lib/saju/sinsalTable.ts        「양인살」→「양인격」 · 괴강살 문구 →「양인일주」
lib/saju/toTongbyeonInput.ts   격국·조후·세 용신 일치를 통변 재료에 배선
lib/saju/tongbyeonPrompt.ts    프롬프트에 그 네 줄을 싣는 자리
```

## ★대표님 확정 (2026-07-28)

```
① SERVICE_BUDGET.saju = 1600
     지지 합이 들어오면서 재료가 무거워졌습니다.
     3갈래 기준 잘림  1400자 9.2%  →  1600자 2.9%
     ⚠️ 나머지 여섯(unse·couple·exam·monthly·mulsang)은 그대로입니다.
        대운·세운 재료는 500자 안팎이라 여유가 있습니다.

② 준삼합·반합(교재 81쪽)은 넣지 않는다
     "재료가 너무 비대해지는 것을 막고 core 합(방합·삼합·육합) 위주로 통변한다"
     ★코드 주석에 확정으로 적어 두었습니다. 다음 사람이 되살리지 않도록.
```

## 넣은 뒤 눈으로 볼 것

```
사주보기   재료에 「巳午未 화국(방합) · 월지-일지-시지 — …」 같은 줄이 새로 붙는지
           프롬프트에 「사회에서 쓰는 그릇(격)」·「세 기운이 모이는가」 두 줄이 있는지
           ★재료가 길어졌으니 통변 길이·말투가 흔들리지 않는지
대운·세운   합 줄이 늘어난 만큼 상한(1200)에 걸리지 않는지
진로적성    상한이 원래 1600이라 안 바뀝니다
```

## 검증 스크립트

`04-verify-19-cases.py` (1차 묶음에 있음) 를 그대로 쓰십시오. 19/19 가 나와야 합니다.

## 아직 안 한 것

```
통관용신 · 병약용신 · 종격 방향     교재 151쪽에 있으나 미구현
子午卯酉月 규칙 · 합충형파 무력화   교재 157쪽. 격 판정 세부
극신약·중화 등급                   교재에 없음. 우리가 만든 것
편인격 상신                        151쪽 표(관성) vs 176쪽 본문(재성) — 연재쌤 확인
己 일간 양인격                     교재 178쪽과 부딪힘 — 연재쌤 확인 (1차 묶음 5장)
```
