# 넣는 법 — 2026-07-28 3차 (방합 기준 통일 · A등급 순화)

```
바탕    1차(용신·격국) + 2차(지지 합·상한 1600) 가 이미 들어간 저장소
파일    두 개만 바뀝니다  (+65 / -14)
검사    npx tsc 0건 · A등급 30칸 전수 통과
```

## 넣는 법

```bash
cd myjae
unzip -o ../myjae-src3-20260728.zip
git status --porcelain          # ★ M 두 줄만 나와야 합니다
npx tsc --noEmit --skipLibCheck
npx next build
git add -A
git commit -m "방합·삼합 기준 통일(3글자) · 144칸 A등급 순화"
git push
```

## 바뀌는 파일 둘

```
app/manseryeok/result-new/ExpertDetail.tsx   방합·삼합을 3글자 완성으로만 표시
lib/saju/jijiGrade.ts                        A등급 여섯 자리 어투 순화
```

---

# ① 방합·삼합 기준 통일

**전** — 두 글자만 있어도 방합·삼합으로 표시
**후** — 세 글자(寅卯辰·巳午未·申酉戌·亥子丑)가 다 있어야

```
보기)  壬辰 丙寅 乙酉 乙亥
   전  辰-寅 「방합」   ← 卯 가 없는데 떴습니다 (반방합)
   후  辰-寅  없음      ← 통변 재료와 같아졌습니다
```

**전수 실측 — 지지 네 자리 20,736가지 중 96.2%에서 표시가 달라집니다.**
도표의 선과 격자표 뱃지가 눈에 띄게 줄어듭니다. **거짓 표시가 사라지는 것입니다.**

⚠️ **삼합도 함께 고쳤습니다.** 준삼합(두 글자)이 곧 삼합의 반합이라 결이 같습니다.
   방합만 고치면 삼합 쪽에 같은 어긋남이 그대로 남습니다.

⚠️ 되돌리시려면 `relationOf` 의 `every(has)` 두 군데만 지우면 됩니다.
   다만 그때는 `jaryoPick.jijiHapBrief` 도 함께 풀어야 합니다. **한쪽만 고치지 마십시오.**

---

# ② 144칸 A등급 순화 — 여섯 자리

33-8장에서 **D등급**의 병명·센 말 아홉 자리를 다듬었는데, **A등급의 단정하는 말은 그대로 남아 있었습니다.**
실제 통변에 「재물의 결실이 확실하게 온다」가 나가는 것을 보고 찾았습니다.

**★뜻은 하나도 안 뺐습니다. 어투만 옮겼습니다.** (교훈 BR)

```
子辰合   "고지가 되면 발복하지만 묘지가 되면 죽는다"
       → "고지로 서면 힘을 받고 묘지로 서면 기운이 크게 가라앉으니 어느 쪽인지 함께 보셔야"
       ★33-8장에서 막은 종류입니다

亥卯木   "주식 투자, 재테크에 유리하다. 공격적으로 투자해도 된다"
       → "재물 쪽으로 문이 열리기 쉽습니다. 다만 투자는 평소 잣대로 살펴 결정하시는 편이"
       ★손님이 믿고 움직였다가 잃으면 다치는 자리입니다

午戌火   "창업, 부동산에 투자할 때는 午火가 올 때 해라"
       → "창업이나 부동산을 살피신다면 午火가 드는 때가 결이 맞습니다"

寅午合   "확실한 재성운이 온다" · "원하는 일이 성사된다"
       → "재물 쪽에 기운이 모이는 때로 봅니다" · "문이 열리기 쉬운 때입니다"

巳酉金局 "부동산 횡재운으로 대단히 긍정적이다"
       → "부동산 쪽으로 뜻밖의 결실이 따르기 쉬워 대단히 긍정적으로 봅니다"

암합     "애정사로 불륜이 발생할 수 있다"
       → "마음이 밖으로 향하기 쉬우니 인연의 선을 스스로 정해 두시면 좋습니다"

午未合   "그럴 때 배신수를 조심해라"
       → "믿던 자리에서 어긋나는 일이 없도록 살펴 주십시오"
```

**검증** — A등급 30칸 전수에서 `죽는다·확실한·횡재·불륜·공격적으로 투자·해라·성사된다` 0건.

⚠️ **되살리지 마십시오.** 이 표는 대운·세운 재료로 AI 에게 그대로 나갑니다.
   沖 쪽은 33-3장에서 이미 같은 잣대로 다듬었습니다.
   `[안 씀] "좋은 일이 생깁니다"  [씀] "자리가 움직이는 때로 봅니다"`

---

## 아직 안 한 것

```
B·C 등급        같은 눈으로 안 훑었습니다. 다음에 보십시오
「재물」 갈래     CATEGORY_NEEDS['재물'] 에 「합」 이 없습니다
                노후·재물 · 직업·사업 · 취업 도 함께 봐야 합니다
```

## 넣은 뒤 눈으로 볼 것

```
사주보기 → 전문가 상세 → 형충회합
   도표의 선이 줄었는지 (거짓 방합·삼합이 사라짐)
   격자표 뱃지도 함께 줄었는지
대운·세운 통변
   "확실한"·"투자해도 된다" 같은 말이 사라졌는지
```
