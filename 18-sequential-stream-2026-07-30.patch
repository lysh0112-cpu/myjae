diff --git a/14-verify-exam-seven.ts b/14-verify-exam-seven.ts
index 5509fa1..51209f6 100644
--- a/14-verify-exam-seven.ts
+++ b/14-verify-exam-seven.ts
@@ -115,7 +115,7 @@ console.log('  합격운 7대 카테고리 — 진학/취업 두 벌 관통 검
 console.log('════════════════════════════════════════════════════════\n')
 
 // ── ① 갈래가 빠짐없이 한 번씩 ─────────────────────────────────────
-console.log('① 일곱 갈래가 세 묶음에 빠짐없이 한 번씩 들어가는가')
+console.log('① 일곱 갈래가 묶음에 빠짐없이 한 번씩 들어가는가')
 {
   const flat = SEVEN_GROUPS.flat()
   const dupe = flat.filter((k, i) => flat.indexOf(k) !== i)
diff --git a/app/manseryeok/exam-luck-result/components/ExamResultShell.tsx b/app/manseryeok/exam-luck-result/components/ExamResultShell.tsx
index 8e79ac4..fa8944a 100644
--- a/app/manseryeok/exam-luck-result/components/ExamResultShell.tsx
+++ b/app/manseryeok/exam-luck-result/components/ExamResultShell.tsx
@@ -97,8 +97,14 @@ function ExamLuckResultInner({ mode }: { mode: ExamMode }) {
   const [dayunList, setDayunList] = useState<DayunItem[]>([])
   const [tong, setTong] = useState('')
   const [tongState, setTongState] = useState<'idle' | 'loading' | 'done' | 'failed'>('idle')
-  /** ★2026-07-29 — 세 묶음 가운데 몇이 왔나. 기다리는 손님에게 보여 줍니다. */
+  /**
+   * 몇 번째 묶음까지 받았나.
+   * ⚠️ ★2026-07-30 (2차) — **화면에는 안 씁니다.** 손님에게 «묶음» 은 우리 사정입니다.
+   *    개발 중에 «어디까지 왔나» 를 보려고 남겨 둔 값입니다.
+   *    지우지 마십시오 — 묶음이 몇 번째에서 막히는지 확인할 때 씁니다.
+   */
   const [doneGroups, setDoneGroups] = useState(0)
+  void doneGroups
   /**
    * ★2026-07-30 — 왜 실패했는지. 전에는 상태코드를 버려서 아무도 몰랐습니다.
    * ⚠️ 손님에게는 부드럽게 보여 주고, 개발자용 자세한 말은 title 속성에 담습니다.
@@ -331,29 +337,53 @@ function ExamLuckResultInner({ mode }: { mode: ExamMode }) {
       //      「■ 🧬 1. 타고」 처럼 반만 온 제목도 dna 로 잡힙니다.
       // ══════════════════════════════════════════════════════════
 
+      // ══════════════════════════════════════════════════════════
+      //  ★★2026-07-30 (2차) — 진로적성과 «같은 구조» 로 단순화했습니다.
+      //                        (대표님 지시 — 속도보다 끊기지 않는 것이 최우선)
+      //
+      //   [무엇을 버렸나] 세 묶음을 Promise.all 로 «나란히» 부르던 것.
+      //     나란히 부르면 빠르지만 실패할 길이 셋으로 늘어납니다 —
+      //       · 같은 키로 스트림 셋이 동시에 열려 분당 출력 토큰(OTPM)을 다툽니다
+      //         (묶음 상한 2,400+2,400+3,600 = 8,400 을 한꺼번에 잡아 둡니다)
+      //       · 셋 중 하나만 막혀도 손님 화면은 «덜 온» 상태로 남습니다
+      //       · 어느 것이 왜 막혔는지 손님도 우리도 알기 어렵습니다
+      //
+      //   [무엇으로 바꿨나] career-result/page.tsx 와 같은 얼개.
+      //     ① 한 번에 «하나만» 부릅니다. 끝나면 다음 것을 부릅니다.
+      //     ② 받은 글을 acc 하나에 계속 이어 붙이고, 조각마다 setTong(acc).
+      //        → 손님에게는 «끊기지 않는 한 흐름» 으로 보입니다.
+      //     ③ 묶음을 넷으로 잘게 쪼갰습니다(2+2+2+1). 한 호출이 짧아
+      //        Vercel 함수 시간 제한에 걸릴 여지가 줄어듭니다.
+      //
+      //   ⚠️⚠️ «한 번에 일곱 갈래» 로는 안 갑니다. 35부가 실측한 자리입니다.
+      //        여섯 대목 한 번 → 두셋만 씀 · 세 대목 한 번 → 첫 하나만 씀 (교훈 CV)
+      //        프롬프트를 네 번 다듬어도 같은 자리에서 실패했습니다.
+      //        ★그래서 «호출은 차례로, 화면은 한 흐름» 으로 풀었습니다.
+      //          손님 눈에는 진로적성과 똑같고, AI 에게는 한 번에 둘뿐입니다.
+      //
+      //   ⚠️ 차례로 부르니 전체 시간이 늘어납니다(대략 두 배).
+      //      대표님이 그렇게 정하셨습니다. 되돌리려면 아래 for 를
+      //      Promise.all 로 바꾸면 되지만, 위에 적은 세 위험이 함께 돌아옵니다.
+      // ══════════════════════════════════════════════════════════
+
       /** 조각이 이만큼(밀리초) 안 오면 그 묶음은 멈춘 것으로 본다 */
-      const STALL_MS = 45000
+      const STALL_MS = 60000
       /** 한 묶음이 아무리 길어도 이 시간을 넘기지 않는다 */
-      const HARD_MS = 240000
+      const HARD_MS = 180000
 
-      /** 묶음별로 지금까지 받은 글 — ★ref 로 둡니다 (클로저가 낡지 않게) */
-      const partsRef: string[] = ['', '', '']
+      /** ★진로적성과 같이 «하나의 글» 로 쌓습니다. 묶음별로 나누지 않습니다. */
+      let acc = ''
       /** 실패 이유 — 화면에 알려 줍니다 */
       const failNotes: string[] = []
 
-      /** 지금까지 받은 것을 차례대로 이어 붙여 화면에 올린다 */
-      const flush = () => {
-        if (cancelled) return
-        setTong(partsRef.filter(Boolean).join('\n\n'))
-      }
-
       /**
-       * 한 묶음을 받아 온다 — 실패해도 다른 묶음은 살린다.
-       * @param idx 묶음 차례. partsRef 자리를 정합니다 (순서가 곧 리포트 차례)
+       * 한 묶음을 받아 acc 에 이어 붙인다.
+       * ★career-result/page.tsx 178~245줄과 같은 얼개입니다. 새로 설계하지 않았습니다.
+       * @returns 한 글자라도 받았는가
        */
-      const runGroup = async (g: typeof SEVEN_GROUPS[number], idx: number): Promise<string> => {
+      const runGroup = async (g: typeof SEVEN_GROUPS[number], idx: number): Promise<boolean> => {
         const sp = buildSevenPrompt(sevenArgs, g)
-        if (!sp) return ''
+        if (!sp) return false
 
         // ★멈춤 감지 — 조각이 올 때마다 시계를 되감습니다
         const ac = new AbortController()
@@ -363,101 +393,93 @@ function ExamLuckResultInner({ mode }: { mode: ExamMode }) {
           if (stallTimer) clearTimeout(stallTimer)
           stallTimer = setTimeout(() => ac.abort('stall'), STALL_MS)
         }
+        const before = acc.length
 
         try {
-          const r = await fetch('/api/tongbyeon', {
+          const res = await fetch('/api/tongbyeon', {
             method: 'POST', headers: { 'Content-Type': 'application/json' },
             signal: ac.signal,
-            // ★묶음이 실제로 쓰는 양에 맞춰 상한을 좁힙니다.
-            //   한 갈래가 5~8문장이니 갈래당 1,200 토큰이면 넉넉합니다.
-            //   상한을 크게 잡으면 모델이 그만큼 여유를 두고 생성해 느려집니다. (교훈 CX)
+            // ★묶음이 실제로 쓰는 양에 맞춰 상한을 좁힙니다. (교훈 CX)
+            //   상한을 크게 잡으면 모델이 그만큼 여유를 두고 생성해 느려집니다.
             body: JSON.stringify({
               systemPrompt: sp, premium: true,
               maxTokens: g.length * 1200,
             }),
           })
-          // ★2026-07-30 — 이유를 버리지 않습니다.
-          //   전에는 `if (!r.ok || !r.body) return ''` 로 상태코드를 통째로 버려서,
-          //   401(키 없음)·429(한도)·529(붐빔)를 구분할 수 없었습니다.
-          if (!r.ok) {
+          // ★실패 이유를 버리지 않습니다. 401·429·529 는 대응이 전혀 다릅니다.
+          if (!res.ok) {
             let why = ''
-            try { why = (await r.text()).slice(0, 200) } catch { /* 본문을 못 읽어도 status 는 남는다 */ }
-            failNotes.push(`${idx + 1}묶음 HTTP ${r.status}${why ? ` — ${why}` : ''}`)
-            console.error('합격운 묶음 실패', g, r.status, why)
-            return ''
+            try { why = (await res.text()).slice(0, 160) } catch { /* 본문을 못 읽어도 status 는 남는다 */ }
+            failNotes.push(`${idx + 1}묶음 HTTP ${res.status}${why ? ` — ${why}` : ''}`)
+            console.error('합격운 묶음 실패', g, res.status, why)
+            return false
           }
-          if (!r.body) { failNotes.push(`${idx + 1}묶음 — 응답 본문이 비었습니다`); return '' }
+          if (!res.body) { failNotes.push(`${idx + 1}묶음 — 응답 본문이 비었습니다`); return false }
 
-          const rd = r.body.getReader()
-          const dec = new TextDecoder()
-          let buf2 = ''
-          let out = ''
-          const take2 = (line: string) => {
+          const reader = res.body.getReader()
+          const decoder = new TextDecoder()
+          // ★청크가 줄 중간에 잘릴 수 있다. buf 로 완성된 줄만 처리한다. (교훈 AG)
+          let buf = ''
+          const take = (line: string) => {
             if (!line.startsWith('data: ')) return
             const d = line.slice(6)
             if (d === '[DONE]') return
             try {
-              const j = JSON.parse(d)
-              if (j.text) {
-                out += j.text
-                // ★★여기가 고친 자리입니다 — 조각마다 화면에 올립니다
-                partsRef[idx] = out
-                flush()
-              }
+              const parsed = JSON.parse(d)
+              // ★★진로적성 229줄과 같은 한 줄 — 조각마다 화면을 갱신합니다
+              if (parsed.text) { acc += parsed.text; if (!cancelled) setTong(acc) }
             } catch { /* 부서진 줄 — SSE 는 조각으로 오므로 정상입니다 */ }
           }
 
           armStall()
           for (;;) {
-            const { done, value } = await rd.read()
+            const { done, value } = await reader.read()
             if (done) break
             armStall()
-            buf2 += dec.decode(value, { stream: true })
-            const ls2 = buf2.split('\n')
-            buf2 = ls2.pop() ?? ''
-            for (const l of ls2) take2(l)
+            buf += decoder.decode(value, { stream: true })
+            const ls = buf.split('\n')
+            buf = ls.pop() ?? ''
+            for (const line of ls) take(line)
           }
-          buf2 += dec.decode()
-          if (buf2.trim()) take2(buf2.trim())
-          return out
+          // ★마지막 줄에 개행이 없으면 여기 남는다. 버리면 끝 문장이 잘린다. (2026-07-27)
+          buf += decoder.decode()
+          if (buf.trim()) take(buf.trim())
+          return acc.length > before
         } catch (e) {
-          // ★중간까지 받은 것은 버리지 않습니다. 반쪽이라도 손님에게 값이 됩니다.
-          const partial = partsRef[idx]
+          // ★중간까지 받은 것은 acc 에 이미 들어가 있습니다. 버리지 않습니다.
           const why = ac.signal.aborted
             ? (String(ac.signal.reason) === 'stall'
                 ? `${STALL_MS / 1000}초 동안 글이 안 와서 멈췄습니다`
                 : `${HARD_MS / 1000}초를 넘겨 끊었습니다`)
             : String(e)
-          failNotes.push(`${idx + 1}묶음 — ${why}${partial ? ' (받은 만큼은 살렸습니다)' : ''}`)
+          const got = acc.length - before
+          failNotes.push(`${idx + 1}묶음 — ${why}${got > 0 ? ` (${got}자는 살렸습니다)` : ''}`)
           console.error('합격운 묶음 실패', g, why)
-          return partial
+          return got > 0
         } finally {
           clearTimeout(hardTimer)
           if (stallTimer) clearTimeout(stallTimer)
         }
       }
 
-      // ★나란히 부르고, 조각이 올 때마다 차례를 맞춰 이어 붙인다
-      //   ⚠️ 차례로(순차) 부르지 않습니다. 셋을 잇달아 부르면 시간이 세 배입니다.
-      //      ★대신 이제는 «먼저 오는 묶음의 글» 이 바로 화면에 뜹니다.
+      // ★차례로 부릅니다 — 한 번에 하나. 화면에는 한 흐름으로 이어집니다.
       try {
-        await Promise.all(SEVEN_GROUPS.map(async (g, i) => {
-          await runGroup(g, i)
-          if (!cancelled) {
-            flush()
-            setDoneGroups(partsRef.filter(Boolean).length)
-          }
-        }))
-        const acc = partsRef.filter(Boolean).join('\n\n')
-        // ★하나도 못 받았을 때만 실패로 봅니다. 두 묶음이라도 왔으면 보여 줍니다.
+        for (let i = 0; i < SEVEN_GROUPS.length; i++) {
+          if (cancelled) return
+          setDoneGroups(i)
+          // 묶음 사이에 빈 줄을 둡니다 — 파서가 ■ 로 자르므로 앞에 개행이 있어야 합니다
+          if (acc && !acc.endsWith('\n\n')) { acc += '\n\n'; setTong(acc) }
+          await runGroup(SEVEN_GROUPS[i], i)
+        }
+        if (cancelled) return
+        setDoneGroups(SEVEN_GROUPS.length)
+
+        // ★하나도 못 받았을 때만 실패로 봅니다. 일부라도 왔으면 보여 줍니다.
         if (!acc.trim()) {
-          if (!cancelled) {
-            setFailWhy(failNotes.join(' · ') || '까닭을 알 수 없습니다')
-            setTongState('failed')
-          }
+          setFailWhy(failNotes.join(' · ') || '까닭을 알 수 없습니다')
+          setTongState('failed')
           return
         }
-        if (cancelled) return
         setTong(acc)
         // ★일부만 온 경우도 알려 줍니다. 조용히 넘기면 손님이 «왜 짧지» 합니다.
         if (failNotes.length) setFailWhy(failNotes.join(' · '))
@@ -584,32 +606,22 @@ function ExamLuckResultInner({ mode }: { mode: ExamMode }) {
           <div style={{
             textAlign: 'center', padding: '16px 0 18px', color: ACCENT, fontSize: 13,
           }}>
-            {/* ★2026-07-30 — 여기도 «돌고 있음» 을 보여 줍니다. 막대만 있으면 멈춘 줄 압니다. */}
+            {/* ★2026-07-30 (2차) — «n/3 묶음» 표시를 걷어냈습니다. (대표님 지시)
+                 [왜] 손님에게 «묶음» 은 우리 사정입니다. 알 필요도 없고,
+                   숫자가 0 에 머물면 멈춘 줄 압니다. 실제로 그렇게 보였습니다.
+                 → 진로적성과 같게 «쓰는 중» 하나만 둡니다.
+                   ★진짜 진행 표시는 위에서 «글이 차오르는 것» 자체입니다.
+                 ⚠️ 다만 차례로 부르니 2~3분 걸립니다. 그동안 아무 숫자도 없으면
+                    역시 불안하므로 «몇 자 왔나» 만 조용히 보여 줍니다. */}
             <style>{`@keyframes spin { from { transform: rotate(0deg); } to { transform: rotate(360deg); } }`}</style>
             <div>
               <span style={{ display: 'inline-block', animation: 'spin 1.2s linear infinite', marginRight: 6 }}>✦</span>
               풀이를 쓰는 중이에요… 조금만 기다려 주세요
             </div>
-            <div style={{
-              display: 'flex', gap: 5, justifyContent: 'center', marginTop: 10,
-            }}>
-              {[0, 1, 2].map(i => (
-                <span key={i} style={{
-                  width: 30, height: 4, borderRadius: 20,
-                  background: i < doneGroups ? ACCENT : '#f0dbe4',
-                  transition: 'background .3s',
-                }} />
-              ))}
-            </div>
             <div style={{ fontSize: 11, color: '#b08a9a', marginTop: 7 }}>
-              {/* ★2026-07-30 — 「0/3 묶음」만 떠 있어 멈춘 것처럼 보였습니다. (대표님 지적)
-                   [왜] 묶음 개수는 «다 끝난 묶음» 만 셉니다. 첫 묶음이 끝나기까지
-                     1~2분이니 그동안 늘 0/3 이었습니다.
-                   → 이제 글이 오는 대로 위에 뜨고, 여기에는 «몇 자 왔나» 를 보여 줍니다.
-                     숫자가 움직이면 손님이 멈춘 줄 알지 않습니다. */}
-              {doneGroups}/3 묶음 마무리
-              {tong.length > 0 && ` · ${tong.length.toLocaleString()}자 도착`}
-              {tong.length === 0 && ' · 첫 문장을 기다리고 있어요'}
+              {tong.length > 0
+                ? `${tong.length.toLocaleString()}자 · 계속 이어서 쓰고 있어요`
+                : '첫 문장을 기다리고 있어요'}
             </div>
           </div>
         )}
diff --git a/lib/saju/examLuck/buildExamSeven.ts b/lib/saju/examLuck/buildExamSeven.ts
index c08164f..30af763 100644
--- a/lib/saju/examLuck/buildExamSeven.ts
+++ b/lib/saju/examLuck/buildExamSeven.ts
@@ -101,13 +101,29 @@ export function sevenOf(target: ExamTarget): SevenSection[] {
 }
 
 /**
- * 세 묶음으로 나눈다 — 한 번에 둘셋이면 AI 가 다 씁니다. (교훈 CV)
- * ⚠️ 이 묶음을 넷으로 쪼갤 일이 생기면 화면의 진행 표시(«n/3 묶음»)도 함께 고치십시오.
+ * 갈래를 묶음으로 나눈다 — 한 번에 «둘까지» 만 시킵니다.
+ *
+ * ── ⚠️ 왜 «한 번에 일곱» 이 안 되는가 (교훈 CV — 35부가 실측한 것) ────
+ *   여섯 대목 한 번  →  앞 두셋만 씀
+ *   세 대목 한 번    →  첫 하나만 씀
+ *   프롬프트를 네 번 다듬어도 같은 자리에서 실패했습니다.
+ *   ★대목 수가 문제가 아니라 «여러 대목을 한 번에» 가 문제입니다.
+ *
+ * ── ★2026-07-30 — 셋에서 넷으로 쪼갰습니다 (대표님 지시로 «순차» 로 바꾸면서) ──
+ *   전   2 + 2 + 3   (나란히 호출)   ← 3묶음이 3,600 토큰으로 가장 길었습니다
+ *   후   2 + 2 + 2 + 1 (차례로 호출)
+ *   [왜] 이제 한 번에 하나만 부릅니다. 묶음을 잘게 쪼개면
+ *        ① 한 번의 함수 호출이 짧아져 Vercel 시간 제한에 안 걸립니다
+ *        ② 한 묶음이 실패해도 잃는 것이 적습니다
+ *        ③ 한 번에 둘뿐이니 교훈 CV 로부터 더 안전합니다
+ *   ⚠️ 차례로 부르니 전체 시간은 늘어납니다. 대표님이
+ *      «속도보다 끊기지 않는 것이 최우선» 이라 정하셨습니다.
  */
 export const SEVEN_GROUPS: SevenKey[][] = [
   ['dna', 'subject'],
   ['ratio', 'monthly'],
-  ['dday', 'apply', 'mentor'],
+  ['dday', 'apply'],
+  ['mentor'],
 ]
 
 // ══════════════════════════════════════════════════════════════════
