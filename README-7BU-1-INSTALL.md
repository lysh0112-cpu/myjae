# 7부 봉투 1 — 승진운 표 · 홈 카드 이름 바꾸기

```
날짜   2026-09-12
기준   저장소 50e99e4f 「통변수정9」 위에 얹습니다
파일   신설 둘 · 고침 다섯 = ★일곱
검사   2,360 → ★2,444 · 실패 0 · exit 0
타입   tsc --noEmit ★오류 0
eslint 85 / 147  ★기준선 그대로 (하나도 안 늘림)
AI     ⛔ 이번 봉투는 AI 를 ★한 번도 부르지 않습니다 (표와 이름뿐입니다)
```

---

## 1. 넣는 법 (세 걸음)

```
1  이 zip 을 풀면 폴더 모양이 저장소와 «똑같습니다».
   ⇒ 저장소 맨 위에 ★그대로 덮어쓰십시오. (폴더째 끌어다 놓으시면 됩니다)

2  터미널에서
      npm run verify        ← ★2,444 · 실패 0 이 나오면 정상
      npx tsc --noEmit      ← ★아무것도 안 나오면 정상

3  커밋 → 올리기 → Vercel 🟢Ready 확인
```

⚠️ **package.json 도 들어 있습니다.** 새 검사(50번)를 `npm run verify` 에 묶은 것뿐입니다.

---

## 2. 무엇이 바뀌나 — 손님 화면

```
홈 카드 이름   「합격운/취업운」  →  ★「합격운/취업운/승진운」
카드 소개      「시험과 일자리」  →  ★「시험 · 일자리 · 자리」
⚠️ 그 카드는 ★지금 «꺼져» 있습니다 (6부에서 처음 값을 «숨김» 으로 두었습니다).
   ⇒ 관리자 → 💰 가격 관리 → 「숨겨 둔 서비스」 토글을 켜야 보입니다.
   ⇒ ★이번 봉투로는 손님 화면이 «하나도» 안 바뀝니다. 켜실 때 바뀝니다.
```

🔴 **아직 «승진운» 화면은 없습니다.** 이름만 먼저 바꾼 것입니다.
   ⇒ 지금 그 카드를 누르면 **진학 / 취업 두 탭**만 나옵니다. 승진 탭은 다음 봉투입니다.

⇒ 그래서 **토글은 승진운 화면이 붙은 뒤에 켜시는 편이 낫습니다.**

---

## 3. 🔴 이번에 «밟을 뻔한» 자리 둘 — 검사가 잡아 주었습니다

### ① 압핀(찜)이 «말없이» 풀릴 뻔했습니다

```
[찾은 것]  압핀은 saju_records 의 title 에 ★«카드 이름 그대로» 저장됩니다
           (lib/saju/pinnedServices.ts — service_type='pinned' · title=serviceName)
⇒ 이름만 바꾸면 ★이미 「합격운/취업운」을 찜해 두신 분의 압핀이 «남의 것» 이 됩니다.
[고침]     ★옛 이름을 EXAM_LUCK_NAME_OLD 로 남기고,
           홈이 isExamLuckName() 으로 ★«둘 다» 내 것으로 읽습니다.
⇒ 검사에 박았습니다 — 옛 이름을 지우면 빨간불이 켜집니다.
```

### ② 이름이 «세 곳» 에 붙박이로 적혀 있었습니다

```
[겪은 일]  홈만 고쳤더니 검사가 ★실패 3 으로 멈춰 세웠습니다.
           홈 카드 · 서비스 낱장(SOLO_NAMES) · 검사 ★셋이 한 벌이었습니다.
[고침]     ⛔ 느슨하게 풀지 않고, ★이름을 lib/homeFlags.ts 한 곳에서만 오게 했습니다.
           낱장도  SOLO_NAMES = ['궁합', ★EXAM_LUCK_NAME]
[검사]     ★옛 검사보다 «더 엄해졌습니다» —
           전에는 «글자» 만 맞추어 세 파일이 따로 바뀌어도 못 잡았는데,
           지금은 ★«한 곳에서 오는지» 를 봅니다. 다시 갈릴 수 없습니다.
```

---

## 4. 🔴 다음 봉투에서 «반드시» 함께 고칠 자리 (지뢰)

```
buildExamSeven.ts:437   const isMove = isJob && v.jobSituation !== 'new'

⇒ JobSituation 에 'promote' 를 ★«그냥» 더하면
  'promote' !== 'new' 이므로 ★«이직» 으로 읽힙니다.
⇒ ⇒ 승진을 물어보신 손님께 ★「몸담은 곳을 옮기실 수 있습니다」 가 나갑니다.

⇒ ★검사 50번 ⑪ 이 «두 자리를 함께» 봅니다. 하나만 고치면 빨간불입니다.
```

⚠️ 5부에서 「절반만 고치고 끝났다」던 바로 그 종류의 자리입니다. 검사에 박아 두었습니다.

---

## 5. 파일 일곱

```
★신설 둘
  lib/saju/examLuck/tables/promotion.ts   ★승진운 표 (계산 없음 — 연재쌤 검수용)
  50-verify-promotion.ts                   ★그물 82항목

★고침 다섯
  lib/homeFlags.ts                  이름 한 곳 · 옛 이름 남김 · isExamLuckName()
  app/home-new/page.tsx             카드 이름·소개 · 필터를 isExamLuckName 으로
  app/home-new/components/ServiceSection.tsx   SOLO_NAMES 를 한 곳에서
  28-verify-naming-flow.ts          ㉒-u 를 «더 엄하게» + 검사 둘 더함
  package.json                      50번을 npm run verify 에 묶음
```

---

## 6. ⚠️ 이 표에 «제가 정한 값» 셋 — 연재쌤·대표님이 고치실 자리

```
① 「문」(결재하고 사람을 맡는 자리)이 ★어디인가
   회사 차장→부장 · 공무원 6급→5급 · 은행 차장→부지점장 ·
   경찰 경사→경위 · 소방 소방장→소방위 · 군인 원사→소위
   ⇒ ★교재 사례 한 건(『물(物), 형상 명리학』 — 偏印 «결재권»)에서 제가 끌어냈습니다.

② 한자말을 ★«한 갈래에 셋까지»
   ⇒ 「과하지 않게」 를 셀 수 있게 제가 어림으로 잡은 값입니다.
   ⇒ 실제 통변을 여러 벌 뽑아 보고 정해야 합니다 (교훈 BA — 문턱은 재고 정한다).

③ 인사 시기의 ★«두 달 앞» 이 눈여겨볼 달
   ⇒ 연말(12월) 인사 → 10월. 석 달 앞일 수도 있습니다.

⇒ 셋 다 promotion.ts ★«한 파일» 에 모여 있습니다. 고치실 때 여기만 보시면 됩니다.
```

⚠️ 그리고 직급표 여섯 벌은 **법령과 업계 관행에서 «찾은 값»** 입니다 (공무원법·경찰공무원법·소방·군인사법·은행 관행). 줄마다 출전을 적어 두었습니다.

---

```
문의 — lysh6728@naver.com · 010-6493-2252
```
