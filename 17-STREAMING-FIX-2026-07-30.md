# 🔧 「0/3 묶음에서 멈춤」 — 원인과 수정 (2026-07-30)

```
기준 커밋   3313bc6  (2026-07-30 12:41)   ← 대표님이 첫 패치를 푸시하신 그 커밋
고친 파일   2개  ·  +213 / −18
```

---

## 1. 결론부터 — 타임아웃도 한도도 아니었습니다

대표님이 주신 로그가 정답을 담고 있었습니다.

```
/api/tongbyeon   Status 200   ·   Error/Fatal 0건
```

**서버는 멀쩡했습니다.** 그런데 화면이 안 움직였습니다.
진로적성(잘 되는 쪽)과 견주자 **딱 한 줄**이 달랐습니다.

```
진로적성  career-result/page.tsx 229줄
    if (parsed.text) { acc += parsed.text; if (!cancelled) setTong(acc) }
                                           ↑ 읽는 «도중에» 화면을 갱신

합격운    runGroup 안
    if (j.text) out += j.text
                ↑ 제 안에만 쌓고 화면에 안 알림
```

합격운은 **한 묶음이 통째로 끝나야** 화면에 처음 나타나는 구조였습니다.
한 묶음이 2,400 토큰이니 1~2분입니다. 그동안 `0/3` 이 그대로 떠 있습니다.

```
★멈춘 것이 아니라 «원래 그렇게 만들어져 있었습니다».
  제가 어제 넣은 결함입니다. 죄송합니다.
```

### 왜 「풀이를 쓰지 못했어요」도 안 떴나
`Promise.all` 이 안 풀리는 동안 `tongState` 는 계속 `'loading'` 입니다.
실패도 완료도 아닌 상태가 몇 분간 이어지니 «영원히 멈춤» 으로 보입니다.

---

## 2. 무엇을 고쳤나 — 네 가지

### ① 조각이 올 때마다 화면을 갱신합니다  ★이것이 본 수정
진로적성과 같은 방식입니다. 이제 첫 문장이 오는 즉시 뜨고 글이 차오릅니다.

⚠️ 세 묶음이 나란히 흐르므로 묶음별 버퍼를 **ref** 에 두고 차례대로 이어 붙입니다.
`useState` 클로저에 담으면 조각이 사라집니다(교훈 K).

### ② 진행 표시가 실제로 움직입니다
```
전   0/3 묶음 · 일곱 갈래를 나눠 쓰고 있어요        ← 1~2분간 그대로
후   0/3 묶음 마무리 · 첫 문장을 기다리고 있어요
     0/3 묶음 마무리 · 1,204자 도착                ← 숫자가 계속 오릅니다
```
「묶음 개수」는 **다 끝난 묶음** 만 셉니다. 그것만 두면 멈춘 것처럼 보입니다.

### ③ 멈춤을 감지해 끝냅니다
```
STALL_MS  45초   조각이 45초 동안 안 오면 그 묶음은 멈춘 것으로 봅니다
HARD_MS  240초   아무리 길어도 4분을 넘기지 않습니다
```
⚠️ **중간까지 받은 글은 버리지 않습니다.** 반쪽이라도 손님에게 값이 됩니다.
그리고 세 묶음 중 둘만 와도 실패로 처리하지 않고 보여 줍니다.

### ④ 실패 이유를 버리지 않습니다
전에는 이 한 줄이 원인을 통째로 삼켰습니다.
```ts
if (!r.ok || !r.body) return ''     // ← 401·429·529 를 구분할 수 없었음
```
이제 상태코드와 본문 앞토막을 남기고 화면에 알립니다. **[다시 시도]** 단추도 넣었습니다.

---

## 3. 적용

```bash
cd myjae
git pull                                              # 3313bc6 인지 확인
git apply --check 17-streaming-fix-2026-07-30.patch    # ★먼저 이것부터
git apply 17-streaming-fix-2026-07-30.patch
npm run verify:all && npm run build
git add -A && git commit -m "합격운 스트리밍 — 조각마다 화면 갱신 · 멈춤 감지 · 실패 이유 표시"
git push origin main
```

제가 `3313bc6` 을 새로 클론해 실제로 붙이고 `tsc 0건` · 검사기 통과까지 확인한 뒤 넘깁니다.

```
git apply --check   충돌 0 ✅
적용본 tsc          0건 ✅
적용본 verify:seven 전부 통과 ✅
```

⚠️ 이 패치는 **`3313bc6` 위에 얹는 것**입니다. 어제 드린 `16-` 패치는 이미
그 커밋에 들어가 있으니 **다시 적용하지 마십시오.**

---

## 4. ⚠️ 이 수정이 «드러낼» 수 있는 다음 문제

지금까지는 글이 통째로 오거나 아예 안 왔습니다. 이제 **오는 대로 보입니다.**
그래서 그동안 안 보였던 것이 보일 수 있습니다.

### 만약 카드가 문장 중간에서 뚝 끊겨 있으면
그건 **Vercel 함수 시간 제한**입니다. 스트림이 잘렸다는 뜻입니다.

```
route.ts 27줄   export const maxDuration = 300
```
⚠️ 이 값은 «요청» 이고 «보장» 이 아닙니다. 요금제 상한을 넘으면 조용히 깎입니다.

```
확인   Vercel → 프로젝트 → Settings → Functions → Max Duration
       로그의 Duration 이 상한 근처(60s·300s)에서 잘려 있는지
고치는 길
  ① maxTokens 를 낮춘다 — 지금 묶음당 2,400/3,600 (buildSevenPrompt 부르는 자리)
  ② 묶음을 넷으로 쪼갠다 — SEVEN_GROUPS
     ⚠️ 그때 화면의 «n/3» 표시도 함께 고쳐야 합니다
  ③ 요금제를 올린다
```

### 만약 세 묶음 중 하나만 늦게 오면
같은 API 키로 세 스트림을 동시에 엽니다. 분당 입력 토큰(ITPM)에 걸리면
한 묶음이 뒤로 밀립니다. 이제 `[다시 시도]` 로 손님이 스스로 해결할 수 있고,
자세한 까닭은 그 문구에 마우스를 올리면 보입니다.

---

## 5. 검사 결과

```
tsc --noEmit              0건 ✅
tsc -p strict             0건 ✅
verify:daeun              18/18 ✅
verify:seven              전부 통과 ✅  ← 「부분 도착」 검사를 새로 넣었습니다
verify:live               발행 직전까지 전부 통과 ✅
04-verify-19-cases.py     19/19 ✅
eslint                    4 error / 16 warning — ★기준선과 동일
```

### 새로 넣은 검사 — 「부분 도착」
조각마다 화면을 갱신하면 **반쪽만 온 글**이 파서에 들어갑니다.
그때도 갈래를 놓치거나 엉뚱한 갈래로 잡히면 도표가 딴 곳에 붙습니다(교훈 CY).

```
1자씩 늘려 가며 전 구간 확인 → «엉뚱한 갈래» 0건
「■ 🧬 1. 타」 만 와도 dna 로 잡힙니다
```

---

## 6. 이번에 배운 것 (인수인계서에 얹을 것)

### 교훈 DE — 200 과 «오류 0건» 은 «잘 됐다» 가 아니다 ★★★
스트리밍 응답은 **헤더를 보내는 순간** 200 으로 기록됩니다.
본문이 한 글자도 안 갔어도 로그는 200 입니다.
그래서 「Status 200 · Error 0건」을 보고 서버 문제가 아니라고 판단한 것은 맞았지만,
**«그러니 타임아웃이다» 로 건너뛴 것은 틀렸습니다.**

```
★스트리밍이 안 보일 때 볼 순서
  ① 로그의 Duration      — 몇 초에 끝났나 (1초? 60초? 300초?)
  ② 클라이언트가 조각을 «화면에» 올리는가   ← 이번 원인이 여기였습니다
  ③ 그다음이 타임아웃
  ②를 건너뛰면 서버만 몇 시간 뒤집니다.
```

### 교훈 DF — «잘 되는 화면» 과 한 줄씩 견주라 ★★★
합격운이 안 되고 진로적성은 됐습니다. 같은 API, 같은 SSE 형식입니다.
두 수신 루프를 나란히 놓자 `setTong` 한 줄이 없는 것이 바로 보였습니다.
**같은 일을 하는 화면이 이미 있으면, 새 화면을 의심하기 전에 그 둘을 나란히 놓으라.**

### 교훈 DG — «끝나면 보여 준다» 는 «멈춘 것» 과 구별되지 않는다 ★★
`Promise.all` 뒤에서 한 번에 그리면, 기다리는 몇 분 동안 화면은
«실패» 도 «진행» 도 아닌 상태입니다. 손님에게는 멈춘 것과 똑같습니다.
**오래 걸리는 일은 «끝났는가» 가 아니라 «움직이는가» 를 보여 주라.**
진행 표시가 숫자로 오르지 않으면 진행 표시가 아니다.
