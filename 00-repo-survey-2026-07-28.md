# 저장소 조사 보고 — 2026-07-28

> `git clone --depth 1 https://github.com/lysh0112-cpu/myjae.git` (16MB)
> 세 가지를 조사했습니다.
> **1부 배선도 · 2부 오늘 전사분 전수 대조 · 3부 ㉚ 沖 자리 문제**

---

# ═══════════════════════════════
# 0. 먼저 — 제가 잘못한 것
# ═══════════════════════════════

첫 지시가 "저장소를 클론해서 구조를 정확히 파악할 것"이었는데, 저장소를 못 받은 채로
자료를 만들며 나아갔습니다. 그 결과 **이미 저장소에 있는 것을 새로 찾은 듯이 보고**했습니다.

| 제가 올린 것 | 실제 |
|---|---|
| 剋 열을 `geuk` 칸으로 새로 담자 | `cheonganTrait.ts` `tendency` 에 이미 있음 |
| ㊱ 일간-관성 合/沖 규칙이 없을 수 있다 | `examLuck/tables/rules.ts` 에 이미 있음 (합 +5 / 충 -4) |
| ㉞ 25칸 격자 표가 따로 있느냐 | `career/tables/yukchin.ts` 에 이미 있음 |
| ㉟ 서울대 오행이 두 곳에서 다르다 | `career/tables/gwa.ts` 머리말에 이미 적혀 있음 |
| ㉛ 土 문이과 비율을 정해야 한다 | `career/gyeyeol.ts` 가 사례 넷으로 검증해 이미 결론 냄 |
| ㊲ 인문=육친 / 자연=오행 으로 갈라야 한다 | `career/tables/gwa.ts` `PICK_BY` 에 이미 구현 |
| 육친별 문이과가 새 자료다 | `career/tables/yukchin.ts` `YUKCHIN_GYEYEOL` 에 이미 있음 |

**일곱 건입니다. 연재쌤 확인 목록에 헛으로 올릴 뻔했습니다.**
특히 剋 열은, 대표님이 두 번 짚어 주셔서 겨우 바로잡은 그 답이
`cheonganTrait.ts` 를 한 번만 열었으면 십 분 만에 나올 자리였습니다.

```
★교훈 CC — 저장소를 못 봤으면 거기서 멈춰라
  자료 파일은 저장소 없이도 만들 수 있다고 여겼으나, 만들 수 있는 것과
  만들어야 하는 것은 다르다. 이미 있는 것을 또 만들면 부품이 둘이 된다(교훈 BQ).
  ★"없다" 는 판정은 저장소를 전수로 훑은 뒤에만 할 수 있다. (교훈 CB 의 확장)
```

---
---

# ╔══════════════════════════════════════════════════════╗
# ║   1부 — 배선도                                        ║
# ╚══════════════════════════════════════════════════════╝

# 1-1. 규모

```
app          219 파일        lib          108 파일
hooks          6            components     2
lib/saju      73 (18,047줄)  lib/saju/career 15 + tables 9
                             lib/saju/examLuck 8 + tables 2
```

# 1-2. 교재 자료 파일과 그 쓰임

32부에서 만드신 자료 아홉은 **거의 전부 `jaryoPick` 과 `toTongbyeonInput` 두 곳만**
부릅니다. 화면은 거의 안 씁니다.

| 자료 파일 | 출전 | 참조 | 누가 |
|---|---|---|---|
| `cheonganTrait.ts` | 41~47 | 2 | jaryoPick · toTongbyeonInput |
| `ohaengNature.ts` | 20~24 | 2 | jaryoPick · toTongbyeonInput |
| `ohaengTable25.ts` | 25 | 2 | 〃 |
| `ohaengTrait.ts` | 26·27~28 | 2 | 〃 |
| `hapMeaning.ts` | 78~83 | 2 | 〃 |
| `chungMeaning.ts` | 84~86 | 3 | + monthly-fortune |
| `hyeongPaHae.ts` | 87~93 | 2 | jaryoPick · monthly-fortune |
| `sinsalTable.ts` | 94~97 | 2 | jaryoPick · toTongbyeonInput |
| `jijiTrait.ts` | 48·50~73 | 4 | + JijiTraitView(안 쓰임) · buildCareerPrompt · mulsang |
| `byeongjon.ts` | 74~77 | 5 | + ByeongjonView · 궁합 |
| `jijiGrade.ts` | 144칸 | 4 | UnseFlow · coupleJijiText · monthlyFortune · toTongbyeon |

# 1-3. 한가운데 — `jaryoPick.ts` (283줄)

```
                     ┌─ cheonganTrait  ohaengTrait  ohaengNature  ohaengTable25
   jaryoPick.ts ─────┤  chungMeaning   hapMeaning   sinsalTable   hyeongPaHae
                     └─ simsanOhaeng (점수·등급)

   내보내는 잔손
     cheonganBrief(dayStem, cap, gender)   일간 몇 줄. ★gender 를 넘길 때만 성별 줄이 나감
     cheonganImage(dayStem)                물상 한 줄
     ohaengBrief(score, target, what)      과다·결핍만. what 으로 건강·개운·다루는법 고름
     hapChungBrief(saju, target, opt)      ★3부에서 다룰 자리
     salHits / salBrief(saju, target, …)   걸린 살만
     hyeongPaHaeBrief(saju, target)
     munYiBrief(score)  stage25(el)
     isRuleLine(t)                         판정 조건 줄 거르개 (교훈 BW)
     needsOf(cats, table)                  갈래 → Need 집합. 모르면 전부
     pickLines(need, {...})                고른 것으로 재료 줄 만들기

   Need 열둘
     일간 오행 건강 개운 합 충 형파해 살 직업 다루는법 인생단계 문이과
```

**Need 에 「육친」이 없습니다.** 오늘 만들 자료가 들어갈 자리입니다.

# 1-4. 여섯 서비스가 jaryoPick 을 쓰는 방식 — 두 갈래

```
① needsOf + pickLines 를 통째로 쓰는 곳  (질문 갈래로 고름)
     toTongbyeonInput.ts        needsOf(a.questionCategories, CATEGORY_NEEDS)
     toCoupleTongbyeonInput.ts  needsOf(opts.questionCategories, COUPLE_CATEGORY_NEEDS)
     mulsangTongbyeonPrompt.ts  needsOf(questions.map(q => q.category))

② 잔손만 직접 부르는 곳  (카드별로 나눠 붙임 — pickLines 안 씀)
     career/buildCareerPrompt.ts   cheonganBrief salBrief hyeongPaHaeBrief
                                   munYiBrief ohaengBrief hapChungBrief
     examLuck/buildExamPrompt.ts   같은 여섯
     app/api/monthly-fortune       cheonganBrief salBrief hapChungBrief
                                   ohaengBrief stage25
```

> ★②는 `needsOf` 를 안 씁니다. 카드마다 붙일 자리가 정해져 있어서입니다(32-3장).
> **육친을 얹을 때도 이 두 갈래를 따로 손봐야 합니다.**

# 1-5. 십신 계산 — 부품이 몇 벌인가 (교훈 BQ 점검)

```
✅ 한 벌입니다. 걱정 없습니다.

yongsinNew.ts       sipsinOf(dayStem, other) → 십신 열 가지            ← 본체
examLuck/sipsin.ts  sipsinOfChar(dayStem, ch) → 본기(BONGI) 거쳐 위를 부름  ← 래퍼
career/yukchin.ts   yukchinOf(dayEl, el) → 다섯 짝                    ← 오행 기준, 결이 다름
```

`yukchinOf` 는 **오행만 받아 음양을 안 봅니다.** 다섯 짝을 낼 때는 맞지만,
열 십성이 필요하면 `sipsinOf` 를 써야 합니다. 섞으면 안 됩니다.

# 1-6. `career` 갈래 (진로적성)

```
calcPerson.ts     명식·점수 준비 (saju[] 를 '년주/월주/일주/시주' 로 만듦)
careerScore.ts    오행 점수 (simsanOhaeng 과 같은 계산기)
yongsin.ts        용신
gyeyeol.ts        ★계열(문·이과) 카드 — 교재 두 규칙을 견주어 ⓑ 채택, 사례 넷 검증
gyeokguk.ts  ilju.ts  jobs.ts  jobStructure.ts  sinsal9.ts  special.ts
ohaengGijil.ts  yukchin.ts
tables/  gwa(학과+대학135) gyeokguk ilju jobStructure jobs ohaeng sinsal yongsin yukchin
buildCareerPrompt.ts  카드별로 재료를 붙여 프롬프트를 만듦
```

# 1-7. `examLuck` 갈래 (합격운)

```
examScore.ts   규칙 표를 돌려 점수
tables/rules.ts  ★가점 13 · 감점 12 · 특수 1
  가점  일간관성합+5 일주천합지합+4 관인상생+4 정인운+3 정관운+3 상관격용신운+3
        삼합방합육합+2 편인운+2 식신운+2 조후해결+2 기신간합+2 관성관대 관인합충동시+1
  감점  상관정관-5 일간관성충-4 일주천극지충-4 관인형충-3 비겁관성충-3 용신관인합거-3
        재극인-3 식상극관-3 겁재운-2 공망-2 재극인약-1
hapchung.ts  ilganGwanHap · ilganGwanChung 등
jobChange.ts  이직 여섯 갈래   examDay.ts  시험일   sipsin.ts  래퍼
buildExamPrompt.ts  통변 + 파서
```

---
---

# ╔══════════════════════════════════════════════════════╗
# ║   2부 — 오늘 전사분 전수 대조                          ║
# ╚══════════════════════════════════════════════════════╝

# 2-1. 결론 한 줄

```
진로적성 비밀노트 126~139쪽   → 거의 전부 이미 있습니다.  새로 넣을 것 셋
명리적성 비법노트 106~131쪽   → 거의 전부 없습니다.      이것이 오늘의 진짜 일
```

# 2-2. 진로적성 126~139 — 이미 있는 것

| 오늘 전사한 것 | 저장소 |
|---|---|
| 학과 목록 (오행5 × 인문/자연) | `career/tables/gwa.ts` `GWA` |
| 대학 135곳 (서울35·지방캠12·경기인천13·지방64·한의대11) | `career/tables/gwa.ts` `UNIV_136` |
| 서울대가 129쪽·136쪽에서 다름 | `gwa.ts` 머리말에 이미 적힘. UNIV_136 만 씀 |
| 육친별 문이과 (문과5·이과3·공통2) | `career/tables/yukchin.ts` `YUKCHIN_GYEYEOL` |
| 인문=육친 / 자연=오행 | `gwa.ts` `PICK_BY` + `gyeyeol.ts` 166~171줄 |
| 土 무진술 6:4 / 기축미 4:6 | `gyeyeol.ts` — 두 규칙을 견주어 ⓑ 채택 |
| 강점 지능 70 : 용신 30 | `gyeyeol.ts` 108·129줄 |
| 25칸 격자 (오행×육친 기질+직업) | `career/tables/yukchin.ts` — **저장소 쪽이 더 자세합니다** |
| 일주별 직업적성 | `career/tables/ilju.ts` (100~127쪽) |
| 특목고·자사고 = 신왕관왕 | `examLuck/tables/rules.ts` |
| 일간-관성 合/沖 합격운 | `rules.ts` 일간관성합 +5 / 일간관성충 -4 |
| 공망 시험일 | `rules.ts` 공망 -2 |
| 학업 상승운 인성·식상 | `rules.ts` 정인운·편인운·식신운 |

# 2-3. 진로적성 126~139 — 없는 것 (셋)

```
① 등급별 누적 비율   1등급 4% … 9등급 100%          ❌ 없음
② "같은 사주를 가진 사람이 우리나라에 50~80명"      ❌ 없음   ★맺음말감
③ 시상일위상관격                                    ❌ 없음
   (월상일위격·이기성상격·삼기성상격은 career/special.ts 확인 필요)
```

# 2-4. 명리적성 3장 육친론 106~131 — 없는 것 (거의 전부)

| 쪽 | 내용 | 저장소 |
|---|---|---|
| 106~115 | 십성 열 개 성향 서술 | ❌ **없음** |
| 106~115 | 육친 관계 대상 (처갓집·장모 / 이복형제 / 시어머니 …) | ❌ **없음** |
| 116~117 | 많을 때 장단점 (오프라인70·삼사일언·셔터맨) | ❌ **없음** |
| 118 | 없거나 고립되었을 때 | ❌ **없음** |
| 119 | 특성과 보완점 (剋 두 방향) | ❌ **없음** |
| 120 | 20세 이전 분석법 · 공부 잘하는 순서 | ❌ **없음** |
| 121 | 月支 세 갈래 (辰戌丑未 중개업 등) | ❌ **없음** |
| 121 | 자리별 沖 | ⚠️ **`chungMeaning.ts` original 안에만** (3부) |
| 122~131 | 편중·혼잡 부작용·개운법 (외화내빈·사주증후군) | ❌ **없음** |

```
낱말 대조로 확인한 「없음」
  혼자서 사막 · 망한 집에 들어가도 · 마중물 · 노조 위원장 · 재고귀인
  처갓집 · 이복형제 · 시어머니 · 오프라인 70 · 삼사일언 · 셔터 맨
  노래방 · 보완해라 · 시상일위상관격 · 커플 매니저 · 사주증후군 · 외화내빈
```

> ★**「剋 열」만은 이미 있습니다** — `cheonganTrait.ts` `tendency`.
> 甲 편재적 성향 … 癸 상관적 성향. 오늘 만들 자료에 `geuk` 칸을 또 두면 두 벌이 됩니다.

# 2-5. ⚠️ 책 이름이 꼬여 있습니다

`career/tables/*` 는 전부 **『명리적성 비법노트』**로 적혀 있는데, 쪽 번호가 3장 육친론과
부딪힙니다.

| career/tables | 적힌 쪽 | 같은 쪽의 다른 자료 |
|---|---|---|
| `ilju.ts` 일주 | 100~127 | 3장 육친론 106~131 |
| `yukchin.ts` 육친 | 79~89 | `hapMeaning` 합 78~83 · `chungMeaning` 충 84~86 |
| `yongsin.ts` 용신 | 94~97 | `sinsalTable` 살 94~97 |
| `sinsal.ts` 신살 | 90~93 | `hyeongPaHae` 형파해 87~93 |
| `ohaeng.ts`·`jobs.ts` | 73~78 | `jijiTrait` 지지 ~73 · `hapMeaning` 78~ |
| `gwa.ts` 학과·대학 | 132~139 | 진로적성 비밀노트 132~139 |

그런데 `gwa.ts` 가 적어 둔 배치가 **오늘 받은 PDF(진로적성 비밀노트)와 정확히 맞습니다.**

```
gwa.ts 머리말:  "129쪽 짧은 대학 목록 / 136~139쪽 ⑥ 용신 오행에 맞는 대학교"
오늘 PDF:        129쪽에 짧은 목록 · 136~139쪽에 ⑥ 용신 오행에 맞는 대학교   ✅ 일치
career/ilju.ts:  일주 100~127쪽
오늘 PDF 126쪽:  계축·계해 일주가 끝나고 10번 항목 시작                      ✅ 일치
```

```
→ career/tables/* 는 「진로적성 비밀노트」에서 만든 것으로 보입니다.
   머리말의 책 이름이 잘못 적혀 있습니다.
★대표님이 두 책 표지를 보고 확인해 주셔야 합니다. 확인 뒤 머리말을 고쳐야
  다음 사람이 쪽 번호로 헤매지 않습니다.
```

---
---

# ╔══════════════════════════════════════════════════════╗
# ║   3부 — ㉚ 沖 자리 문제 조사                            ║
# ╚══════════════════════════════════════════════════════╝

# 3-1. 무엇이 문제인가

교재(명리적성 121쪽)는 沖을 **자리마다 다르게** 읽습니다.

| 자리 | 교재 판정 | 뜻 |
|---|---|---|
| **月支 沖** | **긍정 70%** | 이동·좋은 직장으로 스카우트·사업 시작. **부정적으로 보지 않는다** |
| **年支 沖** | **긍정** | 승진·이사·조상 땅 개발 |
| **日支 沖** | **100% 부정** | 배우자궁. 부부 불화·별거·이혼수 |
| **時支 沖** | **50% 긍정** | 유학·이민·승진·이직 (자식 문제는 부정) |

**규칙이 이미 저장소 안에 적혀 있습니다.** 다만 나가지 않는 칸에 있습니다.

```ts
// lib/saju/chungMeaning.ts  180~185줄  巳亥沖 항목의 original
original:
  '巳亥沖. 月支와 日支가 沖을 할 때 가장 영향력이 크다(월일 〉일시 〉연월). ' +
  '年支와 時支에서 沖을 하면 긍정 50%, 부정 50%. ' +
  '月支에 沖이 일어나면 긍정적이다(이사, 승진, 직업 변동, 어학연수, 유학). 日支沖은 부정적이다. ' + …
```

```ts
// ChungRow 인터페이스 98~99줄
/** ★교재 원문 — 화면에도 통변 재료에도 넣지 말 것 */
original: string
```

**규칙이 `original` 안에 있어 어디로도 안 나갑니다.** 교훈 BO 그대로 —
표에만 있는 규칙은 없는 규칙입니다.

# 3-2. 코드가 자리를 아예 안 받습니다

```ts
// chungMeaning.ts 265줄
export function findChungByChars(a: string, b: string): ChungRow | undefined {
  return JIJI_CHUNG.find(r => r.chars.includes(a) && r.chars.includes(b))
}
```

글자 둘만 받습니다. 부르는 쪽도 자리를 안 봅니다.

```ts
// jaryoPick.ts  83~92줄  hapChungBrief
for (let i = 0; i < branches.length; i++) for (let j = i + 1; j < branches.length; j++) {
  const r = findChungByChars(branches[i], branches[j])
  …
  out.push(`${r.key} — ${[...say, ...extra].join(' ')}`)
}
```

`branches` 는 `saju.map(p => p.branch)` — **`pillar` 를 버리고 글자만 뽑습니다.**

# 3-3. 그래서 손님에게 무엇이 나가고 있나

`子午沖` 의 `say` 는 이렇습니다.

```
마음이 자리를 못 잡고 소심해집니다. 충동적으로 사고 나서 자기 행동을 뉘우칩니다.
매매나 계약에는 불리한 때입니다.
```

**年支-月支 에 子午沖 이 선 사람도 이 말을 듣습니다.** 교재는 그 자리를
「이사·승진·직업 변동」이라 부르는데 말입니다.

# 3-4. 실측 — 몇 사람이 영향을 받나

지지 네 자리를 전수로 돌렸습니다.

```
지지 네 자리 전수                     20,736가지
沖이 하나라도 있는 명식                8,364 (40.3%)

沖이 걸리는 자리 짝 (각 1,728회씩 고르게)
  년지-월지   긍정 / 긍정 70%      ← 지금 부정으로 나감  ❌
  년지-시지   긍정 / 50% 긍정      ← 지금 부정으로 나감  ❌
  월지-시지   긍정 70% / 50% 긍정  ← 지금 부정으로 나감  ❌
  년지-일지   긍정 / 100% 부정     ← 부정 맞음  ✅
  월지-일지   긍정 70% / 100% 부정 ← 부정 맞음(영향력 가장 큼)  ✅
  일지-시지   100% 부정 / 50% 긍정 ← 부정 맞음  ✅

沖 걸림 총 10,368건 중
  月支가 낀 것  5,184 (50.0%)   ← 교재는 긍정 70%
  日支가 낀 것  5,184 (50.0%)   ← 교재는 100% 부정
```

**명식 단위로 다시 세면**

```
「긍정으로 읽어야 할 沖」이 하나라도 있는 명식
    4,752 / 20,736 = 22.9%   (沖 있는 명식의 56.8%)

沖이 전부 긍정 쪽인 명식
    3,600 / 20,736 = 17.4%   (沖 있는 명식의 43.0%)
    → 이분들은 좋은 소식을 통째로 나쁜 소식으로 듣고 있습니다
```

```
★손님 다섯에 한 명꼴(22.9%)이 영향을 받고,
  여섯에 한 명(17.4%)은 방향이 통째로 뒤집혀 있습니다.
```

> 31부에서 고치신 대운수 순행 버그가 손님의 약 9%였습니다. **이건 그보다 큽니다.**

# 3-5. 고칠 수 있나 — 예. 한 함수 안에서 끝납니다

**부르는 쪽을 한 글자도 안 고쳐도 됩니다.**

```
① Pill 에 pillar 가 이미 있습니다
     export type Pill = { pillar: string; stem: string; branch: string }

② 부르는 네 곳 모두 saju 를 통째로 넘깁니다 (branches 로 줄이는 것은 함수 안에서)
     toTongbyeonInput · buildCareerPrompt · buildExamPrompt · monthly-fortune

③ pillar 값이 저장소 전체에서 같습니다 — 전수 확인함
     '년주' '월주' '일주' '시주'
     만드는 곳 일곱 (calcPerson · birth-timing 둘 · wedding · moving · couple · ai-chat)
     전부 같은 이름을 씁니다. 어긋난 곳 0.
```

즉 **`jaryoPick.ts` 의 `hapChungBrief` 안에서 `pillar` 를 살려 읽기만 하면 됩니다.**

## 고치는 모양 (제안)

```ts
// lib/saju/jaryoPick.ts

/** 자리별 沖 판정 — 교재 121쪽. 일지가 끼면 부정, 아니면 변화·변동으로 읽는다 */
const CHUNG_TONE: Record<string, { tone: '변동' | '부정'; note: string }> = {
  '년주-월주': { tone: '변동', note: '자리를 옮기거나 일이 바뀌는 쪽으로 읽습니다' },
  '년주-시주': { tone: '변동', note: '멀리 나가거나 자리를 옮기는 일이 생길 수 있습니다' },
  '월주-시주': { tone: '변동', note: '일자리와 사는 곳이 함께 움직이기 쉽습니다' },
  '년주-일주': { tone: '부정', note: '' },
  '월주-일주': { tone: '부정', note: '' },   // 교재: 영향력이 가장 크다
  '일주-시주': { tone: '부정', note: '' },
}

export function hapChungBrief(saju: Pill[], target: Target, opt = {…}): string[] {
  …
  if (opt.충) {
    for (let i = 0; i < saju.length; i++) for (let j = i + 1; j < saju.length; j++) {
      const r = findChungByChars(saju[i].branch, saju[j].branch)
      if (!r || seen.has(r.key)) continue
      seen.add(r.key)
      // ★자리를 본다 (교재 121쪽). 順은 년→월→일→시 로 맞춘다
      const key = [saju[i].pillar, saju[j].pillar].sort(byPillarOrder).join('-')
      const t = CHUNG_TONE[key]
      const where = `${saju[i].pillar.replace('주','지')}-${saju[j].pillar.replace('주','지')}`
      if (t?.tone === '변동') {
        // 교재: 부정적으로 보지 않는다. 변화·변동으로 읽는다
        out.push(`${r.key}(${where}) — ${t.note} 이동·승진·직장 변동·유학처럼 자리가 바뀌는 일로 봅니다.`)
      } else {
        const say = r.say.filter(t2 => !isRuleLine(t2)).slice(0, 2)
        const extra = opt.건강 && target === 'adult' ? (r.sayAdult ?? []).slice(0, 2) : []
        out.push(`${r.key}(${where}) — ${[...say, ...extra].join(' ')}`)
      }
    }
  }
  …
}
```

## ⚠️ 손대기 전에 정해야 할 것 셋

```
① 「긍정 70%」를 어떻게 말할까
   교재는 月支沖을 70% 긍정이라 합니다. 100%가 아닙니다.
   → 지금 제안은 「변화·변동으로 봅니다」 로 방향만 돌리고 단정하지 않았습니다.
     "좋습니다" 라고 못 박으면 나머지 30%에서 손님이 다칩니다.

② 沖이 여럿일 때
   년월沖(변동)과 월일沖(부정)이 함께 서면 두 줄이 같이 나갑니다.
   → 교재는 "월일 〉 일시 〉 연월" 순으로 영향력을 매깁니다.
     센 것부터 내보내고 개수를 자르는 편이 좋겠습니다.

③ 대운·세운 沖은 다른 이야기입니다
   이 조사는 **원국 안의 沖**만 다뤘습니다.
   chungMeaning.ts 의 CHUNG_RULE 은 "대운·세운은 일단 월지에 먼저 대입한다" 고
   합니다. 운에서 오는 沖은 별도로 봐야 합니다. 이번에 건드리지 마십시오.
```

## 검증 방법

```
① 무작위 3만 명식으로 돌려 「변동」과 「부정」 비율을 잰다
   기대값: 변동이 낀 명식 22.9% · 전부 변동인 명식 17.4%
   (32-7장 GRADE_CUT 을 정하실 때 쓰신 방법. 교훈 BA)
② 고치기 전후로 재료 줄을 뽑아 눈으로 견준다
③ node check-imports.js · python3 hookcheck.py app · npx tsc -p tsconfig.strict.json
```

---
---

# ═══════════════════════════════
# 4. 그래서 오늘 할 일 (다시 세움)
# ═══════════════════════════════

```
🔴 1. ㉚ 沖 자리별 판정                 손님 22.9%가 지금 잘못 듣고 있습니다
                                        jaryoPick.hapChungBrief 한 함수. 부르는 쪽 안 고침

   2. 명리적성 3장 육친론 자료 파일      저장소에 정말 없습니다
        yukchinTable.ts   십성 열 개 (106~115)  ★geuk 칸은 빼고
                                        cheonganTrait.tendency 와 대조만
        yukchinGroup.ts   다섯 짝 (116~119 · 122~131)
        yukchinRule.ts    배선 규칙 (120~121) ← 재료가 아님

   3. Need 에 '육친' 을 더하고           CATEGORY_NEEDS · COUPLE_CATEGORY_NEEDS
      pickLines 에 얹기                  ★②갈래(career·exam·monthly)는 따로 손봐야 함

   4. 작은 것 셋                         등급 비율 · "50~80명" 맺음말 · 시상일위상관격

   5. career/tables/* 머리말 책 이름     ★대표님이 표지 확인 뒤
```

**폐기: 제가 만든 `yukchinTable.ts` 초안** — `geuk` 칸이 두 벌이라 다시 짜야 합니다.

---

**끝. 배선도·전수대조·沖 조사 셋 다 마쳤습니다.**
