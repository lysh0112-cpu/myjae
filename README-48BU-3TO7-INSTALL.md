# README — 48부 3~7차 · 전문분야 토글 · 별칭(호) · ★서비스별 거르기

```
날짜   2026-08-07
지시   [대표님]
  3차  「전문분야를 홈서비스 버튼에 맞춰서 각 상담사별로 ★토글로」
  4차  「본명은 ★관리자만 관리하고, 고객들은 ★별칭인 "호" 가 보이도록」
  7차  「상담사 ★리스트가 위로 가게 해야 관리하기 좋을 것 같은데」
  5~6차  「관리자화면에서 "궁합"을 ★두 사람만 지정할 경우,
         궁합보기 서비스에서 고객이 고를 수 있는 상담사는 ★2명만 나와야 하는데」

검사   ★1,679건 전부 통과 (실패 0)
타입   tsc --noEmit 통과
eslint 오류 82 · 경고 137  ★기준선 그대로
빌드   ✅ 98개 화면 렌더 확인
```

---

## 1. ⚠️ SQL 을 «먼저» — 둘 다 이미 돌리셨습니다

```
① _SQL_48bu_3차_상담사_전문분야_복수.sql   ✅ 돌리심
② _SQL_48bu_4차_상담사_별칭.sql            ✅ 돌리심
```

★5차는 SQL 이 «없습니다». 화면만 고쳤습니다.
압축을 풀어 저장소 위에 덮으시면 됩니다.

---

## 2. ★5차 — 서비스별 거르기 (이번에 새로 된 것)

```
[전]  궁합에서 상담을 눌러도 ★활성 상담사가 «전부» 나왔습니다.
[후]  ★관리자가 「궁합」을 켜 준 분만 나옵니다.
```

★값으로 재 보았습니다 (지금 대표님 자료 그대로) —

```
couple  → ★영암 · 연재      (청산 빠짐 · 독동은 비활성)
career  → ★청산 · 연재
saju    → 영암 · 청산 · 연재   ← ⚠️ 아래 「0명 배려」 참조
```

### ★배관을 새로 깔지 않았습니다

`ConsultButton` 이 ★열두 화면 «전부» 에서 `priceKey` 를 이미 실어 보내고 있었습니다.
`consultant-select` 가 그 값을 ★안 읽고 있었을 뿐입니다. 읽게만 했습니다.

⛔ `SERVICE_SPECIALTIES` 의 key 를 바꾸지 마십시오 —
   `consult_prices` · `ConsultButton` 열두 곳과 ★한꺼번에 어긋납니다.

---

## 3. 🔴🔴 ★배려를 «둘 다» 뺐습니다 [대표님 지시]

```
[대표님]  「★아무것도 안 고른 상담사는 안 나오게 해야지」
          「★두번째 배려도 뺀다」

⇒ ① 아무 분야도 «안 고른» 상담사 → ★안 나옵니다
   ② 걸러서 «0명» 이어도 → ★0명 그대로. 전체로 되돌리지 «않습니다».
⇒ ★관리자가 켜 준 분만 나옵니다.
```

### 🔴 지금 자료로 재 보면 — ★열 중 여덟이 0명입니다

```
career       → 청산 · 연재
couple       → 영암 · 연재
mulsang · saju · wedding · birth · moving · naming · naming_baby · tarot
             → ★0명 「현재 상담 가능한 상담사가 없습니다」
```

### ⇒ ★반드시 둘 중 하나를 하십시오

```
① ★가격 관리에서 그 여덟 서비스의 «상담 버튼» 을 끄십시오
   (consult_prices.active — 끄면 ★버튼 자체가 안 뜹니다 · ConsultButton:76)
② 또는 상담사분들께 ★해당 토글을 켜 주십시오

⛔⛔ ★「0명이면 전체를 보여 주는」 되돌림을 «다시 넣지» 마십시오.
     ★대표님이 «일부러» 빼신 것입니다. 앞 세션이 넣었다가 지시로 걷었습니다.
```

## 4. ★한 가지 더 고쳤습니다 — 안 물어보신 것

```
🔴 `useEffect` 의 의존 배열이 ★`[]` 였습니다.
   ⇒ 손님이 «다른 서비스» 로 다시 들어와도 ★옛 목록이 그대로 남습니다.
     (궁합에서 보고 나갔다가 진로적성으로 들어오면 궁합 목록이 남습니다)
   ⇒ ★`[priceKey]` 로 고쳤습니다.
⚠️ 거르기를 붙이지 않았으면 «드러나지 않았을» 버그입니다.
```

---

## 5. ⛔ 손대지 말 것 — 48부에 더해짐

```
⛔⛔ ★손님 화면에 상담사 이름을 낼 때는 «언제나» ★shownName() —
     `c.name` 을 직접 쓰면 ★본명이 샙니다.
     ⚠️ 조회에도 ★alias 를 «반드시» 넣으십시오 (`select('id, name, alias, …')`).
        빠뜨리면 ★화면은 멀쩡히 뜨고 본명만 «조용히» 나갑니다. 검사도 못 잡습니다.
     ⇒ 지금 쓰는 손님 화면 ★넷 — consultant-select · mypage-new
        · consulting(채팅) · api/summarize(★손님께 톡으로 갑니다)

⛔ ★관리자 목록·상담사 대리 화면에는 «거르기·별칭» 을 걸지 마십시오.
   대표님은 ★본명으로, ★다 보시고 관리하십니다.

⛔ ★specialty(한 칸)를 지우지 마십시오. 손님 화면이 아직 읽습니다.
   저장하면 specialties 와 «함께» 채워집니다.

⛔ ★「0명이면 전체를 보여 주는」 배려를 빼지 마십시오.
```

---

## 6. ⚠️ 순번 — «노출 통제가 아닙니다»

```
[대표님]  「순번은 필요해. 상담사들 중에 ★가장 잘하는 상담사를
            1번부터 순서대로 올려야 해서. 그냥 놔두자」

⇒ ★순번은 «차례» 만 맡습니다. 0 이어도 손님께 보입니다.
⇒ 노출 통제는 ★「비활성」 버튼입니다.
⛔ 순번 0 에 「숨김」 뜻을 붙이지 마십시오.
   ★한 번 붙였다가 «되돌렸습니다» — 숨기는 스위치가 둘이 되면
   나중에 「분명 활성인데 왜 안 보이지」 하고 헤매게 됩니다.
```

---

## 7. □ 눈으로 봐 주셔야 할 것

```
□ ★궁합 결과 → 상담 버튼 → 상담사가 ★둘만 (영암 · 연재)
□ ★진로적성 → ★둘만 (청산 · 연재)
□ 다른 서비스에서 들어왔다 나갔다 해도 ★목록이 «따라 바뀌는지»
□ 상담사 고르기에 ★별칭만 (본명 안 보임)
□ ★마이페이지 내 예약 · 채팅 제목에도 별칭
□ ★관리자 목록에는 「오안순 (영암)」 처럼 ★본명이 먼저
```

---

## 8. ⚠️ 아직 «안 한» 것

```
□ 🔴 ★아침마다 회원 관리가 막히는 것
     [겪은 일]  「어제 밤까지 됐는데 아침에 안 되네」 → 재로그인하니 됨
     [까닭]  `requireMaster` 가 `profiles` 를 ★anon 키로 읽습니다.
        정책이 `auth.uid() = id` 인데, 밤새 토큰이 만료되면
        `auth.getUser()` 는 통과하는데 그 뒤 조회에 ★uid 가 안 실려
        등급을 못 읽고 ★403 「관리자만 사용할 수 있습니다」.
     [고칠 방향]  ★등급만 service_role 로 읽기.
        누구인지(user.id)는 ★여전히 쿠키 세션으로 검증하므로 보안은 그대로.
        ⇒ `requireMaster` ★한 곳만 고치면 관리자 API 가 «전부» 풀립니다.
     ⛔ ★RLS 정책은 건드리지 마십시오 — profiles 에는 전 회원의 생년월일이 있습니다.
```


---

## 9. ★7차 — 관리자 상담사 화면 «자리 바꿈»

```
[대표님]  「상담사 ★리스트가 위로 가게 해야 관리하기 좋을 것 같은데」

[전]  등록 폼(길다) → 상담사 목록
[후]  ★상담사 목록 → 등록 폼
   ⇒ 상담사를 보려고 ★긴 폼을 지나 내려가지 않아도 됩니다.
```

### ⚠️ ★자리만 바꾸면 «어긋나는» 것이 셋 있었습니다 — 함께 고쳤습니다

```
① 「수정」을 누르면 ★맨 위로 스크롤했습니다
   ⇒ 폼이 아래로 갔으니 맨 위로 가면 ★폼이 안 보입니다.
   ⇒ ★폼 자리로 «내려가게» 고쳤습니다 (goForm · formRef).
   ⛔ scrollTo({ top: 0 }) 으로 되돌리지 마십시오.

② 저장한 «뒤» 에 폼 자리에 머물렀습니다
   ⇒ 폼은 비워지므로 ★빈 폼만 보고 있게 됩니다.
   ⇒ ★목록(위)으로 올려 «결과가 보이게» 했습니다.

③ 폼이 아래로 가서 ★찾기 어려워졌습니다
   ⇒ 목록 머리에 ★「＋ 상담사 등록」 단추를 달았습니다. 누르면 폼으로 내려갑니다.
```

⚠️ ★화면 하나를 옮기면 «그 화면을 오가는 길» 이 함께 어긋납니다.
   자리만 바꾸고 끝냈으면 「수정을 눌렀는데 폼이 안 보인다」가 났을 것입니다.

---

```
48부 3~7차 끝.

★잘한 것
  · ★배관이 이미 깔려 있는 것을 찾았습니다 (priceKey). 새로 짓지 않았습니다.
  · ★거르기 결과를 «값으로 찍어» 확인했습니다 (궁합 → 영암·연재 둘).
  · ★본명이 새는 곳을 전수로 세었습니다 — 넷이었습니다.
  · ★0명이 되는 것을 미리 «알려 드리고» 대표님이 고르시게 했습니다.
  · ★순번 0 숨김을 «되돌렸습니다» — 대표님 말씀을 듣고 «덜어내는» 것도 일입니다.

⚠️ ★다음 세션에게 —
   손님 화면·상담사 화면·관리자 화면은 ★«서로 다른 조건» 으로 그려집니다.
   같은 상담사 목록인데 관리자는 «다» 보고, 손님은 ★활성 + 전문분야로 걸러 보고,
   이름도 관리자는 «본명», 손님은 ★«별칭» 입니다.
   ⇒ 한 곳을 고치면 ★나머지 둘에서도 열어 보십시오. (47부 가장 아픈 교훈)
```
