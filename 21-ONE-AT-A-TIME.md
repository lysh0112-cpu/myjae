# 🔧 한 주제가 끝나면 다음 주제 (2026-07-30, 6차)

```
기준 커밋   efc127a  (2026-07-30 14:18)
고친 파일   1개  ·  ExamResultShell.tsx
```

---

## 무엇이 잘못돼 있었나 — 캡처가 그대로 말해 줬습니다

```
1번  🧬 타고난 공부 DNA와 적성   ← «지금 쓰고 있어요…»
2번  🎯 과목별 유불리와 올인 전략  ← 이미 다 나옴
3~7번                        ← 차례를 기다리고 있어요
개발용 상자                   ← 1/7
```

제가 «둘씩» 부르게 해 놓았는데, 그 방식이 이랬습니다.

```
한 물결(둘)이 **다 끝나야** 다음 물결이 시작합니다.  (Promise.all)
→ 1번이 늦으면 2번은 이미 끝났는데도 3·4번은 시작조차 못 합니다.
→ 화면이 그 상태로 굳어 «안 넘어간다» 로 보입니다.
```

**대표님이 처음부터 하나씩 하라고 하신 그 이유였습니다.**

---

## 고친 것

```ts
const CONCURRENCY = 1      // ← 2 에서 1 로
```

이제 이렇게 흐릅니다.

```
1번 시작 → 1번 끝 → 2번 시작 → 2번 끝 → 3번 시작 → …  → 7번 끝
```

느린 갈래가 있어도 **그 하나만** 기다립니다. 그리고 60초가 지나면
`STALL_MS` 가 걸려 그 갈래를 포기하고 다음으로 넘어갑니다.
한 갈래가 화면 전체를 붙잡는 일이 없어집니다.

⚠️ **1 에서 올리지 마십시오.** 주석에 이 이야기를 그대로 적어 두었습니다.
   올리고 싶어지면 올리는 대신 `STALL_MS` 를 줄이십시오 —
   느린 하나를 빨리 포기하는 쪽이 안전합니다.

---

## 함께 고친 것 — 거짓말하던 표시

차례가 이미 지났는데 글이 없는 갈래가 «차례를 기다리고 있어요» 로 남아 있었습니다.
하나씩 부르므로 그 갈래는 이미 끝난 것입니다.

```
지금 쓰는 갈래        지금 쓰고 있어요…
차례가 지났는데 없음   ★이 갈래는 받지 못했어요
아직 차례가 안 옴      차례를 기다리고 있어요
```

---

## 적용

```bash
cd myjae
git pull                              # efc127a 인지 확인
unzip -o 21-one-at-a-time.zip
npm run verify:all && npm run build
git add -A && git commit -m "합격운 — 한 주제가 끝나면 다음 주제 (CONCURRENCY 1)"
git push origin main
```

`efc127a` 를 새로 클론해 붙이고 확인했습니다.
```
원격의 현재 값     const CONCURRENCY = 2
패치 적용 뒤       const CONCURRENCY = 1     ✅ 눈으로 확인
충돌 0 · tsc 0건 · strict 0건 · verify:seven 통과 · eslint 0건
```

---

## ⚠️ 시간은 늘어납니다

일곱을 차례로 부르니 **2~3분** 입니다. 대표님이 «속도보다 끊기지 않는 것» 으로
정하셨고, 이번에는 «넘어가는 것» 을 우선하셨습니다.

화면이 그 시간을 견딜 수 있게 되어 있습니다.
```
· 일곱 자리가 처음부터 다 서 있어 «몇 개 남았는지» 보입니다
· 지금 쓰는 갈래에 진한 테두리가 갑니다
· 글자수가 계속 오릅니다
```

빨라지고 싶으시면 «둘씩» 이 아니라 이쪽을 보십시오.
```
① MATERIAL_NEEDS 를 더 좁힌다      — 프롬프트가 작아지면 첫 문장이 빨리 옵니다
② 「앞으로의 흐름」 카드(1,518자)를 갈래별로 자른다
   지금 여섯 갈래가 이 카드를 통째로 받습니다. 여기가 가장 큰 덩어리입니다
```

---

## 교훈 DR — «둘씩» 은 «하나씩» 의 두 배가 아니다 ★★★

물결(wave) 방식은 «한 물결이 다 끝나야 다음» 입니다.
그래서 둘씩 부르면 **느린 쪽이 빠른 쪽까지 붙잡습니다.**
하나씩보다 빠른 것은 «둘 다 비슷할 때» 뿐이고, 하나가 늦으면
화면은 하나씩보다 **더 멈춰 보입니다** — 다음 둘이 시작조차 못 하니까요.

**나눠 부를 때 «몇 개씩» 이 아니라 «먼저 끝난 것이 다음을 시작할 수 있나» 를 보라.**
그게 안 되면 하나씩이 낫다.
