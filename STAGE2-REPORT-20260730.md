# 2단계 작업 보고 — 통합 판정 엔진 · 검사 그물

```
작업 시점   2026-07-30
기준        1단계(오행 정규화) 적용 뒤
산출물      ① 16-verify-naming.ts            신설 554줄 · 검사 113건
            ② lib/saju/resourceJudge.ts      신설 610줄
            ③ app/api/naming/route.ts        +65  (AI 재료 연결)
            ④ app/manseryeok/naming/diagnosis/page.tsx  버려지던 값 전달
            ⑤ package.json                   prebuild 관문에 그물 추가
            ⑥ stage2-resource-judge-20260730.patch
```

---

## 0. ★그물이 «설계 구멍» 을 하나 잡았습니다

요청대로 그물을 먼저 깔았습니다. 첫 실행에서 **2건이 실패**했고, 둘 다 진짜 문제였습니다.

### 실패 ① — 시험 자료 오류 (제 잘못)

```
❌ 고립이 아닌 자리는 안 잡습니다 (금 10점, 생하는 토 20점)
```

제가 시험 자료를 `토: 20점` 으로 두었는데, 「받쳐 준다」고 보려면 생하는 오행이
**발달(25점) 이상**이어야 합니다. 20점은 받쳐 주지 못하므로 금은 실제로 고립입니다.
→ 시험 자료를 `토: 40점`(발달)으로 고쳤습니다.

### 실패 ② — ★엔진의 설계 구멍

```
❌ 구신(56)은 기신(19)보다 가볍고 무관(38)보다 무겁습니다
```

기신·구신·무관을 견주려 했는데 순서가 뒤죽박죽이었습니다. 진단해 보니 —

```
기신 토   총점  19   흐름관계 목→토 = 순극  (flowAvg −2)
구신 화   총점  56   흐름관계 목→화 = 순생  (flowAvg +2)
무관 금   총점  38   흐름관계 목→금 = 역극  (flowAvg −1)
```

**계산은 맞았습니다.** 오행을 바꾸면 «균형» 만 바뀌는 게 아니라 «흐름» 도 함께 바뀌는데,
총점만 있어서 **어느 칸 때문에 점수가 달라졌는지 갈라볼 수 없었습니다.**

→ `breakdown { flow, yongsin, balance, rawTotal, cappedTo }` 를 신설했습니다.
   시험은 이제 `breakdown.balance` 만 견줍니다(흐름을 고정).
   ★디버깅에도 쓰입니다 — 배점을 조일 때 어느 칸이 움직였는지 보입니다.

**그물을 먼저 깔라고 하신 것이 정확했습니다.** 판정을 먼저 만들고 나중에 그물을 깔았다면
이 구멍을 못 봤을 겁니다. 총점만 보면 아무 이상이 없어 보입니다.

---

## 1. `16-verify-naming.ts` — 검사 113건

```bash
npx tsx 16-verify-naming.ts          # 요약
npx tsx 16-verify-naming.ts --dist   # 걸림 비율 분포 (교훈 BO)
npm run verify:naming                # 같음
npm run verify                       # ★prebuild 관문 — 대운 + 일곱갈래 + naming
```

| 구획 | 무엇을 잠그나 |
|---|---|
| ① `normalizeOhaeng` 경계값 23건 | 한자·괄호·전각공백·ZWSP·BOM·NFD 한글·복수오행·빈값 |
| ② 표준 다섯 값 | 무작위 오염 1,000건 → 도메인 밖 반환 0건 |
| ③ 공백·비가시문자 | `' 熺'`(행 5002 실물) 포함 |
| ④ 사주 프로필 | 기신·구신·한신·isStrong 수습 · 과다(점수·글자수) · 결핍 · 고립 |
| ⑤ 관계 판정 | 순생/역생/순극/역극/비화/모름 — 방향 |
| ⑥ `judgeResource` 불변식 26건 | ①~④ 요구사항 전부 + 점수 범위 + 상한 |
| ⑦ 프롬프트 재료 | 판정 어휘 0건 · 「해당 없음」 0건 · **조사 어색함 0건** |
| ⑧ 1단계 회귀 | 한자 입력이 정규화되는지 · 81수리표 |
| ⑨ 무작위 관통 4,000건 | 예외 0 · 불변식 위반 0 · 경고 누락 0 |

### ★관문이 잠기는지 확인했습니다 (교훈 CS)

```
lib/saju/ohaeng.ts 에서 木 정규화를 일부러 빼기
  → 종료코드 1 · 실패 4건  ✅
      ❌ ★한자 — 덕암 원자료 표기   "木" → null (기대 목)
      ❌ BOM                    "木﻿" → null
      ❌ 자원오행 흐름 — 柳()→承(금)→炫(화)
되돌림 → 종료코드 0  ✅
```

⚠️ **DB 쪽 검사는 이 파일에 넣지 않았습니다.** 망을 부르면 `prebuild` 가 물립니다.
자원오행 다섯 값·공백·중복은 `_SQL_hanja_stage1_20260730.sql` 의 STEP 0·1·2·5 확인 쿼리가 담당합니다.

---

## 2. `SajuOhaengProfile` — 버려지던 값을 수습했습니다

```
calcYongsinCompat 이 주던 것   { isStrong, yongsin, heeksin, gisin, gusin, hansin, score, description }
naming 이 받던 것              { yongsin, heeksin, score }            ← 넷을 버렸습니다
2단계가 받는 것                isStrong · gisin · gusin · hansin 까지
```

그리고 세 가지를 **새로 계산하지 않고 있는 부품을 불러** 얹었습니다. (교훈 CJ)

```
등급(결핍/약함/발달/과다)  → simsanOhaeng.grade()      잣대를 새로 만들지 않았습니다
글자 수 세기               → ohaengNature.ohaengOfChar  글자→오행 표를 새로 만들지 않았습니다
오행 정규화                → ohaeng.normalizeOhaeng     1단계 창구 그대로
```

### 과다 판정 — 두 잣대를 «함께» 봅니다

```
EXCESS_POINT_MIN = 50    simsanOhaeng 의 «과다» 문턱 (100점 만점)
EXCESS_COUNT_MIN = 4     ★대표님 지시 — 여덟 글자 가운데 넷 이상
```

둘 중 **하나만 걸려도** 과다로 봅니다. 실측으로 확인했습니다 —
점수 40(문턱 아래)이어도 글자가 4자리면 과다로 잡힙니다.

⚠️ `saju` 를 안 넘기면 글자를 셀 수 없어 점수만으로 판정합니다(`hasCount: false`).
옛 화면이 안 깨지게 선택값으로 두었습니다.

### ⚠️ 고립(孤立) — 우리가 정한 잣대입니다

인수인계서 3-4장 ㉖ 에 「「고립」의 잣대」가 **미결로 남아 있습니다.**
정통 명리의 고립은 «글자의 자리» 를 봐야 하지만 지금 우리에게는 점수만 있습니다.
그래서 보수적 대용을 두고 **한 함수에 몰아** 두었습니다.

```
잣대 : 점수가 0보다 크고 25 미만(약함) + 그 오행을 생해 주는 오행도 25 미만
       → 약한데 받쳐 줄 힘도 없는 자리
걸림 : 무작위 5,000명에서 26.4%

★연재쌤 확인 대기. 잣대를 고치실 때 judgeIsolated() 하나만 고치면 됩니다.
```

---

## 3. `judgeResource` — 요청하신 네 가지

배점은 **흐름 30 · 용신 40 · 균형 30 = 100** 입니다.

### ① 과다 오행 중복 투입 → 경고 + 감점

```
과다(50점+ 또는 4글자+)를 이름이 또 보태면   −12  + 경고
발달(25~49)을 보태면                        −4   (경고 없음 — 가볍게)
```

재료에 이렇게 나갑니다.

```
이미 넉넉한 기운   화 60점 · 여덟 글자 중 4자리 — 이름이 여기에 더합니다
```

### ② 결핍·고립 보충 → 가산

```
결핍(0점)을 채우면   +6
고립을 채우면        +4
```

### ③ 기신 투입 → 경고 + 감점

```
기신(忌神)  −15   ★가장 무겁게
구신(仇神)  −8
```

**옛 로직은 이 값을 받지도 않았습니다.** 재료가 손안에 있는데 안 쓰던 자리입니다.

### ④ ★상극 예외 인정 — 보완하면 감점하지 않습니다

```
잣대 : 상극(순극·역극) 두 글자 가운데 «어느 한쪽» 이라도
       용신 또는 희신을 담고 있으면 그 관계를 «비화»(중립)로 봅니다

CLASH_EXEMPT_WHEN_SUPPLEMENTING = true    ← 끄고 싶으면 여기 한 곳
CLASH_EXEMPT_INCLUDES_HEEKSIN   = true    ← 희신까지 인정할지
```

**왜 필요한가** — 이름의 목적은 사주를 보완하는 것입니다. 용신을 담으려고 고른 글자가
이웃과 상극이 되는 일은 흔하고, 그것을 감점하면 «용신을 담지 말라» 는 말이 됩니다.

실측 — 그물이 이렇게 확인합니다.

```
용신 = 토 · 이름에 垈(토)      목剋토 이지만 토가 용신 → exempted=true · 경고 없음 · 흐름 +1.0
용신 = 수 · 이름에 垈(토)      목剋토, 토는 보완 아님   → exempted=false · 경고 남음 · 흐름 −2.0
```

⚠️ **감점만 면제하고 «가점» 은 주지 않습니다.** 상극이 좋아지는 것은 아닙니다.
★연재쌤 확인 대기 — 희신까지 인정할지, 용신만 인정할지.

### 그 밖에 함께 고친 것

| 자리 | 옛 로직 | 2단계 |
|---|---|---|
| 상극 감점 | **없음** (극·무관·비화가 점수상 동일) | 순극 −2 · 역극 −1 |
| 상생 방향 | 무시 (목生화 = 화←목) | 순생 +2 · 역생 +1 |
| 성↔끝 관계 | 건너뜀 | 세 글자 이상에서 반 무게로 봄 |
| `hasYongsin` ↔ `yongsinChars` | 성 제외 ↔ 성 포함 (**어긋남**) | 둘 다 given 만 — 통일 |
| 판정 불가 | 조용히 통과 | `problems` + 점수 상한(40/60) |
| 경고 | 담을 자리 없음 | `warnings[]` 신설 |

---

## 4. AI 프롬프트 연결

`app/api/naming/route.ts` 에 두 가지를 얹었습니다.

**① 사람이 읽는 정리** — `resourceFactsBlock()` 이 만든 덩이

```
[자원오행 — 글자에 담긴 기운]
흐름  柳(목)→炫(화)→垈(토)
관계  木生火 — 앞 글자가 뒤 글자를 살립니다
관계  木剋土 — 앞 글자가 뒤 글자를 누릅니다 [성↔끝]

[사주와의 관계]
사주가 바라는 기운(용신)   수
이름에 담긴 기운            화 · 토
용신을 담은 글자            없습니다
이미 넉넉한 기운            화 60점 · 여덟 글자 중 4자리 — 이름이 여기에 더합니다
꺼리는 기운(기신)           토 — 이름에 들어 있습니다
비어 있는 기운              금 · 수 — 이름이 채우지 않았습니다

[참고하실 자리 — ★단정하지 말고 «이런 견해가 있다» 로 담담히 전하세요]
· 柳(목)와 垈(토)가 서로 누르는 자리로 봅니다
· 사주에 화 기운이 이미 넉넉한 편인데(60점 · 여덟 글자 가운데 4자리) 이름의 炫도 화를 더합니다
· 토는 이 사주가 꺼리는 기운(기신)으로 봅니다. 이름의 垈가 토입니다
```

**② JSON facts** — `자원오행_정밀` · `참고할자리` · `판정못한자리`

### ⚠️ 방침을 지킨 세 가지

```
① 「좋다/나쁘다」 를 한 번도 쓰지 않습니다. 그물이 판정 어휘 0건을 검사합니다
② ★점수(score·breakdown)를 프롬프트에 넣지 않았습니다
   숫자를 주면 AI 가 「85점입니다」 처럼 판정해 버립니다
③ 걸린 것이 없으면 그 줄을 «아예» 넣지 않습니다 (교훈 BF)
   「해당 없음」이라 적으면 AI 가 그 말을 손님에게 옮겨 씁니다
```

### ★그물이 잡은 또 하나 — 조사

첫 재료 블록이 이랬습니다.

```
· 토은 이 사주가 꺼리는 기운… 이름의 垈가 토입니다
· 이름의 炫도 화을 더합니다
```

**이 문장은 AI 에게 재료로 나갑니다.** 어색한 표현을 두면 AI 가 그대로 옮겨 손님에게 씁니다.
받침을 보고 조사를 붙이는 `josa()`·`josaOf()` 를 넣고 그물에 검사를 추가했습니다.

⚠️ **옛 5관점 facts 를 지우지 않았습니다.** 화면·파서·보관함이 그대로 돕니다.
새 판정은 자원오행·사주보완 두 관점의 «근거를 넓히는» 역할입니다.

⚠️ `gisin`·`gusin`·`hansin`·`isStrong`·`saju` 는 모두 **선택값**입니다.
안 보내면 예전과 똑같이 돕니다 — 개명 화면 넷은 아직 안 보내므로 무해합니다.

---

## 5. ★수정 전/후 영향 범위

실제 덕암 5,111자에서 무작위 이름 **5,000개**(성1+이름2)를 만들어 대조했습니다.

### 옛 로직에 «판정 자체가 없던» 것

| | 걸린 비율 |
|---|---|
| 상극인데 감점이 0이던 자리 | **61.9%** |
| 기신·구신 투입 (재료조차 없었음) | **58.0%** |
| ★④ 상극 예외 인정이 적용된 이름 | **49.2%** |
| 결핍 보충 가산 | 39.5% |
| 고립 보충 가산 | 26.4% |
| 과다 오행 중복 투입 | 13.5% |

```
경고가 하나라도 나가는 이름     77.1%      ← 옛 로직은 0% (담을 자리가 없었습니다)
자원오행 등급이 바뀐 이름       61.8%
```

### ★등급 분포 — 「보통」이 실제로 0.0% 였습니다

```
옛   좋음 65.8%  ·  보통  0.0%  ·  아쉬움 34.2%
새   좋음 39.3%  ·  보통 38.0%  ·  아쉬움 22.7%
```

명세서 2-C 에서 「두 글자 이름은 ratio 가 0·0.5·1.0 뿐이라 «보통» 이 나올 수 없다」고
적었던 것이 **실측으로 확인됐습니다.** 손님 셋 중 둘이 «좋음», 셋 중 하나가 «아쉬움» —
사실상 두 갈래였습니다.

### 후보 정렬

```
옛 로직이 «구별 못 하던» 두 후보를 새 로직이 갈라냄   51.6%
```

개명 화면의 추천 순위가 절반 가까이 달라집니다. ★배포 전에 실기기로 후보 목록을 보십시오.

---

## 6. 검증 — 기준선 그대로

```
npx tsc --noEmit --skipLibCheck        0건 ✅
npx tsc -p tsconfig.strict.json        0건 ✅
npx tsx 16-verify-naming.ts            113건 전부 통과 ✅
npm run verify (prebuild 관문)          대운 18/18 · 일곱 갈래 · naming 113 ✅
관문 잠김 시험                          일부러 틀림 → 1 ✅ / 되돌림 → 0 ✅
eslint — 새 파일 둘                     0건 ✅
eslint — 저장소 전체                    81 error / 126 warning = 기준선 그대로 ✅

❌ npm run build     확인 못 함 (fonts.googleapis.com 403 · 이 환경 제약)
❌ 실기기 발행        확인 못 함 (브라우저·API 키 없음)
❌ 실제 AI 서술       ★확인 못 했습니다 — 아래 7장
```

⚠️ 처음에 새 파일이 기준선을 82/129 로 올렸습니다(`prefer-const` 1 · 안 쓰는 import 3).
전부 고쳐 **81/126** 으로 되돌렸습니다.

---

## 7. ⚠️ 확인 못 한 것 — 솔직히

```
① ★AI 가 이 재료로 «무엇을 써 오는가» 를 못 봤습니다
   확인한 것은 «AI 에게 무엇이 나가는가» 까지입니다. (36부 15-e2e 와 같은 한계)
   ⚠️ 특히 경고가 다섯 줄까지 나가므로 AI 가 «겸손» 을 잃고 겁주는 문장을 쓸 위험이 있습니다.
   ★실기기로 한 사람을 발행해 자원오행·사주보완 대목을 눈으로 보십시오.
   문장이 무거우면 프롬프트의 [★위 «참고하실 자리» 를 다루는 법] 을 조이는 쪽입니다.

② max_tokens 3500 을 아직 안 고쳤습니다
   재료가 늘었으므로 «잘림» 위험이 커졌습니다.
   1단계 점검에서 지적한 자리이고 3단계 후보입니다. (교훈 DS)

③ 배점과 감점 폭은 «제가 정한 값» 입니다
   흐름 30 / 용신 40 / 균형 30 · −12 / −15 / −8 / +6 / +4
   무작위 표본으로 분포는 재봤지만 «옳은 값» 인지는 연재쌤 판단입니다. (교훈 DK)

④ 개명 화면 넷은 아직 judgeResource 를 안 씁니다
   diagnoseName(옛 엔진)으로 후보를 줄 세우고 있습니다.
   ★일부러 그렇게 두었습니다 — 「내이름 감정」에서 먼저 서술이 괜찮은지 보고
     그다음에 후보 정렬을 옮기는 것이 순서입니다. (교훈 DU)
```

## 8. ★연재쌤 확인이 필요한 자리 셋

```
① 고립(孤立) 잣대        judgeIsolated() — 우리가 정한 것. 인수인계서 ㉖ 미결
② 상극 예외의 범위        용신만 인정할지, 희신까지 인정할지
                         (CLASH_EXEMPT_INCLUDES_HEEKSIN)
③ 배점과 감점 폭          기신 −15 이 적절한지, 과다 −12 가 적절한지
```

---

## 9. 다음 (3단계 후보)

```
· max_tokens 3500 → 넉넉히 (또는 안 보내기) · res.ok 검사 · logAiError  ← ★1단계 점검에서 지적
· 개명 화면 넷을 judgeResource 로 옮기기 (후보 정렬)
· DB 2단계 컬럼 — resource_ohaeng_secondary · radical_ohaeng · meaning_ohaeng
  → JudgeChar.secondary 가 이미 자리를 비워 두었습니다 (route.ts toJudgeChar)
· scoreSuri 의 구멍 — 4글자 이상 격 0개인데 «좋음» · 외자 격 중복 · 복성
· strokes → strokes_kangxi 로 옮기기
```

**★이 문서의 «이러면 된다» 도 다시 재 주십시오. (교훈 DK)**
