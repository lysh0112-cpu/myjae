# 44부 26차 — 보관함 열 곳을 «한 모습» 으로

```
기준 커밋   8672891  (44부 25차까지 올라간 상태)
검사        1,763 → ★1,785건 · 실패 0
              오행·용신 그물 336 → 349  (㊺ 신설)
              작명 동선 그물 543 → 545  (검사 둘을 새 짜임에 맞게 고침)
타입        npx tsc --noEmit 통과
eslint      오류 82 · 경고 138  (기준선 그대로)
변경        수정 12 · 신규 2 = 14 파일 · ★800줄이 지워지고 537줄이 들어옴
⚠️ 빌드     ★제 쪽에서 «확인 못 했습니다» — 구글 폰트가 막힌 자리입니다
```

## 넣는 법

```bash
cd myjae
unzip -o ../myjae-44bu-26-20260803.zip
git status --porcelain      # ★ M 12줄 + ?? 3줄
npm run verify              # 실패 0 · 오행·용신 349 · 작명 동선 545
npx tsc --noEmit
★npm run build              # ⚠️ 대표님 자리에서 한 번 (25차와 같은 까닭)
git add -A
git commit -m "보관함 열 곳을 공용 부품 둘로 통일 (44부 26차)"
git push
```

---

# 무엇이 문제였나

25차에 «삭제 팝업» 은 한 부품이 되었는데, **보관함 화면 자체가 여전히 열 벌 복사본**이었습니다.

```
아홉 곳    값이 «우연히» 같았습니다 (누가 하나만 고치면 그날로 갈라집니다)
이사택일    혼자 다른 색·크기 · 「N건」 없음 · 버튼이 «위» 에
내이름      제목 색만 #1a1a1a
아래 버튼   서비스마다 제각각 — 보라 #785aaa · 분홍 #c85a8c · 자주 #b45a78 ·
            흙색 #967850 · 갈색 #b46e46 · 주황 #c8783c …
```

**⚠️ 대표님 확정 — 「모두 통일해줘. 서비스별로 보관함을 차별화할 필요없어」**

---

# ★무엇을 했나

## ① 부품 둘

```
app/components/common/StorageShell.tsx   ★틀 — 바탕·머리말·「N건」·기다림·빈 화면·아래 버튼
app/components/common/StorageRow.tsx     ★줄 — 카드 테두리·여백·✕ 버튼
```

### ★한 모습이 «한 곳» 에 — `S`

```ts
bg   #FDF6F0   바탕
bar  rgba(250,250,248,0.96) + blur(10)   머리말
line #f0e0d5   선
card #FFFBF7   줄카드
ink  #3a2e28   제목        sub #5c3a1e  잔글씨
back #96502e   ←           del #6b5340  ✕
btn  #b46e46   ★아래 버튼 — 서비스별 색을 걷어내고 하나로
```

⚠️⚠️ **색·크기를 바꾸시려면 `StorageShell` 의 `S` 한 곳만 고치십시오. 열 화면이 함께 바뀝니다.**

### ⚠️ 줄 «안» 은 손대지 않았습니다

부품은 **테두리와 ✕ 만** 맡습니다. 줄 안쪽은 화면마다 정말 다릅니다 —
내 이름의 한자 22px, 궁합의 「이름 ♥ 이름」, 타로의 관심사 배지, 택일의 kind 배지.
★그대로 `children` 으로 받습니다. **한 글자도 안 옮겼습니다.**

⚠️ ✕ 가 카드 누르기(결과로 이동)와 겹치지 않게 하는 `stopPropagation` 은
**부품이 대신합니다.** 화면에서 또 적지 마십시오.

## ② 열 화면을 갈아 끼웠습니다

```
궁합 · 사주 · 물상 · 타로 · 내 이름 · 결혼택일 · 출산택일 · 이사택일 · 진로적성 · 합격운
★800줄이 지워지고 537줄이 들어왔습니다
```

### ★이사택일 — 정본으로 끌어올렸습니다

```
전   버튼이 «위» · 「N건」 없음 · 카드 #FFFDF9 · 글 #7A6440 · ✕ 15px #C9BBA6
★후  나머지 아홉 곳과 «똑같습니다»
```

### ★서비스 색을 «전부» 걷어냈습니다 (대표님 지시)

```
걷어낸 것   아래 버튼 색 · 이름 두 글자 배지 색 · 궁합 등급 문구 색 ·
            합격운 안내 상자의 분홍
남긴 것     ⚠️ 타로 «관심사» 배지 · 내 이름 «종류/관계» 태그 · 택일 «kind» 배지
            ★이것들은 서비스 색이 아니라 «한 화면 안에서 기록을 가르는» 색입니다.
              하나로 만들면 뜻이 사라집니다.
```

⚠️ 쓰이지 않게 된 색 상수 아홉 개(`ACCENT`·`BG`·`CARD`·`LINE`·`accent`·`line`·`ink`·`SOFT`)는
**지운 자리에 까닭을 적어 두었습니다.** 되살리실 때 무엇이 있었는지 알 수 있습니다.

## ③ 🔴 사진에 있던 결함 하나 — 「궁합 궁합」

```
app/manseryeok/couple-storage/page.tsx
  「+ 새 {info.badge} 궁합 보기」   ⇒ ★화면에 「+ 새 궁합 궁합 보기」
  「아직 저장된 {info.badge} 궁합이 없어요」 ⇒ 「궁합 궁합이 없어요」
```

연인·부부 보관함을 하나로 합칠 때 badge 가 '연인'→'궁합' 이 되면서 **말이 겹친 것**입니다.
대표님이 보내 주신 사진에 그대로 찍혀 있었습니다. 고쳤습니다.

## ④ 검사

### ★㊺ 신설 — 다시 갈라지지 않도록 (13가지)

```
★열 곳이 «모두» StorageShell·StorageRow 를 쓰는가 (10/10)
⚠️ 화면이 «제 머리말» 을 따로 그리지 않는가   ← ★이것이 열 벌로 갈라졌던 까닭입니다
⚠️ 화면이 «제 ✕ 버튼» 을 따로 그리지 않는가
★서비스 색이 남아 있지 않은가
🔴 「궁합 궁합」이 사라졌는가
```

### ⚠️⚠️ 작명 동선 검사 둘을 «고쳤습니다» — 헐겁게 한 것이 아닙니다

부품으로 옮기자 43부의 두 검사가 걸렸습니다.

```
🔴 ★하단 버튼이 갈래마다 «하나» 입니다
🔴 머리의 건수가 «보이는 목록» 과 같습니다
```

**둘 다 «뜻» 이 아니라 «자리» 를 보고 있었습니다** — 버튼과 「N건」이 화면 파일 안에
있다고 보고 낱말로 훑고 있었습니다. 그래서 **뜻을 그대로 재도록 고쳤습니다.**

```
전   /view.button === '작명' && \(/          ← 화면이 버튼을 «직접 그린다» 고 봄
★후  ① 화면이 갈래를 보고 «글자 하나» 를 고르는가   (view.button === '작명' ?)
     ② ★틀이 그 글자를 «한 번만» 그리는가          (actionLabel && ( 이 정확히 1회)

전   /shownRecords.length}건/
★후  ① 화면이 «보이는 목록의 수» 를 넘기는가  (count={records ? shownRecords.length : null})
     ② 틀이 그 수를 「N건」으로 그리는가
```

★**역시험을 두 번 했습니다** —

```
· 건수를 일부러 «전체 건수» 로 바꾸니 → 🔴 잡았습니다
· 틀이 버튼을 «두 번» 그리게 망가뜨리니 → 🔴 잡았습니다
```

⚠️ 검사를 지워서 통과시킨 것이 «아닙니다». 오히려 **재는 자리가 둘로 늘었습니다.**

---

# ⬜ 배포 후 실기 확인

```
□ ★열 보관함이 «같은 모습» 인지 — 머리말·카드·✕·아래 버튼
□ ★★이사택일 — 버튼이 «아래» 로 내려왔는지 · 오른쪽 위에 「N건」이 생겼는지
□ ★진로적성 — 보라 배지·보라 버튼이 «갈색» 으로 바뀌었는지
□ ★합격운 — 분홍이 사라졌는지 (안내 상자 포함)
□ 🔴★궁합 — 아래 버튼이 「+ 새 궁합 보기」인지 (「궁합 궁합」이 «아니어야» 합니다)
□ ★내 이름 정밀분석 — 버튼이 갈래에 맞게 «하나» 인지 · 「N건」이 보이는 수와 같은지
□ ⚠️ 타로 관심사 배지 · 내 이름 종류 태그 · 택일 kind 배지는 «색이 그대로» 인지
□ ★줄을 눌러 결과로 가는지 · ✕ 를 눌러도 결과로 «안» 가는지 (겹침 방지)
□ ★빈 보관함에서 그림글자와 안내가 보이는지
```

⚠️ 검사 1,785건이 다 통과해도 실기 확인은 «따로» 입니다. (44부 교훈)

---

# 🔴 남은 일

```
□ 브라우저 기본 confirm() 여섯 곳 — 통합 보관함·마이페이지·상담사·관리자
   ⚠️ 회원탈퇴·영구삭제는 «브라우저 창이 오히려 나은» 자리라 여쭙고 하겠습니다
□ 🔴 「"요즘 어때, 괜찮아?" 중복」 — step8Timeline.ts:235 가 짚이는 자리입니다.
   ⚠️ 화면 사진을 주시면 그 자리에서 잡겠습니다
□ 「六 대화와 역할」 짓기 (step6Roles)
□ 22차 실기 확인
```
