# 44부 23·24차 «통합» — 오행 그래프 «가운데 0» · A4 궁합서 · 버튼 나란히

```
기준 커밋   726cf74  (44부 22차까지 올라간 상태)
검사        1,727 → ★1,754건 · 실패 0   (오행·용신 그물 300 → 327)
타입        tsc --noEmit 통과
빌드        ★실제 next build 통과
eslint      오류 82 · 경고 138  (그대로)
변경        수정 3 · 신규 1 = 4 파일
```

## 넣는 법

```bash
cd myjae
unzip -o ../myjae-44bu-23and24-20260803.zip
git status --porcelain      # ★ M 세 줄 + ?? 세 줄
npm run verify              # 실패 0 · 오행·용신 그물 327
npx tsc --noEmit
git add -A
git commit -m "오행 그래프 가운데 0 · A4 궁합서 · 버튼 나란히 (44부 23·24차)"
git push
```

---

# ① 오행 그래프 — ★«양방향» 으로

## 무엇이 달라졌나

```
전   [아내 막대][★오행 이름][남편 막대]
     ⇒ 이름이 «가운데» 라 0 이 어디인지 알기 어려웠습니다
     ⇒ 막대가 이름 쪽으로 «안으로» 붙어 방향이 헷갈렸습니다

★후  [목]  아내 막대 ◀──(0 중앙)──▶ 남편 막대  [목]
     [화]  아내 막대 ◀──(0 중앙)──▶ 남편 막대  [화]
      …
```

```
★맨 가운데가 «0» 입니다. 두 막대가 «바깥으로» 뻗습니다
★오행 이름을 «양끝» 에 대칭으로 둡니다
★가운데에 0 선을 그려 시작점을 보입니다
★「아내/남편」 타이틀을 그래프 «위» 좌우로 옮겼습니다
```

### 실제로 이렇게 그려집니다 (사진의 그 두 분)

```
        아내 ◀────(0)────▶ 남편
  목               │█████████████  목   (0 / 45)
  화               │███████        화   (0 / 25)
  토 ██████████████│█              토   (50 / 5)
  금       ████████│████           금   (30 / 15)
  수         ██████│███            수   (20 / 10)
```

## ⚠️ 지시대로 «CSS 만» 바꿨습니다

```
★대표님 지시 — "새로 만들지 마십시오. CSS 스타일 및 레이아웃만 변경"
★계산(compareOhaeng)은 «한 글자도» 안 건드렸습니다
★색(EL_BG · 연재쌤 지정)도 그대로입니다
⚠️ 금(金)은 흰색이라 테두리로 드러냅니다 — 옛 처리 그대로
```

★`NAME_W = 44px` — 「목 나무」가 줄바꿈 없이 들어가는 너비입니다.
⚠️ 좁히면 「금 쇠」만 남고 나머지가 접힙니다. **재고 정한 값입니다.**

---

# ② A4 궁합서 — 인쇄 / PDF 저장

```
★app/manseryeok/couple-result-new/components/CoupleCertificate.tsx  신설
★화면 하단에 「🖨 A4 궁합서 (인쇄 / PDF 저장)」 버튼
```

## ⚠️⚠️ PDF 라이브러리를 «더하지 않았습니다» (교훈 [의존])

```
① 한글 글꼴을 함께 실어야 해 꾸러미가 몇 MB 늘어납니다
   ⚠️ 안 실으면 «글자가 깨진» 궁합서가 손님께 나갑니다. 그것이 더 나쁩니다
② 의존을 더하면 package-lock 까지 함께 올려야 합니다
★브라우저 인쇄가 어느 기기에서나 「PDF로 저장」을 함께 줍니다
  — 아이폰·안드로이드·PC 모두 됩니다
⇒ ★43부 선명장(NamingCertificate)과 «같은 방식» 입니다. 전례를 따릅니다
```

## ★[왜 새 창을 여는가]

```
지금 화면에 @media print 를 걸면 «화면의 다른 것» 을 전부 숨겨야 하고,
한 군데만 빠뜨려도 궁합서에 버튼이 찍혀 나갑니다.
★따로 만든 문서를 새 창에 띄우면 «그것만» 인쇄됩니다. 어긋날 자리가 없습니다.
```

## ★종이에서도 «가운데 0» 그래프가 깨지지 않습니다

```
⚠️ flex 가 아니라 «표(table)» 로 그립니다 —
   인쇄 엔진마다 flex 를 다르게 재는 일이 있어 «표» 가 가장 안전합니다
★이름도 양끝에 대칭 · 가운데 0 선도 그대로
```

## ★쪽 나눔

```
· 「→」 솔루션이 쪽 경계에서 «잘리지 않습니다»  (page-break-inside: avoid)
· 제목만 홀로 남지 않습니다                     (page-break-after: avoid)
· 오행 그래프도 통째로 한 쪽에 들어갑니다
· A4 여백 12mm — 선명장(9mm)보다 조금 넓게. 글이 길어 숨 쉴 자리가 필요합니다
```

## ⚠️ 값을 «다시 계산하지 않습니다» (교훈 CJ)

```
닮음·채움을 compareOhaeng «한 창구» 에서 받습니다
★두 벌로 세면 «종이와 화면이 다른 수» 를 말하는 날이 옵니다
```

## ⚠️ 그 밖

```
· 팝업이 막히면 «조용히 넘어가지 않고» 알려 드립니다 (교훈 U)
· 통변이 «다 나온 뒤» 에만 버튼이 보입니다 — ★반쪽 궁합서를 내지 않습니다
· 궁합서는 «판정을 하지 않습니다». 받은 값을 그리기만 합니다 (교훈 CJ)
```

---

# ③ 하단 두 버튼을 «나란히» (24차)

```
전   ┌──────────────────────────┐
     │  🖨 A4 궁합서 (인쇄/PDF 저장) │
     └──────────────────────────┘
     ┌──────────────────────────┐
     │        📋 해설 복사        │
     └──────────────────────────┘

★후  ┌───────────┐ ┌───────────┐
     │🖨 A4 PDF저장/인쇄│ │ 📋 해설 복사 │
     └───────────┘ └───────────┘
```

★**이름도 바꿨습니다** — 「A4 궁합서 (인쇄 / PDF 저장)」 → **「A4 PDF저장/인쇄」**
   ⚠️ 절반 너비에 들어가야 해서 짧게 줄였습니다.

## 구현

```
· 감싸는 div 에 flexDirection:'row' · gap:8 · alignItems:'stretch'
· 두 버튼 각각 flex:1 — 절반씩
· alignItems:'stretch' 로 «높이를 맞춥니다»
· 색·글꼴·아이콘·둥글기는 «그대로» 입니다 (지시대로)
```

## ⚠️⚠️ 공용 부품을 «고치지 않았습니다»

```
CopyTextButton 은 작명·사주보기도 쓰는 «공용 부품» 입니다.
★부품을 고치지 않고 «감싸서» 너비를 맞췄습니다. (교훈 E)
   감싼 div 에 flex:1 · className="copy-half" 를 주고
   `.copy-half > button { width: 100% }` 한 줄로 채웁니다.
⇒ ★다른 화면은 «하나도» 안 바뀝니다. 검사가 그것을 봅니다.
```

⚠️ 부품 안에 `marginTop:8` 이 박혀 있어 높이가 어긋납니다 —
   ★A4 버튼에도 같은 marginTop 을 주어 **윗선** 을 맞췄습니다.
⚠️ 통변이 아직이면 A4 버튼이 **없습니다** — 그때는 복사 버튼이 혼자 남아
   가로로 꽉 찹니다.

---

# ⬜ 배포 후 실기 확인

```
□ ★★오행 그래프 — 가운데가 «0» 이고 두 막대가 «바깥으로» 뻗는지
□ ★오행 이름이 «양끝» 에 대칭으로 있는지 (가운데가 «아니어야» 합니다)
□ ★가운데에 옅은 세로선(0 선)이 보이는지
□ 「아내 ◀」·「남편 ▶」이 그래프 «위» 좌우에 있는지
□ ⚠️ 「금 쇠」·「목 나무」가 줄바꿈 없이 한 줄로 들어가는지

□ ★「🖨 A4 궁합서」 버튼이 하단에 보이는지
   ⚠️ 통변이 다 나오기 «전» 에는 «안 보여야» 합니다
□ ★눌러서 새 창이 열리는지 — 위에 「인쇄 / PDF 저장」 버튼
□ ★그 버튼을 누르면 «PDF로 저장» 을 고를 수 있는지
□ ★★종이에서도 오행 그래프가 «가운데 0 · 양끝 이름» 인지
□ 「→」 솔루션 줄이 쪽 경계에서 «잘리지 않는지»
□ 명식이 종이 위쪽에 «오행 색» 그대로 나오는지

□ ★두 버튼이 «가로로 나란히» 있는지 (24차)
□ ★두 버튼의 «높이가 같은지»
□ ★이름이 「A4 PDF저장/인쇄」인지
□ ⚠️ 작명·사주보기 화면의 「해설 복사」가 «그대로» 인지 (공용 부품)
```

⚠️ 검사가 1,754건 전부 통과해도 실기 확인은 «따로» 입니다.

---

# 🔴 다음

```
□ 🔴 「"요즘 어때, 괜찮아?" 하고 한 번 물어봐 주시는 것」이 «중복»
   ★대표님이 알려 주신 것입니다. 아직 «안 잡았습니다».
   짐작 — step8Timeline 의 흔들림 솔루션이 해마다 반복됩니다
   ⚠️ ★화면부터 «열어 보고» 답할 것. 짐작으로 말하지 말 것

□ 「六 대화와 역할」 짓기 (step6Roles)
   ★교재 141쪽 신강 = 비겁 + «인성» / 신약 = 식상+재성+«관성»
   ⚠️ 8단계 문서는 인성·관성이 «뒤바뀌어» 있습니다

□ 22차 실기 확인 (아직 못 하셨습니다)
   □ 카드가 «펼친 채» 인지 · 대목이 «열둘» 인지
   □ 한 대목이 «다섯 문단쯤» 인지 · 전체 5,000자 안팎
   □ 「자식운」에 연표가 «없는지»
```
