# 1단계 작업 보고 — 한자 DB 정제 · 오행 정규화 단일 창구

```
작업 시점   2026-07-30
저장소      lysh0112-cpu/myjae   기준 HEAD 54c5034
원자료      ___덕암_인명용한자_가나다_599페이지_완성.xlsx  (시트 '인명용한자' · 5,111행 × 13열)
산출물      ① lib/saju/ohaeng.ts                    신설 188줄
            ② 1단계-오행정규화-20260730.patch        고친 파일 5개 · 578줄
            ③ _SQL_hanja_stage1_20260730.sql        마이그레이션 8단계
```

---

## 0. ★먼저 — 잠재 위험이 아니라 «지금 나가고 있는 오답» 이었습니다

엑셀 원자료의 **자원오행 컬럼은 100% 한자** 였습니다. 한글이 한 글자도 없습니다.

```
木 1,449   火 1,048   水 1,019   金 824   土 771      합 5,111
한글 표기(목/화/토/금/수)  0건
```

그런데 `lib/saju/naming.ts` 의 상생표는 **한글 키** 입니다.

```ts
const GENERATES = { 목:'화', 화:'토', 토:'금', 금:'수', 수:'목' }
GENERATES['木']  // → undefined
```

그리고 「내이름 감정」(`diagnosis/page.tsx:361`)만이 DB 값을 **날것으로** 엔진에 넣고 있었습니다.

### 실제 자료로 측정한 영향

실제 5,111자에서 무작위 이름 4,000개를 만들어 고침 전/후를 대조했습니다.

| 무엇이 | 바뀐 비율 |
|---|---|
| 자원오행 상생 판정 | **65.0%** |
| 자원오행 등급 | **65.0%** |
| 용신 충족(`hasYongsin`) | **35.8%** |
| **★종합 등급** | **64.8%** |

고침 전에는 자원오행 등급이 **모든 이름에서 «아쉬움»** 이었습니다(상생 판정이 언제나 0건).

### 손님에게 나간 피해는 등급이 아니라 AI 서술입니다

등급은 화면에 안 나오는 것이 방침(`naming.ts:8`)이라 눈에 띌 자리가 없었습니다.
대신 AI 에게 이런 **틀린 사실**이 갔습니다.

```json
"자원오행": { "links": [{"rel":"기타"}], "saengCount": 0, "total": 2 }
"사주보완": { "hasYongsin": false, "yongsinChars": [] }
```

프롬프트가 「사실 데이터에 없는 내용은 지어내지 마세요」이므로 AI 는 성실하게
**「상생 관계가 보이지 않는다」 「용신을 담은 글자가 없다」** 로 썼습니다.

### 실제 대조 (柳承炫 · 용신 화)

```
❌ 고침 전   柳(木)→承(金)→炫(火)   상생 0/2   hasYongsin=false   종합 아쉬움
✅ 고침 후   柳(목)→承(금)→炫(화)   상생 0/2   hasYongsin=true    종합 보통
                                                    ↑ 炫이 화를 담고 있는데 못 보던 자리
```

⚠️ 용신이 «목» 인 경우는 고침 후에도 «아쉬움» 입니다 — 목이 성(柳)에만 있고
`hasYongsin` 은 이름만 보기 때문입니다. **점수를 일괄로 올리는 고침이 아닙니다.**

---

## 1. `lib/saju/ohaeng.ts` 신설

### 공개 함수

| 함수 | 반환 | 쓰는 곳 |
|---|---|---|
| `normalizeOhaeng(raw)` | `Ohaeng \| null` | ★기본 창구. 새 코드는 이것을 쓰고 null 을 다루십시오 |
| `ohaengOrEmpty(raw)` | `Ohaeng \| ''` | 옛 `ohaengChar` 자리에 그대로 끼우는 다리 |
| `parseOhaeng(raw)` | `{ohaeng, all, cleaned, problem}` | 진단·검사기·복수 오행 |
| `normalizeOhaengStrict(raw, ctx)` | `{ohaeng, problem}` | 실패를 «세는» 창구 |
| `stripInvisible(raw)` | `string` | 보이지 않는 문자 걷어내기 |
| `cleanHanja(raw)` | `string` | `' 熺'` 같은 공백 정제 |
| `isOhaeng(v)` · `OHAENG_ALL` | — | 타입 좁히기·순회 |

### 옛 `ohaengChar` 보다 나아진 세 가지

**① 폭 없는 문자를 잡습니다.** `trim()` 은 NBSP·전각공백·BOM 은 걷어내지만
ZWSP(U+200B)·ZWNJ·ZWJ·U+2060 은 **유니코드 범주가 Cf 라 공백이 아니어서 남깁니다.**

**② NFC 정규화를 합니다.** macOS 에서 만든 자료의 한글은 NFD 로 풀려 있을 수 있고,
그러면 눈으로 똑같은 «목» 이 `'목' === '목'` 에서 false 가 됩니다.

```
NFD '목' = U+1106 U+1169 U+11A8
  옛 방식  t.includes('목')  →  false   ← 조용히 실패
  새 방식  normalizeOhaeng() →  '목'  ✅
```

**③ 못 읽은 값을 «원값 그대로» 돌려주지 않습니다.**
옛 함수는 `return t` 로 원값을 통과시켰고, 그 값이 상생표에서 조용히 `'기타'` 로 떨어져
**감점 없이 지나갔습니다.** 새 함수는 `null`(또는 `''`)이고 `parseOhaeng` 이 사유를 남깁니다.

### 시험한 경계값 (전부 통과)

```
'木' '木(목)' ' 수 ' '水' '목' '토(土)' '金(금)' '수水'   → 정상 정규화
'土金'          → '토' + problem「오행이 둘 이상…」   ★조용히 넘기지 않습니다
'' '   ' null undefined 'earth'  → null + problem
'\u200b목' '木\ufeff' '\u3000火\u3000' '목\u00a0'  → 정상 (폭 없는 문자·전각공백)
NFD '목'        → '목'   ← 옛 방식이 놓치던 자리
' 熺' → cleanHanja → '熺'
```

---

## 2. 코드 수정 내역

### 고친 파일 5개

```
lib/saju/ohaeng.ts                                  신설 (+188)
app/manseryeok/naming/diagnosis/page.tsx            +22 −8    ★핵심
app/manseryeok/naming/rename/hanja/page.tsx         사본 제거 · 호출 8건
app/manseryeok/naming/rename/result/page.tsx        사본 제거 · 호출 5건
app/manseryeok/naming/rename/newhanja/page.tsx      사본 제거 · 호출 6건 + 저장경로 1
app/manseryeok/naming/rename/newresult/page.tsx     사본 제거 · 호출 7건 + 전달경로 1
                                                    합 258 삽입 / 69 삭제
```

### ① 「내이름 감정」 — 없던 그물을 겁니다

```ts
// app/manseryeok/naming/diagnosis/page.tsx  pickHanja()
hanja: cleanHanja(row.hanja) || row.hanja,          // ★' 熺' 방어
resourceOhaeng: ohaengOrEmpty(row.resource_ohaeng), // ★★여기가 틀리던 자리
```

한자 고르기 목록의 표시줄도 같은 창구로 돌렸습니다. 전에는 손님이 «木» 을 보고
판정은 «목» 으로 도는 어긋남이 있었습니다.

```ts
{ohaengOrEmpty(row.resource_ohaeng) || row.resource_ohaeng}·{row.strokes}획
//                                    ↑ 못 읽은 값이면 원값을 보여 줍니다(정보를 숨기지 않음)
```

### ② 개명 4화면 — 사본 네 벌을 걷어냈습니다

네 벌이 **한 글자도 다르지 않은 사본**이었습니다(대조 확인).
그 자리에 「여기에 다시 사본을 만들지 마십시오」 주석만 남겼습니다. (교훈 CJ)

### ③ ★사본 사냥 중에 찾은 «날것» 두 곳

`ohaengChar` 가 있던 화면 안에서도 두 줄이 정규화를 비껴가고 있었습니다.

| 자리 | 무엇이 새고 있었나 | 처리 |
|---|---|---|
| `newhanja:285` | **보관함에 저장되는** 값이 날것 | 정규화 |
| `newresult:232` | **상담사 화면에 넘기는** 값이 날것 | 정규화 |

판정 경로는 정규화되어 있었지만 **저장·전달 경로**가 빠져 있었습니다.
날것으로 저장하면 다시보기 때 그 값이 되돌아 들어옵니다.
`rename/hanja:185` 도 날것이지만 바로 다음 줄에서 감싸지므로 실제 누수는 아니었습니다.

### 검증 — 기준선과 정확히 같습니다

```
npx tsc --noEmit --skipLibCheck        0건 ✅
npx tsc -p tsconfig.strict.json        0건 ✅
npm run verify (prebuild 관문)          대운 18/18 · 일곱 갈래 전부 통과 ✅
eslint — naming 관련                    9 error / 12 warning   = 기준선 그대로 ✅
eslint — 저장소 전체                    81 error / 126 warning = 기준선 그대로 ✅
                                        (신설 ohaeng.ts 는 0건)
ohaengChar 잔존                         0건 ✅ (주석에만 이름이 남음)
날것 resource_ohaeng → 엔진             0건 ✅
```

⚠️ **실기기 확인과 `npm run build` 는 못 했습니다.** 36·37부와 같은 자리입니다
(브라우저·API 키 없음 / `fonts.googleapis.com` 403). 다만 앞선 점검에서
폰트와 Supabase 환경변수만 스텁으로 채우면 빌드가 완주하는 것은 확인했습니다.

---

## 3. 마이그레이션 SQL — 8단계

`_SQL_hanja_stage1_20260730.sql`. **한 번에 다 돌리지 마십시오.**
단계마다 «확인 쿼리» 가 붙어 있고, 결과를 보고 다음으로 가는 구조입니다.

| STEP | 무엇을 | 자동/수동 |
|---|---|---|
| 0 | 지금 상태 확인 (컬럼 구성 · 자원오행 표기 분포) | 확인만 |
| 1 | 🔴 공백·비가시문자 정제 + 재발 방지 CHECK | 자동 |
| 2 | ★`resource_ohaeng_primary` 신설 + NOT NULL + CHECK | 자동 |
| 3 | `strokes_kangxi` · `strokes_actual` 신설 (값은 복사만) | 자동 |
| 4 | 🔴 중복 21쌍 — **자동 삭제 안 함** | ★연재쌤 |
| 5 | 🔴 오행 표기 불일치 여섯 글자 | ★연재쌤 |
| 6 | `is_name_use` 신설 (`grade='不用'` 문자열 비교 대체) | 자동 + 9건 검토 |
| 7 | `hanja_pickable` 뷰 — 화면은 이것만 읽습니다 | 자동 |
| 8 | 되돌리기 문장 | — |

### 엑셀 전수 조사로 찾은 오염 — 실측 숫자

**① 공백 오염 — 딱 한 건**

```
행 5002   한자 컬럼   ' 熺'   ← 앞에 일반 공백(U+0020)
```

전체 5,111행 × 10컬럼을 훑어 이 하나뿐입니다. 대표님이 말씀하신 그 글자입니다.
`WHERE hanja = '熺'` 가 안 걸리고 **화면에는 «熺» 로 정상처럼 보입니다.**

**② 중복 21쌍 / 42행 — 그 가운데 19쌍이 내용이 다릅니다**

구조적 원인을 찾았습니다. **행 5020 부터 자음이 «가» 로 되돌아가 «희» 까지 다시 흐릅니다.**
두 번째 배치(약 92행)가 **병합 없이 덧붙여진** 것입니다.

획수가 다른 것이 가장 위험합니다 — 수리 4격이 통째로 갈립니다.

```
린 麟   23획(鹿) ↔ 17획(鹿)
림 琳   13획 자원金 ↔ 12획 자원木      ← 획수·자원오행 둘 다
명 明    8획 자원火(日부) ↔ 9획 자원木(目부)
윤 潤   16획 자원水(水부) ↔ 15획 자원木(門부)
유 牖    9획 자원土 ↔ 15획 자원木
반 斑   12획 자원木 ↔ 17획 자원水
경 冏    7획 자원火(冂부) ↔ 7획 자원水(口부)
예 藝   21획(艹) ↔ 17획(艸)
그 밖 — 嶼·聖·嬿·甕·慚·儇·瀅·譓·澔·曦·壻 (품격·비고만 다른 것 포함)
내용이 «같은» 중복 — 凜·揷 두 쌍
```

**어느 쪽이 옳은지 저는 모릅니다.** 원본 책을 봐야 갈립니다.
그래서 **지우지 않고** `is_active = false` 로 «쉬게» 하는 방식을 썼습니다.
내용이 같은 두 쌍만 자동 정리하고, 내용이 다른 19쌍은 **둘 다 살려 두고 표시만** 합니다.
`review_note` 에 사유를 적고 STEP 4-5 가 연재쌤께 드릴 검토표를 뽑습니다. (교훈 CR·DX)

**③ 오행 표기 불일치 — 여섯 글자**

같은 음 + 같은 한자인데 자원오행이 갈린 것: `林 明 潤 牖 斑 冏`

⚠️ **동자이음(같은 글자, 다른 음)은 고치지 마십시오.** 엑셀에 «동자이음» 표기가 1,057건 있고,
음이 다르면 자원오행이 달라도 되는 유파가 있습니다. SQL 에서 5-1(오류)과 5-2(정상 가능)를 갈라 뽑습니다.

**④ 파생 컬럼 정합성 — 자동으로 고치지 않았습니다**

```
획수 → 획수오행 규칙(1,2=木 3,4=火 5,6=土 7,8=金 9,0=水) 어긋남   49건
획수 → 음양 규칙(홀=양, 짝=음)                        어긋남   56건
```

**어느 쪽이 오타인지 알 수 없습니다.** 획수가 틀렸을 수도, 파생값이 틀렸을 수도 있습니다.
다만 코드는 획수에서 **직접 계산** 하므로(`naming.ts` `scoreYinYang`) **지금 손님에게 나가는 값은 획수 기준**입니다.
그래서 급하지 않고, 검토 목록으로만 뽑습니다.

**⑤ 자의품격 ↔ 비고 불일치 — 9건**

```
不用 947  ↔  비고 '불가' 계열 941        → 6건 어긋남
不用 인데 비고 비어 있음         覡(행 212)                    1
비고 '불가' 인데 품격 不用 아님    諫·爺·褑 (모두 小吉)           3
大吉 인데 비고 '사용'                                          3
中吉 인데 비고 '선별사용'                                       1
小吉 인데 비고 '사용'                                           1
```

⚠️ **편찬자가 이미 알고 있었습니다.** 엑셀 «확인필요» 컬럼 10건에
「책 494p 원본 품격'大吉'인데 비고'사용'으로 불일치 표기 - 원본 재확인 필요」처럼 적혀 있습니다.
그 10건 목록을 SQL 주석에 그대로 옮겨 두었습니다.

⚠️ 그리고 구분자가 섞여 있습니다 — `불가;신체` vs `불가(신체)`. 파싱할 때 둘 다 받으십시오.

**⑥ `is_name_use` — 지금 방식의 위험**

```ts
if (row.grade === '不用') return true    // diagnosis/page.tsx:115
```

**한자 문자열 비교**입니다. 표기가 하나만 달라도(공백·`불용`) 조용히 통과합니다 —
`' 熺'` 와 같은 종류입니다. `is_name_use boolean` 으로 옮기고, `grade` 가 비면
**안전한 쪽(false)** 으로 둡니다. 모르는 글자를 손님에게 권하지 않기 위함입니다.

---

## 4. ⚠️ 1단계에서 «하지 않은» 것 — 2단계로

```
· resource_ohaeng_secondary (복수 자원오행)
· radical / radical_ohaeng / meaning_ohaeng · resource_basis · resource_confidence
    ★엑셀에 «부수» 컬럼이 이미 있습니다. 옮기면 됩니다
· school_variants (유파차)
· 코드가 strokes 대신 strokes_kangxi 를 읽게 옮기기
· 코드가 hanja 표 대신 hanja_pickable 뷰를 읽게 옮기기
· 중복 19쌍 · 오행 불일치 6글자 · 품격 불일치 9건 → 연재쌤 판정 뒤 반영
· 판정 알고리즘 고도화 (명세서 3부 — judgeResource · 과다 억제 · 기신 회피)
```

⚠️ **판정 알고리즘 개선(명세서 3부)을 1단계와 함께 하지 않은 이유** —
1단계는 «오답을 고치는 것» 이고 3부는 «잣대를 바꾸는 것» 입니다.
함께 하면 결과가 바뀌었을 때 어느 쪽 때문인지 갈라볼 수 없습니다. (교훈 DU)

### ★그리고 검사 그물 — `16-verify-naming.ts` (지금 0건입니다)

naming 은 다섯 그물 어디에도 없습니다. 1단계가 만든 표준이 무너지지 않게 최소 셋은 필요합니다.

```
① resource_ohaeng_primary 가 다섯 값 밖인 줄이 0건인가
② 같은 음+한자인데 자원오행이 다른 줄이 0건인가
③ 앞뒤 공백·폭 없는 문자가 있는 hanja·hangul 이 0건인가
④ normalizeOhaeng 경계값 (위 시험 목록을 그대로)
```

①②③은 Supabase 를 부르므로 `prebuild` 관문 밖에 두십시오. ④는 순수 함수라 관문에 걸 수 있습니다.

---

## 5. 적용 순서 — 되돌릴 수 있게

```
① 패치를 얹습니다            git apply 1단계-오행정규화-20260730.patch
② tsc · npm run verify       (제가 확인했습니다. 다시 한 번 보십시오)
③ npm run build              ★로컬에서. .env.local 이 있어야 Supabase 프리렌더가 통과합니다
④ SQL STEP 0 만 돌립니다     ★자원오행 표기 분포를 눈으로 보십시오
⑤ STEP 1~3 · 6~7            (자동)
⑥ STEP 4~5 검토표를 연재쌤께
⑦ 실기기 — 「내이름 감정」으로 아는 이름 하나를 풀이해 보십시오
```

⚠️ **④를 건너뛰지 마십시오.** 제가 본 것은 «엑셀» 이고 «DB» 는 못 봤습니다.
DB 가 이 엑셀로 적재된 것이 맞는지는 STEP 0-2 한 줄이 갈라 줍니다.
만약 DB 는 이미 한글로 들어 있다면, 0부의 «64.8%» 는 일어나지 않았고
이번 고침은 **재발 방지**로만 값이 있습니다. 그것도 해 두어야 하는 일입니다.

**★이 문서의 «이러면 된다» 도 다시 재 주십시오. (교훈 DK)**
