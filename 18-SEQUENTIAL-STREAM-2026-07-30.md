# 🔧 합격운 스트리밍 — 진로적성과 같은 구조로 단순화 (2026-07-30, 3차)

```
기준 커밋   b9ccd8e  (2026-07-30 13:13)
고친 파일   3개  ·  +121 / −93
```

---

## 1. 대표님 지시대로 «나란히 부르기» 를 버렸습니다

```
전   Promise.all 로 세 묶음을 동시에      ← 빠르지만 실패할 길이 셋
후   한 번에 하나씩 차례로               ← 화면에는 «한 흐름» 으로 이어집니다
```

`career-result/page.tsx` 178~245줄의 얼개를 그대로 가져왔습니다. 새로 설계하지 않았습니다.

```ts
// 진로적성 229줄 — 이 한 줄이 핵심입니다
if (parsed.text) { acc += parsed.text; if (!cancelled) setTong(acc) }
```

합격운도 이제 `acc` **하나**에 계속 쌓습니다. 묶음별로 나누지 않습니다.
손님 눈에는 진로적성과 똑같이 글이 처음부터 끝까지 이어서 흐릅니다.

### 나란히 부르기가 왜 위험했나
```
· 같은 키로 스트림 셋이 동시에 열려 분당 출력 토큰(OTPM)을 다툽니다
  묶음 상한 2,400 + 2,400 + 3,600 = 8,400 을 한꺼번에 «잡아 둡니다»
  ⚠️ Anthropic 은 실제 출력이 아니라 max_tokens 를 OTPM 예산으로 잡습니다.
     Tier 1 의 OTPM 이 8,000 이면 이 셋만으로 이미 넘습니다.
· 셋 중 하나만 막혀도 손님 화면은 «덜 온» 상태로 남습니다
· 어느 것이 왜 막혔는지 손님도 우리도 알기 어렵습니다
```

---

## 2. ⚠️ 한 가지는 지시와 다르게 했습니다 — 먼저 읽어 주십시오

대표님이 «단일 스트리밍 연결» 이라 하셨는데, **API 호출은 여전히 넷입니다.**
화면은 하나의 흐름이고, 호출만 차례로 넷입니다.

**«한 번에 일곱 갈래» 로는 갈 수 없습니다.** 35부가 하루를 써서 실측한 자리입니다.

```
여섯 대목 한 번  →  앞 두셋만 씀
세 대목 한 번    →  첫 하나만 씀
프롬프트를 네 번 다듬어도 같은 자리에서 실패
```

그 기록이 `buildExamSeven.ts` 머리말과 인수인계서 교훈 CV 에 남아 있습니다.
한 번에 일곱을 시키면 **①②③ 만 써 오고 ④⑤⑥⑦ 이 통째로 빕니다.**
지금 겪고 계신 «중간에 멈춤» 과 화면상 구별이 안 됩니다.

게다가 한 번에 일곱이면 `max_tokens` 가 8,400 이 되어
**한 번의 함수 호출이 가장 길어집니다** — Vercel 시간 제한에 가장 잘 걸리는 모양입니다.
«끊기지 않고 끝까지» 라는 목표와 정확히 반대로 갑니다.

```
★그래서 «호출은 차례로, 화면은 한 흐름» 으로 풀었습니다.
  손님 눈에는 진로적성과 똑같고, AI 에게는 한 번에 둘뿐입니다.
```

되돌리시려면 `ExamResultShell.tsx` 의 `for` 를 `Promise.all` 로 바꾸면 됩니다.
그러면 위에 적은 세 위험이 함께 돌아옵니다. 주석에 적어 두었습니다.

---

## 3. 함께 고친 것

### ① 묶음을 셋에서 넷으로 잘게 쪼갰습니다
```
전   2 + 2 + 3   (3묶음이 3,600 토큰으로 가장 길었습니다)
후   2 + 2 + 2 + 1
```
차례로 부르니 묶음을 잘게 쪼개는 것이 이득입니다.
```
① 한 번의 함수 호출이 짧아져 Vercel 시간 제한에 걸릴 여지가 줄어듭니다
② 한 묶음이 실패해도 잃는 것이 적습니다
③ 한 번에 둘뿐이니 교훈 CV 로부터 더 안전합니다
```

실측 — 가장 큰 호출이 `maxTokens 2,400` 입니다.
```
1묶음 [dna+subject]    프롬프트 5,517자 · maxTokens 2,400
2묶음 [ratio+monthly]  프롬프트 5,431자 · maxTokens 2,400
3묶음 [dday+apply]     프롬프트 5,406자 · maxTokens 2,400
4묶음 [mentor]         프롬프트 4,923자 · maxTokens 1,200
```

### ② 화면에서 «묶음» 말을 걷어냈습니다
```
전   0/3 묶음 마무리 · 585자 도착      ← 손님에게 «묶음» 은 우리 사정입니다
후   ✦ 풀이를 쓰는 중이에요…
     1,204자 · 계속 이어서 쓰고 있어요
```
★진짜 진행 표시는 «글이 차오르는 것» 자체입니다. 진로적성이 그렇게 합니다.

### ③ 멈춤 감지를 넉넉하게 풀었습니다
```
전   STALL 45초 · HARD 240초
후   STALL 60초 · HARD 180초
```
차례로 부르니 한 호출이 짧습니다. HARD 를 줄여 매달리는 시간을 짧게 했고,
STALL 은 늘려 «조금 느린 것» 을 실패로 오해하지 않게 했습니다.

⚠️ 중간까지 받은 글은 `acc` 에 이미 들어가 있어 **버리지 않습니다.**

---

## 4. 적용

```bash
cd myjae
git pull                                              # b9ccd8e 인지 확인
unzip -o 18-sequential-stream-2026-07-30.zip
npm run verify:all && npm run build
git add -A && git commit -m "합격운 스트리밍 — 진로적성과 같은 순차 단일 흐름으로 단순화"
git push origin main
```

`b9ccd8e` 를 새로 클론해 실제로 붙이고 확인한 뒤 넘깁니다.
```
git apply --check   충돌 0 ✅
적용본 tsc          0건 ✅
적용본 strict       0건 ✅
적용본 verify:seven 전부 통과 ✅
적용본 verify:live  발행 직전까지 전부 통과 ✅
eslint              4 error / 16 warning — ★기준선과 동일
```

⚠️ zip 은 세 파일을 통째로 덮어씁니다. 13:13 푸시 뒤에 그 파일을 손대셨다면
zip 대신 안에 든 `.patch` 를 쓰십시오.

---

## 5. 이번에도 안 되면 — 다음에 볼 것 딱 하나

차례로 부르니 이제 **한 번에 하나만 흐릅니다.** 그런데도 중간에 끊긴다면
남은 원인은 하나뿐입니다 — **Vercel 함수 시간 제한.**

그때는 로그에서 이것만 보내 주십시오.

```
Vercel → 프로젝트 → Logs → /api/tongbyeon 필터
  ★Duration 값   (예: 1.2s / 58s / 300s)
  네 번의 호출이 각각 몇 초에 끝났는지
```

```
Duration 이 60s·300s 같은 «딱 떨어지는 값» 에 몰려 있으면  → 시간 제한 확정
       그때 고치는 길
         ① Settings → Functions → Max Duration 확인 (요금제 상한)
         ② maxTokens 를 더 낮춥니다 (지금 2,400 → 1,600)
         ③ 묶음을 일곱으로 쪼갭니다 (갈래 하나에 한 호출)
Duration 이 들쭉날쭉하고 짧으면                         → 시간 제한 아님
       그때는 화면의 실패 문구에 마우스를 올려 HTTP 상태코드를 알려 주십시오
```

⚠️ 그리고 `/admin → 🚨 AI 오류` 탭도 한 번 봐 주십시오.
`route.ts` 가 `!res.ok` 일 때 그 표에 이유를 남깁니다. 거기 한 줄이 있으면
추측할 필요가 없습니다.

---

## 6. 이번에 배운 것

### 교훈 DH — «나란히» 는 빠르지만 실패할 길을 곱한다 ★★★
세 묶음을 Promise.all 로 부르면 시간은 3분의 1이지만,
실패할 자리가 셋으로 늘고 셋이 서로 자원을 다툽니다(OTPM·동시 호출).
그리고 어느 것이 왜 막혔는지 알기 어려워집니다.
**«오래 걸려도 확실한 것» 이 «빠르지만 자주 막히는 것» 보다 낫다.**
속도는 나중에 되돌릴 수 있고, 신뢰는 되돌리기 어렵다.

### 교훈 DI — max_tokens 는 «쓰는 만큼» 이 아니라 «잡아 두는 만큼» 이다 ★★
분당 출력 토큰(OTPM) 예산은 실제 출력이 아니라 `max_tokens` 로 잡힙니다.
셋을 동시에 부르면 상한이 그대로 더해져 한꺼번에 예산을 먹습니다.
**나란히 부를 때는 «상한의 합» 이 분당 한도를 넘지 않는지 먼저 세라.**

### 교훈 DJ — «단순하게» 가 «한 번에 다» 를 뜻하지는 않는다 ★★
호출을 하나로 합치는 것이 단순해 보이지만,
① AI 가 뒤쪽 대목을 안 쓰고 ② 한 호출이 가장 길어져 시간 제한에 가장 잘 걸립니다.
**단순하게 만들 곳은 «손님이 보는 것» 이고, 쪼갤 곳은 «AI 에게 시키는 것» 이다.**
화면은 한 흐름, 호출은 잘게 — 둘은 서로 어긋나지 않는다.
