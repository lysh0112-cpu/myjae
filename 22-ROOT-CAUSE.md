# 🎯 원인 — 합격운이 «제 출력을 스스로 막고» 있었습니다 (2026-07-30, 7차)

```
기준 커밋   8886685  (2026-07-30 14:42)
고친 파일   4개  ·  +55 / −25
```

---

## 1. 왜 진로적성은 되고 합격운은 안 됐나

두 화면이 같은 API(`/api/tongbyeon`)를 씁니다. 보내는 것만 달랐습니다.

```
진로적성   body: { systemPrompt, userPrompt, premium: true }
           career-result/page.tsx:213                        ← maxTokens 를 «안» 보냄

합격운     body: { systemPrompt, premium: true, maxTokens: 1600 }
                                                            ← 스스로를 막음
```

라우트가 이렇게 정합니다.

```ts
// app/api/tongbyeon/route.ts:66
max_tokens: maxTokens > 0 ? Math.min(maxTokens, 16000) : (premium ? 16000 : 3500)
```

```
진로적성   상한 16,000 토큰  →  섹션 여섯 개를 한 호출에 다 씀     ✅
합격운     상한  1,600 토큰  →  한 갈래 쓰면 끝. 다음 갈래는 잘림   ❌
           (묶음 셋일 때는 2,400 / 2,400 / 3,600)
```

**한 갈래가 우리말 700~800자입니다. 1,000 토큰이 넘습니다.**
묶음당 2,400 이면 첫 갈래는 온전하고 **두 번째 갈래가 상한에 걸려 잘립니다.**

★그래서 화면에 **1, 3, 5** 만 남았습니다.
AI 가 지시를 «안 따른» 것이 아니라 출력이 «잘린» 것이었습니다.

---

## 2. 35부 교훈 둘이 서로 어긋나 있었습니다

```
교훈 CV  「여섯 대목 한 번 → 두셋만 씀. 일을 나눠라」
         ← 상한이 좁은 채로 재서 «잘림» 을 «AI 가 안 씀» 으로 읽은 것
교훈 CX  「max_tokens 를 크게 잡으면 느려진다. 좁혀라」
         ← 그래서 더 조여 «잘림» 을 굳혀 놓은 것
```

저는 이 두 문장을 믿고 그 위에 묶음 구조를 쌓았습니다.
**증거는 처음부터 저장소 안에 있었습니다** —
`lib/saju/premium/buildCareerMbtiPrompt.ts` 는 섹션 **여섯 개** 를 한 호출로 씁니다.
그 화면은 하루 종일 잘 됐습니다. 제가 그 하나를 세어 보지 않았습니다.

---

## 3. 고친 것 — 진로적성과 «같은 방식» 으로

```
전   호출 7번 · 갈래 하나씩 · 상한 1,600      →  2~3분 · 갈래가 잘림
후   호출 1번 · 갈래 일곱 전부 · 상한 없음     →  진로적성과 같음
```

```ts
// 이제 이렇게 보냅니다 — 진로적성과 한 글자도 다르지 않습니다
body: JSON.stringify({ systemPrompt: sp, premium: true })
```

```
프롬프트   7,747자 (재료·지시·뼈대 전부 · 일곱 갈래)
출력 상한  라우트 기본 16,000 토큰
예상       한 호출 · 60~90초 · 글이 처음부터 끝까지 한 흐름으로 흐릅니다
```

⚠️⚠️ **`maxTokens` 를 다시 넣지 마십시오.** 넣는 순간 갈래가 중간에서 끊깁니다.
   주석에 이 이야기를 그대로 적어 두었습니다.
   **길어서 느리면 상한이 아니라 `SEVEN` 표의 `len`(분량 지시)을 줄이십시오.**
   상한으로 자르면 문장 중간에서 끊기고, 분량 지시로 줄이면 말이 온전합니다.

### 함께 고친 것
`MATERIAL_NEEDS` 를 «합집합» 으로 씁니다. 한 호출에 갈래가 여럿이므로
그 갈래들이 필요한 카드를 전부 실어야 합니다.
⚠️ 전에 `plan[0]` 것만 쓰면 뒤 갈래들이 재료 없이 글을 씁니다(지어냅니다).

---

## 4. 적용

```bash
cd myjae
git pull                                  # 8886685 인지 확인
unzip -o 22-single-call-root-cause.zip
npm run verify:all && npm run build
git add -A && git commit -m "합격운 — maxTokens 제거(원인) · 한 호출로 통합"
git push origin main
```

`8886685` 을 새로 클론해 붙이고 확인했습니다.
```
충돌 0 ✅
적용 뒤 body: { systemPrompt: sp, premium: true }   ← maxTokens 없음, 눈으로 확인 ✅
tsc / strict  0건 / 0건 ✅
verify:seven  전부 통과 (묶음 1개 · 갈래 7개 · 겹침 0 · 빠짐 0) ✅
verify:live   발행 직전까지 전부 통과 ✅
verify:daeun  18/18 · 격국 19/19 ✅
eslint        4 error / 16 warning — ★기준선과 동일
```

---

## 5. 그대로 남겨 둔 것 (되돌리기 쉽게)

```
SEVEN_GROUPS      이름과 얼개 그대로. 지금은 «한 묶음에 일곱» 입니다.
                  다시 쪼개고 싶으면 이 배열만 나누면 됩니다.
CONCURRENCY = 1   묶음이 하나뿐이니 지금은 뜻이 없습니다. 쪼갤 때를 위해 남겼습니다.
CALL_MAX_TOKENS   ★쓰지 않습니다. «왜 쓰면 안 되는지» 만 주석으로 남겼습니다.
일곱 자리 표시     그대로. 한 호출이라도 글이 오는 순서대로 채워집니다.
🔧 개발용 상자     그대로. 호출 1/1 로 뜹니다.
```

---

## 6. 이번에 배운 것 — 오늘 가장 값진 것

### 교훈 DS — «잘림» 과 «안 씀» 은 화면에서 똑같이 보인다 ★★★
출력 상한에 걸려 잘린 것과 AI 가 지시를 안 따른 것은 화면상 구별이 안 됩니다.
35부는 그것을 «AI 가 뒤 대목을 안 쓴다» 로 읽고 프롬프트를 네 번 다듬었습니다.
프롬프트로는 절대 안 고쳐집니다. 상한 문제였으니까요.
**뒤가 안 나올 때는 프롬프트를 의심하기 전에 max_tokens 를 먼저 세라.**
한 대목의 «글자수 × 대목 수» 가 상한을 넘는지 계산하면 1분입니다.

### 교훈 DT — «잘 되는 형제» 가 있으면 보내는 값을 한 줄씩 대조하라 ★★★
진로적성과 합격운은 같은 API 를 씁니다. 다른 것은 `maxTokens` 한 줄이었습니다.
저는 스트리밍 수신부는 대조했지만(그래서 setTong 을 찾았지만)
**보내는 body 는 대조하지 않았습니다.**
**받는 쪽만 보지 말고 보내는 쪽도 나란히 놓으라.** diff 한 번이면 됐습니다.

### 교훈 DU — 인수인계서의 두 교훈이 서로 어긋날 수 있다 ★★★
CV(나눠라)와 CX(상한을 좁혀라)는 각각 맞아 보였지만, 둘을 함께 지키면
«좁은 상한으로 나눠 부르기» 가 되어 잘림이 구조로 굳습니다.
**교훈을 둘 이상 함께 적용할 때는 «합쳐 놓고» 다시 재라.**
그리고 오늘 새로 적은 이 교훈들도 그렇게 대해 주십시오.
