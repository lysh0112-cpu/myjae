// lib/saju/examLuck/buildExamSeven.ts
//
// ┌──────────────────────────────────────────────────────────────────┐
// │  합격운 — 7대 카테고리 · ★진학(학생) / 취업(성인) 두 벌               │
// │  2026-07-29 신설 · 2026-07-30 지시서 반영하며 두 벌로 가름           │
// └──────────────────────────────────────────────────────────────────┘
//
// ── ⚠️ 왜 «세 번 나눠» 부르는가 (교훈 CV — 지우지 마십시오) ────────────
//   한 번에 여러 대목을 시키면 AI 가 앞의 두셋만 길게 쓰고 뒤를 안 씁니다.
//   실제로 겪은 것을 그대로 적습니다.
//       여섯 대목 한 번  →  두셋만 씀
//       세 대목 한 번    →  첫 하나만 씀
//   프롬프트를 네 번 다듬어도 같은 자리에서 실패했습니다.
//   ★대목 수가 문제가 아니라 «여러 대목을 한 번에» 가 문제입니다.
//   그래서 일곱을 «2+2+3» 으로 나눕니다. 한 묶음이 둘셋이면 다 씁니다.
//
// ── ⚠️ 마크다운 헤더(###)가 아니라 ■ 를 씁니다 ────────────────────────
//   지시서는 «### 마크다운 헤더» 를 요구하지만, 이 저장소의 파서
//   (buildExamPrompt.parseExamTongbyeon) 는 `■ 제목` 을 기준으로 자릅니다.
//   ### 로 바꾸면 ①파서 ②보관함에 이미 저장된 옛 통변이 **둘 다** 깨집니다.
//   ★스트리밍·끊김 안전이라는 «뜻» 은 그대로이고 기호만 ■ 입니다.
//   ⚠️ 굳이 ### 로 가시려면 stripLead·isHeading 을 함께 고치고
//      옛 저장본 마이그레이션을 먼저 준비하십시오.
//   ★대신 지시서의 «이모지 + 번호» 는 제목에 그대로 넣었습니다.
//     화면에 그리는 제목은 SEVEN 표의 것이고, sevenKeyOf 가 이모지·번호를
//     떼고 맞추므로 AI 가 제목을 조금 흘려 써도 잡힙니다.
//
// ── ⚠️ 두 벌로 가른 까닭 (2026-07-30 대표님 지시) ─────────────────────
//   한 벌로 두고 target 으로 낱말만 바꾸던 때, 성인에게 «수시 정시 비율» 을
//   묻고 학생에게 «지원 전략» 을 어른 말로 쓰는 일이 생겼습니다.
//   갈래 «이름과 개수» 자체가 달라야 하는 자리였습니다.
//       진학  공부DNA · 과목 · 수시정시 · 12개월 · D-Day · 목표대학 · 학부모
//       취업  일의결 · 준비순서 · 시험:경력 · 12개월 · D-Day · 지원전략 · 맺음말
//   ★키(key)는 같게 두었습니다. 화면 코드가 갈래마다 갈리지 않게 하려고요.

import type { ExamCard, ExamTarget } from './types'

export interface SevenArgs {
  name: string
  gender: string
  age: number
  target: ExamTarget
  /** 시험 준비인가 일자리인가 — 취업 탭에서만 갈립니다 */
  kind?: 'exam' | 'job'
  /** 판정 카드 — 재료로만 씁니다 */
  cards: ExamCard[]
  saju?: Array<{ pillar: string; stem: string; branch: string }>
  hourUnknown?: boolean
  /** 폼에서 받은 것들 — ★지시서 1장 여섯 변수 */
  studentGrade?: string | null
  gradeBlock?: string | null
  scoreRange?: string | null
  targetMajor?: string | null
  targetType?: string | null
  targetAcademic?: string | null
  examDate?: string | null
  /** 시험 당일 일진·월운·십성 (examDay 가 낸 것) */
  examDayNote?: string | null
  /** ★2026-07-30 — 원국 합격 신호 (passSignal.passSignalBlock) */
  signalBlock?: string | null
  /** ★2026-07-30 — 업상대체 세부 전공 (upsang.upsangBlock) */
  upsangBlock?: string | null
  /** 올해 */
  year: number
}

export interface SevenSection {
  key: SevenKey
  /** 화면에 그리는 제목 — 이모지·번호를 포함합니다 (지시서 3장) */
  title: string
  len: string
}

export type SevenKey = 'dna' | 'subject' | 'ratio' | 'monthly' | 'dday' | 'apply' | 'mentor'

/** ── 진학(학생) 일곱 갈래 — 지시서 3장 그대로 ───────────────────── */
export const SEVEN_STUDENT: SevenSection[] = [
  { key: 'dna', title: '🧬 1. 타고난 공부 DNA와 적성', len: '6~8문장' },
  { key: 'subject', title: '🎯 2. 과목별 유불리와 올인 전략', len: '5~7문장' },
  { key: 'ratio', title: '⚖️ 3. 수시와 정시, 나의 황금 비율', len: '5~7문장' },
  { key: 'monthly', title: '🗓️ 4. 열두 달 마음 페이스메이커', len: '5~7문장' },
  { key: 'dday', title: '✍️ 5. D-Day 시험 당일 실전 수칙', len: '6~8문장' },
  { key: 'apply', title: '🎓 6. 목표 대학 지원과 눈치싸움 전략', len: '5~7문장' },
  { key: 'mentor', title: '💌 7. 수험생과 부모님께 드리는 말', len: '6~8문장' },
]

/** ── 취업(성인) 일곱 갈래 ────────────────────────────────────────── */
export const SEVEN_ADULT: SevenSection[] = [
  { key: 'dna', title: '🧬 1. 타고난 일의 결과 강점 분야', len: '6~8문장' },
  { key: 'subject', title: '🎯 2. 무엇을 먼저 할까 — 준비 순서', len: '5~7문장' },
  { key: 'ratio', title: '⚖️ 3. 시험 준비와 실무 경력, 나의 황금 비율', len: '5~7문장' },
  { key: 'monthly', title: '🗓️ 4. 열두 달 페이스메이커', len: '5~7문장' },
  { key: 'dday', title: '✍️ 5. D-Day 당일 실전 수칙', len: '6~8문장' },
  { key: 'apply', title: '🎓 6. 어디를 노릴까 — 지원 전략', len: '5~7문장' },
  { key: 'mentor', title: '💌 7. 마지막으로 드리고 싶은 말', len: '6~8문장' },
]

/** target 에 맞는 갈래 표 */
export function sevenOf(target: ExamTarget): SevenSection[] {
  return target === 'student' ? SEVEN_STUDENT : SEVEN_ADULT
}

/**
 * 호출 차례 — ★갈래 하나에 호출 하나 (1 : 1).
 *
 * ── ⚠️⚠️ 왜 «묶지 않는가» (2026-07-30 대표님 지시) ──────────────────
 *   인수인계서 교훈 CV 에는 이렇게 적혀 있었습니다.
 *       「대목 수가 아니라 «한 번에» 가 문제다. 일을 나눠라. (묶음 둘셋이면 다 쓴다)」
 *   ★뒤 괄호가 틀렸습니다. 저는 그 문장을 믿고 재보지 않았습니다.
 *
 *   실기기에서 드러난 것 —
 *       묶음 [1,2] [3,4] [5,6,7] 로 부르니 뜬 갈래가 **1, 3, 5** 였습니다.
 *       ★묶음마다 «첫 갈래만» 쓰고 끝냈습니다.
 *       둘로 줄여도 마찬가지였습니다. «둘셋이면 다 쓴다» 는 사실이 아닙니다.
 *
 *   그래서 하나에 하나입니다. 이러면 —
 *       ① AI 가 «빼먹을 갈래» 가 없습니다. 쓸 것이 하나뿐입니다
 *       ② 한 호출이 짧아(1,600 토큰) Vercel 시간 제한에 걸릴 여지가 없습니다
 *       ③ 하나가 실패해도 그 갈래만 빕니다. 나머지는 멀쩡합니다
 *       ④ 관리가 쉽습니다 — 갈래 하나 = 프롬프트 하나 = 호출 하나
 *
 * ── ⚠️ 대신 치르는 값 ────────────────────────────────────────────
 *   재료(판정 카드 ~2,900자)를 일곱 번 되풀이해 보냅니다.
 *   입력 토큰이 묶음 넷일 때보다 대략 두 배입니다.
 *   ★줄이려면 갈래마다 «그 갈래에 필요한 재료만» 골라 보내면 됩니다.
 *     (D-Day 는 일진만, 열두 달은 월운만 — 3-x 할 일에 적었습니다)
 *
 * ⚠️ 이름을 SEVEN_GROUPS 로 그대로 두었습니다. 화면과 검사기가 이 이름을 부릅니다.
 *    «묶음» 이라는 말이 남아 있지만 이제 한 묶음에 하나뿐입니다.
 */
export const SEVEN_GROUPS: SevenKey[][] = [
  // ══════════════════════════════════════════════════════════════
  //  ★★★2026-07-30 (9차) — 갈래 1:1 로 되돌립니다. **단, 상한은 계속 안 보냅니다.**
  //
  //   [오늘 하루 무엇을 헤맸나 — 다음 분은 이 표만 보십시오]
  //
  //       차수    얼개                maxTokens   결과
  //       6차     갈래 1:1 (7 호출)   1,600 보냄  갈래마다 «중간에서» 잘림
  //       7·8차   한 호출에 일곱      안 보냄     호출이 길어 «시간» 에 걸림
  //       9차     갈래 1:1 (7 호출)   안 보냄     ← 이것
  //
  //     ★두 고침을 «함께» 해 본 적이 한 번도 없었습니다.
  //       6차에서 1:1 이 실패한 것은 1:1 탓이 아니라 1,600 탓이었습니다.
  //       그런데 저는 «1:1 이 틀렸다» 고 읽고 얼개를 되돌려 버렸습니다.
  //       7차에서 상한을 없앤 것은 맞았지만, 그때 얼개까지 뒤집을 이유가 없었습니다.
  //
  //   [왜 «한 호출에 일곱» 은 안 되나]
  //     일곱 갈래 = 우리말 5,000~6,000자. 한 호출이 70~140초를 붙잡습니다.
  //     그 사이 어디서든 끊기면(함수 시간 제한·망) **뒤 갈래가 통째로** 사라집니다.
  //     실기기에서 1번만 앉고 2번이 «쓰고 있어요» 로 굳은 캡처가 그 모습입니다.
  //     ⚠️ Vercel 은 `maxDuration = 300` 을 적어도 «요금제 상한» 까지만 줍니다.
  //        Hobby 는 60초입니다. 60초는 우리말 1~2갈래 분량입니다. 정확히 들어맞습니다.
  //
  //   [왜 1:1 이면 세 위험이 한꺼번에 사라지나]
  //     ① 빼먹을 수 없음 — 쓸 것이 하나뿐입니다. 교훈 CV(여러 대목 → 앞 것만)가 안 걸립니다
  //     ② 잘릴 수 없음  — 한 갈래 800자 ≒ 1,200 토큰. 상한은 16,000 입니다
  //     ③ 시간에 걸릴 수 없음 — 한 호출 20~30초. 어떤 요금제에서도 넉넉합니다
  //     ④ 하나가 실패해도 그 갈래만 빕니다. 리포트가 통째로 죽지 않습니다
  //
  //   ⚠️⚠️ **maxTokens 를 다시 보내지 마십시오.** 6차의 잘림이 그대로 돌아옵니다.
  //         ExamResultShell 의 fetch 본문에 maxTokens 가 없는지 보십시오.
  //   ⚠️ 대신 치르는 값 — 재료(~2,900자)를 일곱 번 되풀이해 보냅니다.
  //      입력 토큰이 늘지만, 입력은 출력보다 훨씬 싸고 훨씬 빠릅니다.
  // ══════════════════════════════════════════════════════════════
  ['dna'],
  ['subject'],
  ['ratio'],
  ['monthly'],
  ['dday'],
  ['apply'],
  ['mentor'],
]

/**
 * ⚠️⚠️ **이 값을 쓰지 마십시오.** 남겨 둔 까닭만 적습니다.
 *
 *   합격운이 이 값을 /api/tongbyeon 에 보내고 있었고, 그것이
 *   「갈래가 중간에 잘린다」 의 원인이었습니다. 지금은 **안 보냅니다.**
 *   보내지 않으면 라우트가 premium 기본값 16,000 을 씁니다(진로적성과 같음).
 *
 *   ★출력이 길어 느리다고 느껴지면 이 값을 되살리지 말고
 *     프롬프트의 «분량 지시»(SEVEN 표의 len)를 줄이십시오.
 *     상한으로 자르면 문장 중간에서 끊기고, 분량 지시로 줄이면 말이 온전합니다.
 */
export const CALL_MAX_TOKENS = 1600

// ══════════════════════════════════════════════════════════════════
//  ★지시서 2-A-1 «현실적 가중치 법칙» — 성적·환경 60 : 사주 40
// ══════════════════════════════════════════════════════════════════
//
//   [왜 이 규칙이 필요한가]
//     사주만으로 합불을 말하면 두 가지가 다 나쁩니다.
//       ① 좋게 말하면 손님이 공부를 놓습니다.
//       ② 나쁘게 말하면 준비하는 사람의 마음을 꺾습니다.
//     대표님이 «성적·환경 60 : 사주 40» 으로 못박으신 자리입니다.
//   ⚠️ 이 비율은 «점수를 매기는 식» 이 아니라 «말하는 무게» 입니다.
//      숫자를 손님에게 보여 주지 말라고 아래에 함께 적었습니다.
const WEIGHT_RULE = `[가장 중요 — 말하는 무게]
· 지금의 성적과 환경이 6, 타고난 흐름이 4 입니다. 사주가 성적을 뒤집는다고 쓰지 마세요.
· 흐름은 «준비된 사람이 어느 때에 더 크게 받는가» 를 말해 줄 뿐입니다.
· ★«붙는다·떨어진다» 로 단정하지 마세요. 「이 해의 기운은 이런 쪽에 힘이 붙습니다」 로 쓰세요.
· ⚠️ 「60:40」 「6:4」 같은 숫자를 손님에게 보여 주지 마세요. 우리끼리 쓰는 잣대입니다.`

const TONE = `[말투]
· 존댓말로 다정하되 담담하게. 겁주지 마세요.
· "불합격"·"떨어진다"·"안 된다" 를 쓰지 마세요.
· 어려운 한자말은 풀어 쓰세요. (관성 → 자리의 기운, 인성 → 배움의 기운)
· 재료의 점수·등급 표기를 그대로 옮기지 말고 사람 말로 푸세요.
· 마크다운(#, **, ---)을 쓰지 마세요. 제목은 ■ 로만 씁니다.`

/** 학생에게 쓰면 안 되는 말 — ★예시로 쓰지 마십시오. 예시로 넣으면 AI 가 끌어 씁니다 */
const STUDENT_BAN = '이직, 취업, 취준, 직장, 회사, 공무원, 사업, 승진, 전직, 팀 단위 근무, 조리사, 영양사, 로스쿨'

// ══════════════════════════════════════════════════════════════════
//  갈래마다 필요한 판정 카드
// ══════════════════════════════════════════════════════════════════
//
// ⚠️ buildCards.ts 가 만드는 카드 열쇠입니다. 새 카드를 만들면 여기에도 넣으십시오.
//    넣지 않으면 «알 수 없는 열쇠» 로 보아 모든 갈래에 실립니다(안전한 쪽).
export const ALL_CARD_KEYS = [
  'years', 'dayun', 'examkind', 'examday', 'susi', 'highschool', 'jobchange',
]

/**
 * 갈래 → 실을 카드 열쇠.
 *
 * ⚠️ «없으면 안 되는 것» 만 넣었습니다. 넉넉히 넣으면 프롬프트가 다시 커집니다.
 *    ★어느 갈래의 글이 얄팍하면 그 줄에 카드를 하나 더해 보십시오.
 *      고칠 곳이 여기 한 줄입니다.
 */
const MATERIAL_NEEDS: Record<SevenKey, string[]> = {
  // 공부 결 — 원국과 지금 대운이면 됩니다. 연도별 흐름은 필요 없습니다.
  dna: ['dayun', 'examkind'],
  // 과목 — 그해 세운이 핵심입니다.
  subject: ['years', 'examkind', 'dayun'],
  // 수시:정시 비율 — 전형 판정과 세운.
  ratio: ['susi', 'highschool', 'years'],
  // 열두 달 — 세운과 그달 흐름.
  monthly: ['years', 'examday'],
  // D-Day — 그날 일진이 알맹이입니다.
  dday: ['examday', 'years'],
  // 지원 전략 — 올해가 밀 때인지 지킬 때인지.
  apply: ['years', 'susi', 'jobchange'],
  // 맺음말 — 흐름의 큰 결만. ★교재 맺음말이 years 카드 reasons 에 실려 있어 꼭 필요합니다.
  mentor: ['years', 'dayun'],
}

// ══════════════════════════════════════════════════════════════════
//  갈래마다 «그 자리에서» 읽을 지시 (교훈 CW)
//    ★멀리 있는 훈계보다 곁에 있는 한 줄이 셉니다.
// ══════════════════════════════════════════════════════════════════

function hintStudent(key: SevenKey, v: SevenArgs): string[] {
  const L: string[] = []
  const major = v.targetMajor ? `«${v.targetMajor}»` : ''
  switch (key) {
    case 'dna':
      L.push('원국의 십성으로 «어떻게 배우는 사람인가» 를 먼저 한 가지로 정하세요.')
      L.push('· 이해로 뚫는 결(편인·인성) / 여러 번 돌려 쌓는 결(정인·비겁) / 문제를 풀며 익히는 결(식상·재성)')
      L.push('  셋 가운데 어디에 가까운지 정하고, 왜 그런지 원국으로 밝히세요.')
      L.push('· 재료의 [학업 몰입도] 등급을 그대로 옮기지 말고, 그 뜻을 공부 습관 이야기로 푸세요.')
      if (v.upsangBlock) {
        L.push(`· ★그다음이 알맹이입니다. 아래 [세부 적성] 재료를 써서 ${major || '희망 계열'} 안에서`)
        L.push('  «어느 세부 자리» 가 이 학생에게 극대화되는지 이름을 대어 짚으세요.')
        L.push('  계열 이름만 되풀이하면 안 됩니다. 세부까지 내려가야 리포트가 값을 합니다.')
        L.push('· 그 근거(오행·신살)를 쉬운 말로 한 문장 붙이세요. 한자만 나열하지 마세요.')
      } else if (major) {
        L.push(`· 그 결이 ${major} 계열과 어떻게 맞물리는지 이어 주세요.`)
      }
      break
    case 'subject':
      L.push(`${v.year}년 세운의 기운을 보고 «지금 가장 빨리 오를 과목» 을 짚으세요.`)
      if (v.scoreRange) L.push(`· 지금 성적대(${v.scoreRange})에서 올릴 수 있는 만큼으로 말하세요. 뜬구름은 안 됩니다.`)
      L.push('· 과목 이름을 구체적으로 드세요. 그리고 «공부 비중을 어떻게 나눌지» 까지.')
      L.push('· 반대로 «지금 손대면 시간만 쓰는 과목» 도 하나 짚어 주세요.')
      L.push('· 재료의 [유리한 자리]·[주의할 자리] 가운데 과목과 이어지는 것을 근거로 대세요.')
      L.push('· 오행이 한쪽으로 몰려 있으면, 비어 있는 결을 하루에 조금 섞는 습관을 함께 주세요.')
      break
    case 'ratio':
      L.push('원국의 관성(틀을 지키는 힘)과 인성(쌓는 힘) 비율로 «수시 몇 : 정시 몇» 을 숫자로 내세요.')
      L.push('· ★보기) 「수시 70 : 정시 30 으로 봅니다」 처럼 눈에 보이게. 반드시 숫자를 쓰세요.')
      L.push('· 왜 그 비율인지 원국으로 밝히세요. 숫자만 던지면 점집 말이 됩니다.')
      L.push('· 관인상생이 재료에 있으면 그것을 이 비율의 근거로 삼으세요.')
      if (v.scoreRange) L.push(`· ⚠️ 지금 성적대(${v.scoreRange})가 먼저입니다. 사주가 성적을 뒤집는다고 쓰지 마세요.`)
      if (v.targetType) L.push(`· 손님은 ${v.targetType}를 목표로 하십니다. 그 길에서 여섯 장을 어떻게 안배할지 주세요.`)
      break
    case 'monthly':
      L.push('열두 달 가운데 «가장 잘 붙는 달(골든존)» 하나와 «흔들리기 쉬운 달(위험존)» 하나를 콕 집으세요.')
      L.push('· ★몇 월인지 숫자로 말하세요. 「봄쯤」 같은 말은 도움이 안 됩니다.')
      L.push('· 골든존에는 «무엇을 몰아서 할지», 위험존에는 «어떻게 버틸지» 를 주세요.')
      L.push('· 재료의 달별 흐름을 보고 정하세요. 없는 달을 지어내지 마세요.')
      L.push('· 배움의 기운·꺼내는 기운이 드는 달은 몰입이 붙는 달, 마음이 밖으로 끌리는 기운이')
      L.push('  드는 달은 흔들리는 달로 봅니다. 그 결을 근거로 대세요.')
      break
    case 'dday':
      if (v.examDate) L.push(`${v.examDate} 그날의 일진 기운을 먼저 한 문장으로.`)
      L.push('· 재료의 «일진 — 천간 ○○ · 지지 ○○» 를 보고 그 십성이 이 학생에게 어떻게 작용하는지.')
      L.push('· ★그다음이 알맹이입니다. 넷을 «구체적으로» 주세요. 네 가지를 다 채우세요.')
      L.push('   1교시 입실 직후 마음을 어떻게 가라앉힐지 /')
      L.push('   어느 과목에서 실수가 나기 쉬운지와 막는 법 /')
      L.push('   시험 중 흔들릴 때 할 행동 하나 /')
      L.push('   그날 아침을 어떻게 보낼지')
      L.push('· 재료에 상관견관이나 충·형이 있으면 «마킹·조건 확인» 을 짚어 주세요.')
      L.push('· ★간지만 나열하고 「조심입니다」 로 끝내면 안 됩니다.')
      break
    case 'apply':
      if (v.targetAcademic) L.push(`목표는 «${v.targetAcademic}» 입니다. 그 이름을 부르며 시작하세요.`)
      L.push(`· ${v.year}년 운이 «밀어 볼 때» 인지 «지켜 낼 때» 인지 분명히 말하세요.`)
      L.push('· ★상향·소신·안정을 몇 대 몇으로 둘지 숫자로 주세요. (보기 — 상향 2 : 소신 2 : 안정 2)')
      if (v.scoreRange) L.push(`· ⚠️ 지금 성적대(${v.scoreRange})가 지원의 바탕입니다. 흐름은 그 위에 얹는 것입니다.`)
      L.push('· ⚠️ «붙는다·떨어진다» 로 단정하지 마세요.')
      break
    case 'mentor':
      L.push('사주 이야기를 잠시 내려놓고, 사람 대 사람으로 맺으세요.')
      if (v.studentGrade) L.push(`· 지금 신분(${v.studentGrade})의 무게를 알아주는 말로 시작하세요.`)
      L.push('· 실력이 6이고 흐름이 4라는 것을 «숫자 없이» 담담하게 전하세요.')
      L.push('· ★부모님도 함께 읽습니다. 부모님께 드리는 말을 두세 문장 따로 넣어 주세요.')
      L.push('· ⚠️ 「더 일찍 시작했으면」·「작년에는」 같은 말을 쓰지 마세요.')
      L.push('· 끝은 응원으로. 결과를 점치지 말고, 남은 시간을 어떻게 쓸지로 맺으세요.')
      break
  }
  return L
}

function hintAdult(key: SevenKey, v: SevenArgs): string[] {
  const L: string[] = []
  const isJob = v.kind === 'job'
  const goal = v.targetAcademic || v.targetMajor || ''
  switch (key) {
    case 'dna':
      L.push('원국의 십성으로 «어떻게 일하고 어떻게 익히는 사람인가» 를 먼저 한 가지로 정하세요.')
      L.push('· 파고들어 구조를 잡는 결(편인·인성) / 쌓아 두고 꺼내 쓰는 결(정인) /')
      L.push('  부딪히며 익히는 결(식상·재성) / 틀을 세우고 지키는 결(관성)')
      L.push('· 재료의 [주로 보는 기운] 이 자리의 기운(관성)임을 잊지 마세요. 이 분야에서 가장 먼저 보는 자리입니다.')
      if (v.upsangBlock) {
        L.push('· ★아래 [세부 적성] 재료를 써서 «어느 세부 분야» 가 극대화되는지 이름을 대어 짚으세요.')
        L.push('  큰 갈래 이름만 되풀이하면 안 됩니다.')
        L.push('· 그 근거(오행·신살)를 쉬운 말로 한 문장 붙이세요.')
      }
      break
    case 'subject':
      L.push(`${v.year}년 세운의 기운을 보고 «지금 무엇을 먼저 해야 하는가» 를 순서로 주세요.`)
      L.push(isJob
        ? '· 서류·직무 경험·자격 가운데 지금 손대면 가장 빨리 값이 붙는 것을 하나 고르세요.'
        : '· 여러 과목·영역 가운데 지금 손대면 가장 빨리 오를 곳을 하나 고르세요.')
      L.push('· 반대로 «지금 손대면 시간만 쓰는 것» 도 하나 짚어 주세요.')
      L.push('· 재료의 [유리한 자리]·[주의할 자리] 를 근거로 대세요.')
      break
    case 'ratio':
      L.push('원국의 관성(자리를 맡는 힘)과 인성(쌓는 힘) 비율을 숫자로 내세요.')
      L.push(isJob
        ? '· ★보기) 「자리에 바로 부딪히는 쪽 60 : 자격·공부로 쌓는 쪽 40」 처럼 눈에 보이게.'
        : '· ★보기) 「시험 준비 70 : 실무·경력 30 으로 봅니다」 처럼 눈에 보이게.')
      L.push('· 왜 그 비율인지 원국으로 밝히세요. 숫자만 던지면 점집 말이 됩니다.')
      L.push('· 관인상생이 재료에 있으면 그것을 이 비율의 근거로 삼으세요.')
      L.push('· 지금까지 쌓아 온 경력과 나이가 먼저입니다. 흐름은 그 위에 얹는 것입니다.')
      break
    case 'monthly':
      L.push('열두 달 가운데 «가장 잘 붙는 달» 하나와 «흔들리기 쉬운 달» 하나를 콕 집으세요.')
      L.push('· ★몇 월인지 숫자로 말하세요.')
      L.push('· 좋은 달에는 «무엇을 몰아서 할지», 흔들리는 달에는 «어떻게 버틸지» 를 주세요.')
      L.push('· 재료의 달별 흐름을 보고 정하세요. 없는 달을 지어내지 마세요.')
      break
    case 'dday':
      if (v.examDate) L.push(`${v.examDate} 그날의 일진 기운을 먼저 한 문장으로.`)
      L.push('· 재료의 «일진 — 천간 ○○ · 지지 ○○» 를 보고 그 십성이 어떻게 작용하는지.')
      L.push('· ★그다음이 알맹이입니다. 넷을 «구체적으로» 주세요.')
      L.push(isJob
        ? '   면접장에 들어서기 직전 마음을 어떻게 가라앉힐지 / 어느 질문에서 말이 길어지기 쉬운지와 막는 법 /'
        : '   시작 직후 마음을 어떻게 가라앉힐지 / 어느 영역에서 실수가 나기 쉬운지와 막는 법 /')
      L.push('   흔들릴 때 할 행동 하나 / 그날 아침을 어떻게 보낼지')
      L.push('· 재료에 상관견관이나 충·형이 있으면 «말이 앞서는 것» 을 짚어 주세요.')
      L.push('· ★간지만 나열하고 「조심입니다」 로 끝내면 안 됩니다.')
      break
    case 'apply':
      if (goal) L.push(`목표는 «${goal}» 입니다. 그 이름을 부르며 시작하세요.`)
      L.push(`· ${v.year}년 운이 «밀어 볼 때» 인지 «지켜 낼 때» 인지 분명히 말하세요.`)
      L.push('· ★상향(더 높은 곳) · 소신 · 안정을 몇 대 몇으로 둘지 숫자로 주세요.')
      L.push(isJob
        ? '· 한 곳만 보지 말고 몇 갈래를 나란히 둘지도 말해 주세요.'
        : '· 올해 한 번으로 볼지, 다음 해까지 두 번으로 볼지도 말해 주세요.')
      L.push('· ⚠️ «붙는다·떨어진다» 로 단정하지 마세요.')
      break
    case 'mentor':
      L.push('사주 이야기를 잠시 내려놓고, 사람 대 사람으로 맺으세요.')
      L.push(`· 만 ${v.age}세로 이 길에 서 계신 무게를 알아주는 말로 시작하세요.`)
      L.push('· 준비한 실력이 6이고 흐름이 4라는 것을 «숫자 없이» 담담하게 전하세요.')
      L.push('· ⚠️ 「더 일찍 시작했으면」·「나이가 있으니」 같은 말을 쓰지 마세요.')
      L.push('· 끝은 응원으로. 결과를 점치지 말고, 남은 시간을 어떻게 쓸지로 맺으세요.')
      break
  }
  return L
}

/** 한 호출에 보낼 두 덩이 — 진로적성(buildCareerMbtiPrompt)과 같은 모양입니다. */
export type SevenPrompt = { system: string; user: string }

/**
 * 한 묶음을 쓰는 프롬프트.
 *
 * ⚠️ 재료를 «먼저», 쓸 대목을 «맨 끝» 에 둡니다. (교훈 CW)
 *    거꾸로 짰다가 AI 가 앞의 지시를 다 잊고 재료를 그대로 옮겨 적었습니다.
 *
 * ★2026-07-30 (8차) — 돌려주는 것이 «문자열» 에서 «{system, user}» 로 바뀌었습니다.
 *   진로적성과 같은 방식입니다. 부르는 쪽은 두 값을 그대로 /api/tongbyeon 에 넘기십시오.
 *   ⚠️ 한 덩이로 다시 합치지 마십시오. 합치면 뼈대가 묻혀 뒤 갈래가 안 나옵니다.
 *
 * @param group SEVEN_GROUPS 의 한 덩이
 */
export function buildSevenPrompt(v: SevenArgs, group: SevenKey[]): SevenPrompt | null {
  const table = sevenOf(v.target)
  const plan = table.filter(s => group.includes(s.key))
  if (!plan.length) return null

  const isStudent = v.target === 'student'
  const hint = isStudent ? hintStudent : hintAdult

  // ★★2026-07-30 — 갈래마다 «그 갈래에 필요한 재료만» 보냅니다.
  //
  //   [왜] 1:1 로 바꾸면서 같은 재료(판정 카드 ~2,900자)를 일곱 번 되풀이해
  //     보내게 됐습니다. 프롬프트가 크면 ①첫 문장이 늦게 오고 ②입력 토큰이 곱해집니다.
  //   [어떻게] 카드 열쇠로 고릅니다. 필요 없는 카드를 안 실을 뿐,
  //     카드 자체는 그대로 계산되고 화면·다른 갈래에서 쓰입니다.
  //   ⚠️ 목록에 없는 카드 열쇠가 새로 생기면 **어느 갈래에도 안 갑니다.**
  //      카드를 새로 만들면 아래 표에 그 열쇠를 넣어 주십시오.
  //      (그래서 알 수 없는 열쇠는 «전부에게» 주는 쪽으로 두었습니다 — 아래 fallback)
  // ★갈래가 여럿이면 그 갈래들이 필요한 카드를 «합집합» 으로 실어야 합니다.
  //   ⚠️ plan[0] 것만 쓰면 뒤 갈래들이 재료 없이 글을 씁니다(지어냅니다).
  const needSet = new Set<string>()
  for (const sec of plan) for (const k of MATERIAL_NEEDS[sec.key] ?? []) needSet.add(k)
  const material = v.cards
    .filter(c => needSet.has(c.key) || !ALL_CARD_KEYS.includes(c.key))
    .map(c => `[${c.title}]\n` + c.reasons.map(r => `- ${r}`).join('\n'))
    .join('\n\n')

  const myeongsik = v.saju
    ?.map(p => `${p.pillar} ${p.stem === '?' ? '·' : p.stem}${p.branch === '?' ? '·' : p.branch}`)
    .join(' · ') ?? ''

  // ★지시서 1장 — 폼 여섯 변수를 빠짐없이 싣습니다.
  //   ⚠️ 값이 없는 줄은 아예 넣지 않습니다. «미입력» 이라 적으면 AI 가 그 말을 손님에게 씁니다.
  const who = [
    `${v.name}님 · 만 ${v.age}세 · ${v.gender}`,
    myeongsik ? `명식 ${myeongsik}` : '',
    v.studentGrade ? `· 학년·신분: ${v.studentGrade}` : '',
    v.scoreRange ? `· 지금 성적대: ${v.scoreRange}` : '',
    v.targetMajor ? `· 희망 계열: ${v.targetMajor}` : '',
    v.targetType ? `· 전형 목표: ${v.targetType}` : '',
    v.targetAcademic ? `· 목표: ${v.targetAcademic}` : '',
    v.examDate ? `· 시험(발표) 날짜: ${v.examDate}` : '',
    v.examDayNote ? `· 그날 기운: ${v.examDayNote}` : '',
    v.hourUnknown ? '★태어난 시(時)를 모릅니다. 시주가 필요한 이야기는 단정하지 마세요.' : '',
  ].filter(Boolean).join('\n')

  const role = isStudent
    ? '당신은 입시를 오래 봐 온 사주 상담가입니다.'
    : '당신은 시험과 일자리를 오래 봐 온 사주 상담가입니다.'

  // ══════════════════════════════════════════════════════════════
  //  ★★2026-07-30 (8차) — system(지시) 과 user(재료·뼈대) 로 «나눠» 보냅니다.
  //
  //   [7차까지 무엇이 남아 있었나]
  //     7차가 maxTokens 를 걷어내며 «진로적성과 한 글자도 다르지 않다» 고 적었지만
  //     사실은 한 가지가 더 달랐습니다 —
  //
  //       진로적성  system = 역할·말투(짧게)   user = 재료 + 뼈대(맨 끝)
  //                 buildCareerMbtiPrompt.ts:316~333  ★섹션 여섯을 한 호출에 다 씁니다
  //       합격운    system = 전부 한 덩이       user = "위 안내에 따라 통변을 작성해 주세요."
  //                 → 모델이 «마지막에 본 것» 이 그 한 줄짜리 부탁이었습니다
  //
  //   [왜 이것이 갈래를 잡아먹나] 교훈 CW — AI 는 마지막에 본 것을 씁니다.
  //     뼈대(쓸 갈래 목록)가 system 한복판에 묻혀 있으면 그 힘이 약해집니다.
  //     진로적성은 뼈대를 **user 의 맨 끝** 에 둡니다. 그래서 여섯을 다 씁니다.
  //   ⚠️ 그래서 순서를 이렇게 고정했습니다 — 재료 먼저, 뼈대 맨 끝.
  //      뼈대 뒤에 재료나 훈계를 다시 붙이지 마십시오. 35부가 그렇게 해서 실패했습니다.
  //   ⚠️ 낱말은 한 줄도 새로 쓰지 않았습니다. 있던 문장을 두 덩이로 나눈 것뿐입니다.
  // ══════════════════════════════════════════════════════════════

  const system = `${role}
${TONE}

${WEIGHT_RULE}
${isStudent ? `
★이 손님은 학생입니다. 아래 낱말을 한 번도 쓰지 마세요.
    ${STUDENT_BAN}
${v.gradeBlock ? v.gradeBlock : ''}` : ''}

[형식 — 반드시 지키세요]
· ■ 로 시작하는 제목 줄은 이모지와 번호까지 **한 글자도 바꾸지 마세요.**
· 갈래를 새로 지어내거나, 더하거나, 빼지 마세요.
· 괄호 ( ) 는 채워 넣으라는 뜻입니다. 괄호 자체는 답에 쓰지 마세요.
· ★[한줄]·[태그]·[실천] 은 대괄호까지 그대로 적으세요. 화면이 그것으로 갈라 그립니다.
· ★모든 갈래의 마지막 문장을 반드시 마침표로 맺으세요. 중간에서 끊지 마세요.
· 여는말·맺는말을 따로 쓰지 마세요. 구분선(---)도 쓰지 마세요.`

  const user = `[누구를 보는가]
${who}

[판정 재료 — 이것만 근거로 쓰세요. 없는 것을 지어내지 마세요]
${material}
${v.signalBlock ? `\n[합격 신호 — 원국을 본 것]\n${v.signalBlock}` : ''}
${v.upsangBlock ? `\n[세부 적성 — ★계열 안에서 «어느 자리» 가 극대화되는가]\n${v.upsangBlock}` : ''}

════════════════════════════════════════
[답변 형식 — ${plan.length}장의 카드]
★아래 «뼈대» 를 그대로 옮겨 쓰고 괄호 안만 채우세요.
  카드는 정확히 ${plan.length}개입니다. ①번부터 ${plan.length}번까지 «빠짐없이 순서대로» 쓰세요.
  ⚠️ 중간에서 멈추지 마세요. 마지막 갈래까지 다 쓴 뒤에 끝내세요.
════════════════════════════════════════

${plan.map(s => `■ ${s.title}
[한줄] (가장 하고 싶은 말 한 문장. 스무 자 안팎)
[태그] (낱말 · 낱말 · 낱말 — 두셋, 쉬운 말로)

(본문 ${s.len}. 단락마다 빈 줄로 나눠 쓰세요.)
${hint(s.key, v).map(x => '  · ' + x).join('\n')}

[실천] (지금 바로 해볼 수 있는 일 한 문장)`).join('\n\n')}

════════════════════════════════════════
· 위 ${plan.length}개 갈래를 «전부» 쓰고 끝내세요.`

  return { system, user }
}

// ══════════════════════════════════════════════════════════════════
//  제목 → 갈래 열쇠
// ══════════════════════════════════════════════════════════════════
//
// ⚠️ AI 가 제목을 조금 바꿔 써도 잡아야 합니다. (교훈 CY — 자리가 아니라 제목으로)
//   ★2026-07-30 — 제목에 이모지와 번호가 붙었으므로 그것을 먼저 떼어 냅니다.
//     안 떼면 「🧬 1. 타고난…」 과 「타고난…」 이 다른 것으로 잡혀
//     갈래가 통째로 안 그려집니다.

/** ■ · 이모지 · 번호 · 공백을 떼고 알맹이만 남긴다 */
function bareTitle(title: string): string {
  return title
    .replace(/^■\s*/, '')
    // 이모지·기호 — 한글·한자·영숫자가 아닌 앞머리를 떼어 냅니다
    .replace(/^[^\p{Script=Hangul}\p{Script=Han}A-Za-z0-9]+/u, '')
    .replace(/^\d+\s*[.)·]?\s*/, '')
    .replace(/\s/g, '')
    .trim()
}

const SEVEN_HINTS: Array<[SevenKey, string[]]> = [
  ['dday', ['D-Day', 'DDay', '디데이', '시험 당일', '당일 실전', '실전 수칙']],
  ['dna', ['공부 DNA', '공부 디엔에이', '타고난 공부', '학습 성향', '공부 결', '일의 결', '강점 분야', '강점 직무']],
  ['subject', ['과목', '유불리', '올인', '전략 과목', '준비 순서', '무엇을 먼저']],
  ['ratio', ['수시', '정시', '황금 비율', '비율', '실무 경력']],
  ['monthly', ['열두 달', '12개월', '페이스', '달별', '월별']],
  ['apply', ['지원 전략', '목표 대학', '눈치', '상향', '소신', '어디를 노릴']],
  ['mentor', ['부모님', '학부모', '멘토', '최종', '마지막', '총평']],
]

/**
 * 제목에서 갈래 열쇠를 읽는다.
 * @param target 넘기면 그 벌의 제목을 먼저 맞춰 봅니다. 안 넘기면 두 벌 다 봅니다.
 */
export function sevenKeyOf(title: string, target?: ExamTarget): SevenKey | null {
  const t = bareTitle(title)
  if (!t) return null
  const tables = target ? [sevenOf(target)] : [SEVEN_STUDENT, SEVEN_ADULT]
  for (const tb of tables) {
    for (const s of tb) {
      const b = bareTitle(s.title)
      if (b && (t.startsWith(b) || b.startsWith(t))) return s.key
    }
  }
  for (const [key, words] of SEVEN_HINTS) {
    if (words.some(w => t.includes(w.replace(/\s/g, '')))) return key
  }
  return null
}

/** ⚠️ 옛 이름 — 화면 코드가 아직 부르는 곳이 있어 남겨 둡니다. 새 코드는 sevenOf() 를 쓰십시오. */
export const SEVEN = SEVEN_STUDENT
