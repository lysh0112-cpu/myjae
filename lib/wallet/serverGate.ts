// ══════════════════════════════════════════════════════════════════
//  ★2026-09-10 — 지갑 관문 «서버 쪽» 스위치
//
//  [왜 따로 두나]
//    lib/wallet/consultGate.ts 는 ★브라우저용 supabase 를 import 합니다.
//    ⇒ API 길(서버)에서 그 파일을 부르면 ★빌드가 깨집니다.
//    ⇒ 그래서 «값 하나» 만 여기에 따로 둡니다. 로직은 옮기지 않았습니다.
//
//  ⛔⛔ ★consultGate.ts 의 WALLET_GATE_ON 과 «언제나 같은 값» 이어야 합니다.
//     둘이 어긋나면 —
//       화면은 「돈이 빠진다」고 말하는데 서버는 «안 빼거나»
//       화면은 공짜라 해놓고 서버가 ★조용히 빼 갑니다.
//     ⇒ 켜고 끄실 때 ★두 곳을 «함께» 고치십시오.
//
//  ⚠️ 세 앱(명카페·큐보드·골프온)이 ★같은 날 켭니다 [3부 3-1].
//     ⛔ 한쪽만 켜지 마십시오 — 손님이 앱마다 다른 대접을 받습니다.
//
//  □ 나중에 — 이 값을 환경변수로 빼면 두 곳을 안 고쳐도 됩니다.
//     지금은 «눈에 보이는 상수» 가 낫습니다 (배포를 잊으면 더 나쁩니다).
// ══════════════════════════════════════════════════════════════════

/**
 * 🔴 지갑에서 «실제로» 뺄지.
 *   ⚠️ consultGate.ts 와 ★같은 값으로 두십시오 (2026-09-10 현재 시험용으로 켜 둠).
 */
export const WALLET_GATE_ON_SERVER = true

/** 골프온 앱 딱지 — mc_ledger.service 와 짝입니다. ⛔ 바꾸지 마십시오. */
export const SERVICE_GLF = 'glf'
