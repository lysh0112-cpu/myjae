# 봉투 myjae-10bu-2 — ★누적입니다 (10bu-1 을 «대신합니다»)

```
날짜   2026-09-15 (10부)
⛔ ★myjae-10bu-1.zip 은 «버리십시오». 이것 하나만 쓰시면 됩니다.
   (1 은 zip 안에 껍데기 칸이 있어 그대로 풀면 안 덮였습니다. 이번 것은 고쳤습니다)

담긴 것 — ★파일 일곱
  app/naejeong/page.tsx              일진내정법 화면 정리
  56-verify-naejeong.ts              그물 (224 → 248)
  app/home-new/page.tsx              ★전문가용 만세력
  app/components/common/SisterLinks.tsx   ★제목 통일 · 큐보드/골프온 카드
  app/components/HomeBottomNav.tsx   ★선생님 소개 숨김
  57-verify-home-ui.ts               ★새 그물 (통과 37)
  package.json                       검사 57 을 verify 에 묶음
```

## 넣는 법

```bash
cd myjae
unzip -o ../myjae-10bu-2.zip     # ★껍데기 칸이 없습니다. 바로 덮입니다.
git status --porcelain           # ★ M 여섯 줄 + ?? 57-verify-home-ui.ts

npm run verify                   # ★통과 3,383 · 실패 0 · 그물 30개
npx tsc --noEmit                 # ★오류 0
npm run lint                     # ★85 errors · 147 warnings (기준선 그대로)
npm run build                    # ★exit 0 이어야 합니다 (⚠️ 아래)

git add -A
git commit -m "일진내정법 화면 정리 + 홈 마감(전문가용·제목통일·자매카드·하단바)"
git push
```

⚠️ **빌드에 대해 «정확히»** — 제 컨테이너에서는 `npm run build` 를 **끝까지 못 돌렸습니다.**
`fonts.googleapis.com` 이 막혀 있고, **Supabase 열쇠(.env)가 없습니다.**
폰트만 끊고 돌리니 **`✓ Compiled successfully in 41s`** 였고,
남은 실패는 `/_not-found` 프리렌더에서 **Supabase 키가 없다**는 것뿐이었습니다.
⛔ **대표님 쪽에서 한 번 돌려 봐 주십시오.**

---

# A. 홈 화면 넷 (이번에 하신 말씀)

## ① 전문가용 만세력 — 짙은 바탕

```
[전]  크림 바탕 #FFFBF7 · ★0.5px 테두리 · 글씨 #5a4a3e
      ⇒ 위 서비스 카드들과 «같은 옷» 이라 «다른 물건» 인 것이 안 보였습니다.
      ⇒ 0.5px 은 45부에 «기준 미달» 로 걷어낸 굵기인데 여기만 남아 있었습니다.
[후]  ★#33281F 바탕 · #F6EDE2 글씨 · 1.5px 테두리 · 왼쪽 금색 띠 · PRO 딱지
      설명 한 줄 「원국 · 지장간 · 신살까지 한 화면에」
```

🔴 **대비를 «재서» 넣었습니다** (그물 57 ① 이 매번 다시 잽니다)

```
글씨   #F6EDE2 on #33281F  →  ★12.38 : 1   (본문 기준 4.5)
설명   #CBB9A6 on #33281F  →  ★ 7.53 : 1
화살표 #E8C58A on #33281F  →  ★ 8.73 : 1   (선 기준 3.0)
```

⚠️ **새 색을 지은 것이 아닙니다.** 이 앱의 가장 진한 잉크(#5a4a3e)를 한 단계 더 내린 값입니다.
⚠️ 누를 수 있는 것이 `div` 였던 것을 **`button`** 으로 바꿨습니다 (키보드·읽어주기).

## ② 「함께 쓰는 서비스」 제목 통일

```
[전]  fontSize 12 · 굵기 없음 · 색 ★#96502e (벽돌빛)
[후]  fontSize 12 · fontWeight ★700 · 색 ★#55636f
      ⇒ 「전체 서비스」와 ★한 값도 안 다릅니다. 아이콘·사이·아래 여백까지 같습니다.
```

🔴 **그물 57 ② 가 «두 파일을 대조» 합니다.** 한쪽만 고치면 멈춥니다.
(말로 「같게 두십시오」라고 적어 두는 것으로는 안 지켜집니다 — 7부 0-1)

## ③ 큐보드 · 골프온 카드

```
[전]  모서리 12 · 그림자 없음 · 왼쪽 띠 없음 · 아이콘이 ★지름 10px «점» 하나
      ⇒ 위 카드들과 «마감» 이 달라 덜 만든 것처럼 보였습니다.
[후]  ★홈 cardStyle 과 «같은 값» —
      모서리 16 · 1.5px 선 · inset 4px 왼쪽 띠 · 0 2px 8px rgba(0,0,0,0.04) · overflow hidden
      아이콘은 ★타일(32px)로 — 🎱 당구 · ⛳ 골프
```

⚠️ **왼쪽 띠만 앱 빛깔**입니다 (큐보드 보라 #5a4a7a · 골프온 초록 #3B6D11).
마감은 같게, 어느 앱인지는 갈리게. **지갑 딱지 색과 맞춰 둔 값**이라 새로 짓지 않았습니다.

## ④ 하단바 「선생님 소개」 숨김

```
⛔ ★줄을 «지우지» 않았습니다 — hidden: true 로 «가리기만» 했습니다.
   ⇒ 되살리실 때는 ★hidden 을 false 로 두기만 하면 됩니다.
✅ 남는 칸 셋 — 홈 · 문의사항 · 보관함
```

⚠️ **/teachers 화면은 살아 있습니다.** 지운 것이 아닙니다.
다만 지금 **이 앱 안에서 그리로 가는 길이 한 곳도 없습니다** (grep 으로 셌습니다).
주소를 바로 치면 열립니다 — `/teachers`

🔴 **정하실 것** — 선생님 소개를 아주 안 쓰실 것이면 그대로 두시고,
나중에 다시 쓰실 것이면 **마이페이지나 「문의사항」 안에 길을 하나** 두는 편이 낫습니다.

---

# B. 일진내정법 넷 (앞서 드린 것 — 10bu-1 과 같습니다)

```
① 💌 「여보! 사랑해」가 두 벌이던 것 → ★한 벌
② 리포트와 하단이 겹치던 것 → ★하단 카드 다섯 장을 걷고 «모달» 로
③ 열두 지지 표 → ★문점일 칸 안으로 (문점일 하나로 정해지는 «범례» 입니다)
④ 띠 열둘 · 달 열둘 → ★알약 + 모달
```

🔴 **③ 에 대한 답** — 열두 지지는 **사람마다 다르지 않습니다.**
`sinGungTable(ilJi)` 가 **문점일 일진 지지 하나**로만 정합니다.
생년월일·시·성별을 **한 글자도** 안 씁니다. **같은 날 오신 분은 열두 칸이 모두 같습니다.**

---

# ✅ 되살려 본 것 — 열한 번 모두 잡혔습니다

```
[일진내정법 · 검사 56]
  💌 를 두 벌로 / 손님 자리 표시 뺌 / 열두지지 다시 맨 아래 /
  달 다시 펼침 / 자리별 상세 또 그림          → ★다섯 번 다 실패로 잡힘

[홈 · 검사 57]
  전문가용 다시 크림 바탕 / 제목 옛 벽돌빛 / 제목 굵기만 뺌 /
  카드 모서리 12 / 하단바가 옛 목록 / hidden 을 false → ★여섯 번 다 실패로 잡힘

되돌린 뒤 → ★통과 3,383 · 실패 0
```

---

# 🔴 대표님이 «눌러» 보실 것

**홈 화면**
```
□ 전문가용 만세력이 ★짙은 바탕에 PRO 딱지로 보이는지
□ 「함께 쓰는 서비스」 제목이 ★「전체 서비스」와 같은 크기·색인지
□ 큐보드·골프온 카드에 ★왼쪽 색 띠(보라·초록)와 아이콘이 보이는지
□ 하단바가 ★세 칸(홈·문의사항·보관함)인지
```

**일진내정법**
```
□ 맨 위 「여보! 사랑해」가 ★한 번만
□ 문점일 아래 열두 칸의 ★진한 칸이 손님 자리와 맞는지 (사진의 손님이면 丑·子·寅)
□ 리포트 연지·월지·일지·시지·운시 ★[자세히] → 모달
□ 띠 알약 — ★상문·공망 띠는 「줄이 없습니다」 가 나와야 맞습니다
□ 달 알약 열둘 · ★이번 달이 진한지
```

⚠️ 일진내정법 화면 아래의 **자리별 카드 넷과 운시 카드가 사라진 것이 맞습니다.**
없어진 게 아니라 모달로 갔습니다.

---

# ⛔ 손대기 전에

```
⛔ ★전문가용을 크림 바탕으로 되돌리지 마십시오 (검사 57 ①)
⛔ ★제목 색·굵기를 «한쪽만» 고치지 마십시오 — 두 파일이 같은 값이라야 합니다 (57 ②)
⛔ ★자매 카드 모서리를 12 로 되돌리지 마십시오 (57 ③)
⛔ ★HOME_NAV 를 바로 그리지 마십시오 — HOME_NAV_SHOWN 입니다 (57 ④)
⛔ ★「선생님 소개」 줄을 지우지 마십시오 — 가려 둔 것입니다
⛔ ★하단 자리별 카드를 다시 만들지 마십시오 (56 ⑰)
⛔ ★열두 지지 표를 다시 맨 아래로 내리지 마십시오
⛔ ★띠 이름 표를 «세 번째» 만들지 마십시오 (jijiTrait · TodayFortuneCard 에 이미 둘)
⛔ ★sinButton 이 «일곱» 자리입니다 (56 ⑭)
⛔ ★eslint 를 한 개도 늘리지 마십시오 (기준선 85 / 147)
```

---

```
문의 — lysh6728@naver.com · 010-6493-2252
```
