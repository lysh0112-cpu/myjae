# 58부 ⑤ — 성씨를 «성씨 DB에서만» 가져오기 · 코드 반영

```
작성   2026-08-12
기준   f175cd7 «52152» 에서 이어짐
지시   「성씨는 성씨 DB에서만 가지고 오도록 해야 해」                    [대표님]
       「이씨 고를 때 성씨인 이자만 나오면 되는데 엉뚱한 이자들이
         모두 나와서 헷갈리게 한다는 것이지」                            [대표님]
       「나중에 관리하기도 편하고」                                      [대표님]
검사   tsc 통과 · verify ★통과 1,703 · 실패 0  (1,690 → ★열셋 늘었습니다)
       eslint 82/135 ★기준선 그대로
       ★npx next build — 100/100 화면 그려짐 (실제로 돌려 확인 · 폰트·env 되돌림 완료)
```

⚠️ **SQL 을 «먼저» 넣으셔야 합니다** — `01` · `02` · `04` 가 먼저입니다.
⚠️ **npm 새 꾸러미 없습니다.**

---

## ★넣는 법 — 저장소 뿌리에서 풀면 끝입니다

```
myjae/                                              ← ★여기서 풉니다
├─ lib/saju/surnameDb.ts                            ★신설 — 성씨 읽기 «단일 창구»
├─ lib/saju/hanjaRow.ts                             ★덮어쓰기 (source 칸 하나 더함)
├─ app/manseryeok/naming/diagnosis/page.tsx         ★덮어쓰기
├─ app/manseryeok/naming/rename/newhanja/page.tsx   ★덮어쓰기
└─ 28-verify-naming-flow.ts                         ★덮어쓰기 (검사 그물)
```

```bash
npx tsc --noEmit      # 아무것도 안 나오면 정상
npm run verify        # ★1,703건 · 실패 0
npx eslint .          # ★82 errors, 135 warnings  ← 기준선
```

---

## 1. ✅ 무엇이 달라지나

```
[전]  이여진 → 「이」 →  李 · 異 · 伊 · ★以      ← 以 는 복성 이선(以仙) 의 앞 글자
[후]  이여진 → 「이」 →  李 · 異 · 伊            ✅

      이선○○ → 「이선」 →  ★以仙 카드 한 장 (두 글자가 함께 채워집니다)
```

```
[전]  ① hanja 표에서 «그 소리 한자를 전부» 꺼내고
      ② surnameAllowed.ts(코드)로 «걸러냄»
   ⇒ 성씨 목록이 «뿌리» 가 아니라 ★«거르개» 였습니다.

[후]  ① ★surname_hanja 표가 «보일 글자» 와 «차례» 를 정하고
      ② hanja 표는 ★«값만» — .in('hanja', 목록) 으로 «한자로» 찾습니다
```

★**이제 대표님이 Supabase 에서 한 줄만 고치시면 화면이 바뀝니다.** 배포가 필요 없습니다.

---

## 2. ✅ 덤으로 풀린 것 — 두음법칙

```
같은 한자가 두 소리에 있습니다 —  劉 呂 李 林 柳 梁 羅  ★일곱
    나 羅  不用(미확인)   ↔   라 羅  中吉(새 책)

★소리로 찾으면 「나 羅」가 옛 값에 묶입니다.
★한자로 찾으면 두 줄이 다 잡히고 pickBest 가 «새 책 줄» 을 고릅니다.
```

---

## 3. ⛔⛔ 가장 조심한 자리 — «소리를 덮어씁니다»

```
surnameDb.ts   return { ...row, hangul: sori }
                                 ↑ ★손님이 «쓴» 소리로 덮습니다

[왜]  ① 교재는 «표기음 그대로» 가 정답입니다 (dueum.ts 머리말).
      ② 화면 자물쇠가 row.hangul !== slot.hangul 이면 «거부» 합니다.
      ③ 발음오행 판정이 slot.hangul 을 씁니다.

⛔ 덮어쓰지 않으면 ★「나」로 쓴 손님이 「라」(火)로 판정됩니다.
   ★화면은 «멀쩡히» 뜨고 값만 조용히 갈립니다 (48부 1-4 와 같은 모양).
⇒ 28-verify 가 이 한 줄을 «세어» 지킵니다.
```

---

## 4. ⛔⛔ 걷어낸 것 — 「0개면 전체로 되돌리기」

```
newhanja  const scoped = scopedAll.length > 0 ? scopedAll : scored   ← ★걷어냈습니다

🔴 걸러서 0개면 ★전체를 도로 보여 주던 줄입니다.
   성씨가 아닌 글자(硫·瘤·謬)가 다시 나올 수 있었습니다.
⚠️ 정밀분석(diagnosis)에는 «없던» 줄입니다 —
   주석엔 「같은 규칙입니다」라 적혀 있었는데 ★실제로는 두 화면이 어긋나 있었습니다.
⚠️ 48부 4-2 에서 대표님이 상담사 쪽 「0명이면 전체」를 «일부러» 빼신 것과 같은 모양입니다.
```

⇒ 이제 두 화면이 **정말로 같은 규칙**입니다.

---

## 5. ⚠️ 안 막았습니다 — 세 갈래 안전망

```
① 성씨 표에 «그 소리가 없으면»  → 예전처럼 그 소리 «전체» 를 냅니다
   ⚠️ 귀화하신 분·드문 본관. 막으면 그 집안이 다음으로 못 넘어갑니다.
   ⚠️ 이것은 「0개면 전체」 되돌림이 «아닙니다» — 표에 «있는» 소리는 여기 안 걸립니다.

② 복성 카드가 «못 뜨면»            → 그 자리 낱글자를 «곁들여» 냅니다 (57부 확정)

③ surname_hanja 표를 «못 읽으면»   → 예전 길(surnameAllowed.ts)로 내려갑니다
   ⇒ 배포 순서가 어긋나도 손님이 «막히지» 않습니다.
   □ 표가 자리를 잡으면 그 되돌림과 코드 파일 둘을 걷어냅니다.
     ⛔ ★지금 지우지 마십시오.
```

---

## 6. ★검사 그물 — 열셋 늘었습니다 (1,690 → 1,703)

옛 검사 셋이 «옛 모양» 을 붙잡고 걸렸습니다.
⛔ 느슨하게 풀지 않고 **새 모양으로 다시 조이고 검사를 더 넣었습니다** (57부 방식).

```
[고침]  성씨 칸 거르기 — 한 줄 가르기 → ★if (isSurnameSlot) 갈래
        성씨 창구      — surnameHanja → ★surnameDb
        차례           — surnameRank(syl,…) → ★표의 rank

[새로 넣음]
  ★성씨 값을 «한자로» 찾는가 (소리로 안 찾는가 — 두음 짝)
  ⛔⛔ 돌려주는 줄의 소리를 «손님이 쓴 소리» 로 덮는가      ← ★가장 중요
  ⛔ 복성도 덮는가
  ⛔ ref_* 를 «한 번도» 안 읽는가 (값의 정본은 hanja 표)
  ⛔ 품격(不用)을 안 보는가 · 그 까닭이 적혀 있는가
  ⛔⛔ 「0개면 전체로 되돌리기」가 «없는가»
  ⛔ 두 화면이 «다시» 거르지 않는가
  ⚠️ 복성 안전망(extraHanja)이 있는가
  ⛔ 성씨 한자를 화면에 «직접 적지» 않았는가 (以 가 소스에 없어야)
  ★표의 rank 로 줄을 세우는가
  ⚠️ 이름 칸은 예전 그대로 listPolicy 로 거르는가
  ⚠️ 옛 «한 줄 가르기» 가 남아 있지 않은가
```

---

## ⛔ 손대지 말 것 (58부 코드분에 더해짐)

```
⛔⛔ ★surnameDb 가 돌려주는 줄의 hangul 덮어쓰기를 «빼지» 마십시오 —
     「나」로 쓴 손님이 「라」(火)로 판정됩니다. 화면은 멀쩡히 뜹니다.

⛔⛔ ★성씨 표의 ref_* 를 «판정에» 쓰지 마십시오 —
     획수·자원오행의 정본은 hanja 표입니다. surnameDb 는 ref_* 를 한 번도 안 읽습니다.

⛔⛔ ★「걸러서 0개면 전체를 도로 보여 주는」 되돌림을 «다시 넣지» 마십시오.

⛔ ★성씨 목록·차례를 화면에 «다시 적지» 마십시오 — 판단은 surnameDb.ts 한 곳입니다.

⛔ ★복성 안전망(extraHanja)을 빼지 마십시오 — 카드가 못 뜨면 그 집안이 막힙니다 (57부).

⛔ ★lib/saju/surnameAllowed.ts · surnameHanja.ts 를 «지우지» 마십시오 —
     ① surnameDb 의 되돌림이 아직 씁니다
     ② newhanja 가 «추천 / 그 외» 를 가를 때 surnameRank 를 씁니다
   □ 표가 자리를 잡고 값이 같은 것을 확인한 뒤에 걷어냅니다.

⛔ ★값이 없는 글자를 «획수 0 으로» 내보내지 마십시오 —
     surnameDb 는 hanja 표에 값이 없으면 ★내지 않고 console.warn 을 남깁니다.
     ⇒ 그 글자는 _SQL_58bu_성씨_03_점검.sql 의 ②번이 세어 줍니다. hanja 표에 채우십시오.
```

---

## 다음

```
□ ★점검 SQL(03) 결과 보기 — 성씨인데 hanja 표에 값이 «없는» 글자
□ 화면에서 실제로 눌러 확인 — 「이여진」 「이선○○」 「류」 「황」
□ 그 뒤에 surnameAllowed.ts · surnameHanja.ts 걷어내기 (⛔ 지금 아님)
```
